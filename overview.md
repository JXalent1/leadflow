# LeadFlow

## Purpose
A lightweight, self-hosted SMS lead-generation tool for home-service businesses. It skip-traces a homeowner list, runs an SMS outreach campaign, captures and triages replies, and forwards qualified leads to the client (first client: Talan, a Tallahassee window-cleaning operator). Built to prove conversion on a 500-record pilot before scaling.

## Goals
- Launch a first SMS campaign to ~500 Tallahassee homeowners and measure delivery, reply, positive-reply, and opt-out rates.
- Auto-handle inbound replies: classify interest, honor STOP instantly, forward hot leads to Talan.
- Keep the whole thing free/cheap to host (Vercel + serverless + a free-tier DB).

## Scope

**In scope (MVP):**
- CSV upload + Tracerfy skip-trace integration (append mobile numbers).
- DNC + litigator scrub step (via Tracerfy scrub endpoint) with hard suppression of flagged numbers.
- Twilio outbound SMS send, paced/throttled, from an aged 10DLC-registered number.
- Inbound reply webhook: STOP/opt-out handling + interest classification.
- Lead delivery to Talan when a reply signals interest: shown on the dashboard + an instant SMS ping.
- A basic dashboard: list status, send progress, live reply feed, opt-out count, lead count.

**Out of scope (for MVP):**
- Multi-tenant / multi-client accounts (single campaign, single client for now).
- Billing, auth beyond a single admin password.
- AI-generated reply conversations (MVP uses keyword/classifier triage + human handoff; richer AI qualifier is a later module).
- Number rotation / multi-number pools (single aged Twilio number for the pilot).

## Stack / Tools
- **Frontend/host:** Next.js (App Router) on Vercel free tier.
- **Backend:** Next.js API routes / serverless functions.
- **DB:** Neon Postgres free tier, provisioned via the Vercel Marketplace integration (driver: `@neondatabase/serverless`). Stores contacts, messages, opt-outs, leads.
- **SMS:** Twilio API (aged 10DLC-registered number, already verified).
- **Skip trace + scrub:** Tracerfy API — https://www.tracerfy.com/skip-tracing-api-documentation/
- **Lead delivery:** the dashboard (primary surface) + an instant Twilio SMS ping to Talan. No email/Resend.

## Stakeholders
- **Jordan** — builder/owner (DRX). Runs the platform.
- **Talan** — first client (Tallahassee window cleaning). Receives forwarded leads.

## Key Decisions
_Running log. Most recent on top. Format: `YYYY-MM-DD — decision — why`._

- 2026-06-27 — **Per-client opt-out keyword: a client may advertise AND honor an additional opt-out trigger (e.g. `Reply "2" to opt out`) — this SUPERSEDES/CLOSES the 2026-06-22 "Type 2 to opt out was rejected" concern by actually suppressing on "2".** Why: onboarding a 2nd client (Jeremy, Orlando powerwashing) who wants a `"2"` opt-out instruction, AND a usable operator UI to add/edit clients. The 2026-06-22 rejection was correct *at the time* — the system only suppressed on STOP-family keywords, so advertising "2" without honoring it would be a TCPA break. We now honor it the RIGHT way: new nullable `clients.optout_keyword` (+ optional `optout_instruction`); a **pure `isConfiguredOptOut(body, keyword)`** that matches the keyword **only as the EXACT whole normalized body** (trim/lowercase/strip surrounding quotes+punctuation) — so for `'2'` ONLY `"2"`/`" 2 "`/`'"2"'`/`"2."` opt out, **NEVER** a "2" inside other text (`"2 services"`, `"call me at 2pm"`, `"I have 2 dogs"`, `"$200?"`). In `lib/inbound.ts` the opt-out decision is `optedOut = isOptOut(body) || (keyword && isConfiguredOptOut(body, keyword))`, checked BEFORE classification with the SAME unconditional precedence as STOP — `isOptOut` (STOP family) stays authoritative + always-on + **byte-unchanged**; the keyword is purely ADDITIVE. `lib/sms.ts renderMessage` gained a per-client opt-out line (`optOutInstructionFor(keyword, instruction)`): the safety guard now checks for/append's THAT client's line, so a "2" client never gets a contradictory second STOP line. **Talan (client 1) keeps `optout_keyword=NULL` → render + opt-out are BYTE-IDENTICAL** (proven). **Operational note (operator choice, not a lecture):** carriers honor STOP at the carrier level regardless of the visible copy, and 10DLC traffic omitting standard STOP language can see more carrier filtering — we keep STOP fully working in the classifier no matter the advertised line. **Onboarding UI:** operator can `+ New client` / `Edit` from the cockpit (`POST`/`PATCH /api/clients` + `lib/clients.ts createClient`/`updateClientConfig`) — number, copy, opt-out keyword, window/tz/rate, lead target — with a **live message preview** (real `renderMessage` + `segmentInfo` + the per-client opt-out line) and a client-login creation field. Verified: tsc/build green, `npm test` = **239** (+31: exact-match keyword, configured opt-out precedence, per-client opt-out line with no double line, Talan byte-identical), isolation 28/28, access/cockpit/auto-pause/passthrough all pass, and a new **`npm run test:optout`** live-DB fixture (throwaway client keyword='2' → "2"/STOP suppress, "2 services" does not, STOP-only config does not, Talan stays NULL) self-cleaned → DB pristine.

- 2026-06-25 — **Send-rate cap raised 1000 → 20000/hr; send-batch cap raised 50 → 250.** Why: the operator is an experienced, A2P-compliant 10DLC sender and wants to send much faster than 1,000/hr — the old 1000 clamp (`setClientSendRate`) and the 50-send batch cap (`sendBatchSize`) were early-build artifacts that throttled throughput above ~1,000/hr. New ceiling `MAX_SEND_RATE_PER_HOUR = 20000` (shared pure `clampSendRate` in `lib/pipeline.ts`, used by the API setter + the UI input `max`); batch size still `≈ rate/20` but clamped `[1, 250]`, so the worst-case batch (where the cap first binds, ~5000/hr) is ~179s and every higher rate is faster (10000/hr ≈ 90s, 20000/hr ≈ 45s) — always under the 300s function limit, with the realized rate ≈ target (within-batch pacing scales with the rate). **No safety touched:** the atomic `claimForSend` (no-double-send), the send-window re-check each batch, and opt-out/suppression/eligibility are all independent of the rate and unchanged. Verified: tsc/build green, `npm test` = 211 (pacing tests updated to the new clamps + a realized-rate ≈ target assertion + a `clampSendRate` test), isolation/access/cockpit/auto-pause/passthrough all pass.

- 2026-06-25 — **V7 phase 1 (operator launch UI redesign + no-scrub toggle) BUILT + deployed.** **Design system DECIDED:** light theme, ONE confident accent **indigo-600** (hover `indigo-700`) with `emerald` (success/leads), `amber` (warning/behind-pace), `red` (danger), `slate` neutrals; **Inter** via `next/font`; generous whitespace, `rounded-lg`/`xl`, soft borders + shadows, a visible `indigo` focus ring. Built a **shared Tailwind-only UI kit** in `components/ui/` (no new deps; inline SVG icons) — `Button` (primary/secondary/ghost/danger + loading), `Card`/`CardHeader`, `StatTile`, `Badge` (tone-based), `Field`/`Input`/`Select`, `SegmentedToggle`, `ProgressBar`, `AppHeader`, `wordmark`/`icons` — so the three screens are consistent and V7 phase 2 (client portal + inbox) is a fast follow. Redesigned **login** (branded card, loading submit, inline error), the **operator cockpit** (app header, summary strip, polished client cards: pace badge, progress bar, health stat tiles, restyled billing line — behind-pace-first sort + drill-through unchanged), and the **operator dashboard** (app header, count stat-tile grid, send-progress card, prominent Run pipeline, manual stage controls collapsed into a secondary area). **Functional add:** a **no-scrub toggle** on the upload form (`components/campaign-bar.tsx`, segmented "Standard" vs "No scrub — send whole list") wired to the EXISTING `scrubMode` field on `POST /api/campaigns` (default 'vendor'), plus a per-campaign mode badge on the selector — so the 2,500 `scrub_mode='none'` launch is fully point-and-click with no command. **No send/eligibility/suppression/auth/API behavior changed** beyond surfacing the existing param. Verified: tsc clean, build green, `npm test` = 208, `test:isolation` = 28/28, `test:access`/`test:cockpit`/`test:auto-pause`/`test:passthrough` all pass; throwaway create asserted `scrub_mode='none'` then deleted (live DB pristine). Deployed `vercel --prod`.

- 2026-06-25 — **Module N (no-scrub send mode) BUILT.** Per-campaign `campaigns.scrub_mode text NOT NULL DEFAULT 'vendor'` ('vendor'|'none'); existing rows + the Talan pilot backfill to 'vendor' (byte-unchanged). On a `'none'` campaign the scrub stage is a **passthrough** (`lib/scrub-passthrough.ts` `passthroughScrubBatch`): ONE scoped UPDATE marks the campaign's matched + with-phone + still-`pending` contacts `scrub_status='clean'` with **NO Tracerfy/vendor call, NO `getCredits`, NO `submitScrub`, NO `scrub_jobs` row** — the selection mirrors `getContactsForScrub`'s predicate exactly, minus the vendor call. `app/api/scrub` looks up the campaign's `scrub_mode` and branches: `'none'` → passthrough, else the EXISTING `scrubBatch` runs byte-for-byte; the passthrough returns the same `{scrubbed,clean,suppressed}` shape so the pipeline driver loops + drains identically (`components/pipeline-runner.tsx` untouched). Mode is set on create (`POST /api/campaigns` optional `scrubMode`, default 'vendor') or flipped on an existing campaign (`PATCH /api/campaigns {campaignId, scrubMode}` — operator-only, client-scoped, validates 'vendor'|'none', 404 on a foreign campaign). **LOAD-BEARING invariants held:** `getEligibleContacts`/`claimForSend`/the inbound webhook/opt-out logic are UNCHANGED — passthrough touches `scrub_status` only, and the `NOT EXISTS opt_outs` exclusion is independent, so an **opted-out contact stays excluded even after passthrough marks it clean** (proven). Idempotent (only flips `pending` rows; re-run is a no-op) and `limit`-respecting. **Verified:** `tsc` clean, build green, `npm test` = 208, `test:isolation` = 28/28, `test:access` pass, `test:cockpit` pass, `test:auto-pause` pass, new **`npm run test:passthrough` = 24/24** (marks clean w/ no vendor call + no scrub_jobs created; opted-out-but-still-excluded; no-phone stays pending; 'vendor' routes to scrubBatch; idempotent/limit; setter validates + can't cross clients; client 1 byte-unchanged). Schema applied live (65 stmts). Live DB pristine after. **Deviation:** the explicit live HTTP smoke was covered by `test:passthrough` (same live-DB code path; login is a server action and 0 users are seeded, so a scripted HTTP login is brittle — the route is a thin auth+branch wrapper validated by tsc+build). **Owes a single-reviewer correctness pass** (touches send-eligibility). The TCPA litigator scrub (Module S) is still NOT used on this campaign.

- 2026-06-25 — **DECIDED: add a per-campaign `scrub_mode='none'` (passthrough) so a campaign can be SENT without a vendor DNC scrub — for the 2,500 launch.** Why: the operator pre-filters the list at the source (county roll → target zips → deduped against prior days) and wants to send it as-is, no Tracerfy/TCPA scrub on this campaign. Because send-eligibility requires `scrub_status='clean'`, the scrub stage becomes a **passthrough** that marks the campaign's traced, with-phone, still-`pending` contacts clean with NO external call or spend. **Eligibility query + opt-out/STOP suppression are UNCHANGED** — an opted-out contact stays excluded even though it's marked clean (the `opt_outs` check is independent). Default stays `'vendor'` (existing Tracerfy scrub), so Talan's pilot is byte-unchanged. Spec: `sessions/no-scrub-mode.md` (Module N). The TCPA litigator scrub (Module S) is still being built but is NOT used on this campaign, by operator decision.

- 2026-06-25 — **DECIDED: replace the full DNC scrub with a LITIGATOR-ONLY scrub via TCPA Litigator List; keep Tracerfy for the skip-trace.** Why: the Tracerfy scrub suppressed on ANY flag (federal DNC + state DNC + DMA + litigator), which removed ~40–50% of the list — and in the live pilot scrub logs the litigator count was ZERO, i.e. ALL of that list loss was the DNC *registry*, none of it litigators. The registry scrub is where both the cost (~$0.02/number) and the volume loss come from; the litigator filter removed nothing. So we **decouple** them: drop the DNC-registry scrub and keep ONLY the litigator/TCPA scrub. **Vendor = TCPA Litigator List** (api.tcpalitigatorlist.com) — self-described most-affordable suppression service, ~**$0.001/number** (vs Tracerfy ~$0.02 ⇒ ~20× cheaper), HTTP Basic auth, `POST /scrub/phones/` with `small_list:true` for ≤3000 numbers returns verdicts immediately (our lists are 2,000–2,500, one call). **`type=tcpa`** scrubs ONLY the TCPA litigator + TCPA-troll list (NOT `dnc_fed`/`dnc_state`/`dnc_complainers`), so federal/state-DNC numbers pass through as sendable and only actual litigators/trolls are suppressed — exactly the intended "keep the volume, drop the people who sue" behavior. Net all-in per textable contact drops ~$0.066 → ~$0.035 (trace stays Tracerfy $0.02 + scrub ~$0.001), matching/beating the ~$0.02/person competitor benchmark. **Build = a pluggable scrub-provider seam** (`SCRUB_PROVIDER` env, default `tcpa-litigator`) so Tracerfy's scrub stays available but inactive (reversible, NOT deleted); the existing fail-closed contract is preserved BYTE-FOR-BYTE (a contact is `scrub_status='clean'` ONLY on an explicit clean verdict; missing/error/credit-exhaustion ⇒ stays pending, never clean) and STOP/opt-out suppression + eligibility are UNTOUCHED. The scrub account is the operator's, shared across all clients ⇒ global env, no per-client schema change. **Operator action required before it runs live:** create a TCPA Litigator List account, generate API username/password, fund scrub coins, set `TCPA_LIST_API_USERNAME`/`TCPA_LIST_API_PASSWORD` (+ optional `TCPA_SCRUB_TYPE`, default `tcpa`) in Vercel. Spec: `sessions/scrub-litigator-swap.md` (Module S in modules-v2.md).

- 2026-06-25 — **v2 Module V6 (lead-target auto-pause + track-only billing) BUILT.** Reframed from "billing" to **deliver-then-stop**: each client has a per-period lead TARGET; once met, the send path stops for that client and resumes next period. **Decisions:** (1) **`lead_target` is NULLABLE** (not a NOT-NULL-DEFAULT) — null means "fall back to `lead_guarantee`", matching the codebase's billing_day/message_template nullable-inherit pattern and avoiding a re-apply-schema clobber of an operator-set value. So Talan (lead_target NULL) keeps target = guarantee = 50/month, byte-unchanged. (2) **Weekly period = ISO week, Monday 00:00 UTC → the following Monday 00:00 UTC (exclusive)** — UTC for determinism (matches the V4 billing-cycle convention), Mon-anchored so shifting `now` a week shifts the window exactly a week (clean rollover, a lead falls in exactly one week). Monthly period reuses the V4 billing cycle so auto-pause months line up with the guarantee window. (3) **The gate is SERVER-ENFORCED in the send route** (`getTargetStatus` in `lib/auto-pause.ts`, consulted in `app/api/campaign` before any claim/send, read fresh each batch) — hitting Run can never over-send. It is layered **ON TOP OF** suppression/eligibility and is **purely additive**: target-not-met never makes a suppressed/opted-out contact eligible, and target-met never touches eligibility — it just halts that client. `target <= 0` never auto-pauses (can't strand sending). (4) **Billing is TRACK-ONLY, NO Stripe** — a `client_invoices` table (one row per client per billing cycle, idempotent on `(client_id, period_start)`) with due→invoiced→paid status the operator marks from the cockpit; collection is manual outside the app. **Verified:** build green, `npm test` = 208 (+9 lead-target), `test:isolation` = 28/28, `test:access` = 28, `test:cockpit` pass, new `test:auto-pause` proves the server gate stops EXACTLY at the target (off-by-one safe, contacts still remaining), resumes on raise/rollover, holds for week + month, never weakens suppression, and that billing materializes/marks/scopes correctly + surfaces on the cockpit. Live DB pristine after. **Deferred (out of V6 scope):** the lead-target UI inputs (backend setter + `PATCH /api/client` support shipped; the input widget rides V7's redesign) and Stripe (still later/maybe-never). **Next: focused send-path review.**

- 2026-06-25 — **v2 Module V5 security-review HARDENING applied — all 5 logged Medium/Low items, zero behavior risk; V5 now fully review-clean.** Why: cheap defense-in-depth on the security-critical auth/access path, owed from the review. Applied: scrypt cost-param bounds in `verifyPassword` (N∈[16384,2^20], r∈[8,32], p∈[1,4] — a DB-write compromise can't downgrade/DoS the KDF); `SESSION_SECRET` runtime min 16→32; dummy-hash the no-such-user login path (kills the enumeration timing side-channel); 2 new `access-control-fixture` assertions (cross-client campaignId falls back to the user's own client; `/client` unreachable by an operator); `import "server-only"` on `lib/{session,guard,db,users,portal,cockpit}.ts`. **Server-only + tsx:** the published `server-only` package throws by default outside a React-Server build and the tsx fixtures import these libs directly, so an empty shim `shims/server-only.ts` is mapped via tsconfig `paths` — tsx resolves it to a no-op while `next build` keeps its own enforcing alias (build green, client bundles still clean). Deferred (correctly): a durable login rate-limiter (in-memory is fine pre-scale) and the client-portal `tel:` link (a product question). Verified: build green, `npm test` = 199, `test:isolation` = 28/28, `test:access` = 28 (+2), `test:cockpit` pass. **Next: V6 (billing).**
- 2026-06-25 — **v2 Module V5 SECURITY REVIEW (3-reviewer agent team, read-only Sonnet) — NO Critical/High; the V1 access-control gate is confirmed CLOSED and the auth is sound. Applied none (none qualified); logged Medium/Low.** Three reviewers (access-control / auth / correctness) independently confirmed both adversarial claims HOLD: "a logged-in client can do nothing but read their own leads" (a client 403s on every operator route by role, can reach only `GET /api/portal` locked to their own client_id + the `/client` page from the session — body-ids/campaignId are DB-scoped, no cross-client vector found) and "the session can't be forged or escalated" (HMAC-signed cookie verified before trust, `SESSION_SECRET` fail-closed, fresh token at login = no fixation, the user is re-loaded from the DB each request so role/client_id are authoritative). Per the lead directive (apply Critical+High, log Medium/Low), nothing was applied. **Logged for a follow-up (all zero-behavior-risk hardening, with concrete patches in status.md):** scrypt cost-param bounds in `verifyPassword` (defense-in-depth vs a DB-write compromise — Medium); raise the `SESSION_SECRET` runtime min 16→32; dummy-hash the no-user login path to remove a minor enumeration timing side-channel; a durable login throttle before scale (the in-memory one is per-serverless-instance); an audit log when the operator no-`?clientId=` default fires; two extra `access-control-fixture` assertions (cross-client campaignId fallback + operator-can't-reach-/client); a doc note on `/api/portal`'s `requireUser`; and `import "server-only"` guards on the server-only libs. Re-verified after the review: build green, `npm test` = 199, `npm run test:isolation` = 28/28, `npm run test:access` all-pass. **Module V5 is security-review-clean — ready for V6 (billing).**
- 2026-06-24 — **V1 `?clientId=` access-control gate CLOSED — the resolved client now comes from the logged-in user's session, never a bare request param (lib/access.ts `resolveClientIdForUser`).** Why: this was the hard prerequisite before onboarding a 2nd client. One pure chokepoint decides which client a request may act on: an OPERATOR may act on any client (`?clientId=` honored, else default 1); a CLIENT user is HARD-LOCKED to their own `client_id` and any foreign id returns null → the route 403s. Every operator route/page resolves through it (after `requireOperator`), and the parse-only `requestedClientId` can no longer select a client on its own. Proven by `npm run test:access` (a client-2 user resolves only ever to 2 or null, never 1, for `?clientId=∈{undefined,1,2,3,999}`) + a wired HTTP smoke (client role 403 on every operator route; `/api/portal?clientId=2` 403 for a client-1 user).
- 2026-06-24 — **Auth: scrypt password hashing + a stateless HMAC-signed session cookie, with the DB as the authoritative source of role/client_id.** Why: minimal but secure per the spec, no new dependency, and forgery-proof. **scrypt** (Node core, memory-hard, per-password random salt, constant-time verify) over bcrypt/argon2 — vetted and dependency-free. The session is a signed cookie `b64(payload).b64(HMAC-SHA256(payload, SESSION_SECRET))` — httpOnly + secure (prod) + sameSite=lax + 8h expiry; the signature prevents a client forging role=operator or another client_id. On every request we VERIFY the signature/expiry and then RE-LOAD the user from the DB by id, so a changed/deleted user immediately loses/changes access (revocation + fresh role) — the cookie carries identity, the DB row is the source of truth. `SESSION_SECRET` is REQUIRED (signing/verifying throws if unset → fail closed). Login throttling is basic in-memory (5 fails / 15 min) — documented as best-effort on serverless (per-instance), a durable limiter is later. NO OAuth/SSO/password-reset (out of scope). Users are seeded by `npm run seed:users` from env passwords (hashed) — never committed in SQL.
- 2026-06-24 — **Client dashboard is its own surface (`/client` + `/api/portal` + `lib/portal.ts`), strictly single-client and read-mostly — NOT a reuse of the operator `/api/dashboard`.** Why: the operator dashboard exposes pipeline counts / active runs / controls that a client must never see, and the spec wants the client view clean + minimal. The portal returns ONLY the client's own leads feed + progress to their guarantee (same cycle math as the cockpit), every query scoped to one `client_id` resolved from the session. It is the only API a client role can call (every operator endpoint requires the operator role); a client passing a foreign `?clientId=` gets 403.
- 2026-06-24 — **Billing cycle = `billing_day` → same day-of-month next month, computed in UTC; null `billing_day` = calendar month; a day past a short month's last day clamps to that last day.** Why: V4's cockpit needs "leads THIS cycle vs. the 50/mo guarantee" per client, and the spec allows a calendar month as an acceptable v1. We generalized slightly: when `billing_day` is set the cycle anchors on it (so the guarantee period matches the invoice period once V6 billing lands), and when it's null (Talan today) it's the calendar month. **UTC** boundaries make the math deterministic regardless of server timezone — the per-client *send window* timezone (`send_timezone`) is a separate concern and intentionally does NOT shift cycle boundaries (a lead's billing cycle shouldn't depend on the send TZ). Short-month clamping (e.g. day 31 → Feb 28/29) guarantees exactly one cycle boundary per month with no skipped/duplicated cycles. Math is pure + unit-tested in `lib/billing-cycle.ts` (`currentCycle`, `expectedLeads`, `paceFlag`).
- 2026-06-24 — **The operator cockpit aggregates SUMMARY metrics across ALL clients BY DESIGN — this is operator-only and is NOT an isolation violation.** Why: the agency model has one operator (Jordan) who owns every client, so the control room legitimately needs a cross-client view (who's behind their guarantee, where to push more campaigns). We confined this to ONE function (`lib/cockpit.ts getCockpitData`) that returns per-client COUNTS only (leads / sent / inbound / opt-outs this cycle) — never contact-level data — reachable ONLY from the operator-authed landing (`app/page.tsx` behind `isAuthed()`). No client-FACING route consumes it; per-client drill-downs (the dashboard / inbox) stay strictly single-client-scoped, and per-client logins that replace the shared admin password are still module V5. The light review (single read-only reviewer) grep-confirmed no client surface imports `getCockpitData`/`listClients`/`CockpitView`, so the cross-client read can't leak to a client view. **Pace flag** = `behind` when leads < guarantee×(daysElapsed/cycleLength), `on_track` when ≥, `met` when leads ≥ guarantee; behind clients sort first (the actionable bit).
- 2026-06-24 — **v2 Module V4: operator lead-guarantee cockpit BUILT — the new landing showing every client's leads-this-cycle / guarantee + a behind/on-track/met pace flag; light-review-clean.** Why: the agency's central metric is leads-this-cycle vs. the 50/mo guarantee per client, and the operator needs one screen to see who's behind and where to run more campaigns. **What shipped:** `app/page.tsx` (the admin-authed landing) now renders `components/cockpit-view.tsx` — a card per client (sorted behind-first) with leads/guarantee + progress bar, the pace badge, this cycle's sent count + reply rate + opt-out rate, and days-left; clicking a card opens `/dashboard?clientId=N` (the existing scoped dashboard, now with a "← All clients" back-link). The cross-client aggregate is `lib/cockpit.ts getCockpitData()` (operator-only, per-client summary COUNTS scoped by `client_id` AND the `[cycleStart, cycleEnd)` window — see the two decisions above), fed by `listClients()` (new in `lib/clients.ts`) and the pure cycle/pace math in `lib/billing-cycle.ts`. **Did NOT touch** send / suppression / eligibility / the inbound webhook — V4 adds no send surface. **Verified:** tsc clean, build green (`/` 138 B client, no DB driver leaked), **`npm test` = 183** (+10 billing-cycle), **`npm run test:isolation` = 28/28** (unchanged), new **`npm run test:cockpit`** fixture (proves the cockpit counts ONLY this cycle's leads per client, the pace flag is right, health rates correct, client 1 unchanged) — live DB pristine after (1 client, 0 stray rows). **Light review PASSED** — no Critical/High; cycle math (incl. short-month clamp + boundary inclusivity), pace edges (day-0, zero-guarantee), SQL scoping with no leakage, and the operator-only reachability all confirmed. **Out of scope (untouched):** client logins / `?clientId=` access control (V5), billing (V6), visual redesign (V7). **Next: the V5 prompt (client dashboard + per-client login).**
- 2026-06-24 — **v2 Module V3 focused send-driver review pass (agent team) — both load-bearing claims HOLD; no Critical/High; applied 1 cheap Medium, logged the rest.** A 2-reviewer read-only team on Sonnet (correctness / compliance, `Explore`) audited the driven send pipeline and challenged each other on "the driver can never double-text and can never text an opted-out phone, even across many batches." **Both independently confirmed both HOLD.** **No double-send:** `claimForSend`'s single-statement atomic `UPDATE contacts SET send_status='sending' WHERE … send_status='not_sent' RETURNING id` is the SOLE gate before any Twilio call — Postgres row-locking means only one writer wins per contact regardless of batch count, runId adoption, or two tabs sharing one run (the loser gets `skippedClaimed`); a sent/sending row leaves the eligible pool so it's never re-selected; the two-tab co-drive and "stale run permanently blocks" claims were both explicitly REFUTED. **Suppression re-checked every batch:** every driven batch flows through `getEligibleContacts` (client-scoped `NOT EXISTS opt_outs`, last-10-digit) AND the same opt_outs re-check inside the atomic `claimForSend`; a STOP landing mid-run writes a normalized `opt_outs` row (webhook → `recordOptOut` + `markSuppressed`, idempotent incl. the Twilio-retry recovery path) that the NEXT batch excludes AND that refuses any contact already fetched into the CURRENT batch at claim time — no window across batches can text a STOPped phone. The run-lifecycle extraction (`lib/campaign-runs.ts`) and the rate change touch NO eligibility/suppression logic; trace/scrub stay fail-closed under looping (no-match → suppress, any flag → suppress-first-then-flag, credit-exhaustion throws before submit so nothing is marked clean); scoping stays client_id + campaign_id on every query incl. the `limit:1` remaining-probe; the per-iteration window re-check + route window guard keep sends inside the window; the "undefined" counter is fixed on every real-send path (driver coalesces `Number(data.sent ?? 0)`). **Applied (Medium, correctness M1):** wrapped the batch send + finalize in `app/api/campaign/route.ts` in a try/finally so an EXCEPTION (DB error in `runSend`, a Twilio throw, a finalize-write failure) always closes the run (`finishCampaignRun`, delta 0) instead of leaving it open to 409-block the next Run — the staleness guard would self-heal in ≤6 min, but this drops the blackout to zero and can't re-introduce the stall via an error path (already-sent contacts stay `sent`, so a re-Run safely resumes the remaining `not_sent`). **Logged, NOT actioned:** (L1) two tabs sharing `activeRunId` can co-drive one run for concurrent batches — NO double-text (the claim guarantees it), surprising-timing only; per-session run ownership is module V5 (client logins). (L2) the driver HALTS on a `campaign_already_running` 409 with a "reload to resume" message rather than auto-adopting the handed-back `runId` — **deliberately kept** (auto-adopt would defeat "block a second concurrent operator," an acceptance criterion; the legit resume path already adopts `activeRunId` from a fresh snapshot on the first send batch, and reload re-fetches it). (cosmetic) `opt_outs`/`campaign_runs` carry `client_id`/`campaign_id` via `ALTER` rather than in the `CREATE TABLE` body — self-documentation only, FK + NOT NULL are enforced in the same apply. **Re-verified after the fix:** `tsc` clean, `npm run build` green, **`npm test` = 173**, **`npm run test:isolation` = 28/28**. **Module V3 is review-clean — ready to generate the V4 prompt (operator lead-guarantee cockpit).**
- 2026-06-24 — **v2 Module V3: guided pipeline + de-finicky BUILT — one-click trace→scrub→send via a client-side driver; the send-batch stall + "undefined counter" fixed; live send-rate control.** Why: after the Talan pilot the operator had to babysit the run (re-click per batch, a stalled run that never closed and then blocked "Start send", and a banner that read "undefined sent/failed"). **Auto-driving pipeline:** new `components/pipeline-runner.tsx` is a CLIENT-SIDE driver — the operator hits **Run** once and it re-invokes the EXISTING batch endpoints (`/api/skiptrace`, `/api/scrub`, `/api/campaign`) one small batch at a time, looping each stage until it drains, then advancing to the next (avoids the 300s function timeout AND the manual re-clicking). Resumable: all state is in the DB, so closing the tab + clicking Run again picks up from DB state (and adopts an already-open run via `activeRunId`). **Compliance unchanged (load-bearing):** every send batch STILL goes through `getEligibleContacts` + the atomic `claimForSend` (with the V2 client-level opt_outs re-check), so suppression/eligibility hold on every batch — auto-resume never bypasses them, and a STOP landing mid-run excludes that phone from the rest of the run (the claim's opt_out NOT EXISTS fails). **Stall fix:** a "run" now spans the WHOLE driven send (not one HTTP batch). `POST /api/campaign` accepts a `runId`; the run is created on the first batch, heartbeated each batch (`touchCampaignRun` → new `last_batch_at` column), and closed (`finished_at`) ONLY on the batch that drains the eligible set (or when the window shuts mid-batch). The active-run guard (`getActiveCampaignRun`, staleness measured from `COALESCE(last_batch_at, started_at)`) lets the driver CONTINUE its OWN run (matched by id) while returning 409 + the active `runId` to a fresh caller — so a second concurrent operator is blocked but the driver never blocks itself. The response now carries a `done` flag so the driver knows when to stop. **Counter fix:** every send response returns real `sent`/`failed` (the driver accumulates `Number(data.sent ?? 0)`), so the banner can never show "undefined". **Live send-rate control:** new `PATCH /api/client` (`setClientSendRate`, clamped [1,1000]) persists `clients.send_rate_per_hour`; the send loop reads `client.send_rate_per_hour` fresh on every batch, so a change takes effect on the NEXT batch with **no redeploy** (proven live: PATCH 90 → the immediately-following dry-run reported `ratePerHour:90`). The driver paces the gap BETWEEN batches (`clientPacingDelayMs`) and sizes each batch from the live rate (`sendBatchSize` ≈ rate/20, clamped [1,50]) so a batch's server time stays well under 300s at any rate. **Structure:** run-lifecycle helpers extracted from `lib/db.ts` (now 430) into `lib/campaign-runs.ts`; pure client-safe math in `lib/pipeline.ts` (unit-tested); `campaign-controls.tsx` slimmed to manual-stage overrides (trace/scrub/dry-run, NO SMS) since the real send now lives in the driver. **Verified:** `tsc` + `npm run build` (registers `/api/client`) + **`npm test` = 173** (+7 pipeline math) + **`npm run test:isolation` = 28/28** all green; schema applied live (`last_batch_at`); HTTP smoke (auth gates, live rate, real-zero counters); and an end-to-end driven multi-batch on a throwaway campaign with Twilio MAGIC test numbers proved batch1 leaves the run OPEN (done:false), a second caller with no runId is **409-blocked**, batch2 drains + **CLOSES** the run, both contacts sent exactly once (**no double-send**, 2 outbound messages, run sent_count=2), then cleaned up → live DB back to the pristine pilot (campaign 1, 500 contacts, 91 sent, rate 60). **Limitation logged (shared-admin, V5 territory):** because the snapshot exposes `activeRunId` for resume and there's one shared admin password, two tabs/operators could both adopt the same run and co-drive (pacing ~2×) — but the atomic per-contact claim still guarantees **no double-text**; per-session run ownership is module V5 (client logins). **Out of V3 scope (untouched):** operator cockpit (V4), client logins (V5), billing (V6), visual redesign (V7). **Next: the focused send-driver review.**
- 2026-06-24 — **v2 Module V2 focused review pass (agent team) — campaign scoping + client-level suppression HOLD; no Critical/High; applied 6 cheap Medium/Low, logged the rest.** A 3-reviewer read-only team (compliance / correctness / security, Sonnet) challenged each other on "an opted-out phone can never be texted by any campaign" + "no upload or query crosses a client boundary." **Both HOLD, confirmed independently:** the `NOT EXISTS (opt_outs)` exclusion is in BOTH `getEligibleContacts` and `getSendProgress`, client-scoped, independent of the row's `suppressed` flag, last-10-normalized both sides, across all campaigns; no send path (campaign loop / reply / STOP confirmation / lead forward) can reach an opted-out homeowner; migration correct + idempotent; `resolveCampaignForClient` rejects another client's campaign (falls back to the requester's default); the upload is admin-gated, parameterized (no SQLi), and binds client_id/campaign_id server-side (CSV can't influence them). **Applied (Medium/Low):** (1) `claimForSend` re-checks opt_outs in the atomic UPDATE — closes the TOCTOU race where a STOP lands between eligibility and the claim mid-run; (2) `recordOptOut` normalizes the phone to last-10 before insert (canonical suppression key); (3) added a `clients` sequence `setval` (mirror of campaigns) — kills a latent PK-collision foot-gun when client creation is wired; (4) `getRecentInbound` back to LEFT JOIN + `OR contact_id IS NULL` so orphan inbound STOPs aren't dropped from the dashboard feed; (5) upload size cap (10 MB) + row cap (50k) so one oversized CSV can't blow the 300s ceiling + strand a half-import; (6) +2 isolation assertions proving `resolveCampaignForClient` won't cross a client boundary. **Logged, NOT actioned:** messaging-service-only inbound-STOP resolution (onboarding/V-later, documented), `opted_out` row-vs-distinct count (unique index protects it), `pending`==`eligible` cosmetic duplicate (pre-existing Session 3), csv-parse error detail echoed to the admin (matches existing pattern), CSV formula-injection on a hypothetical future export, and the carried **v1 `?clientId=` access-control gate** (module V5, before client #2). **Re-verified:** schema (56 stmts) + tsc + build + 166 tests + **28/28 isolation** all green; live DB pristine (1 client, campaign 1, 500 contacts). **Module V2 is review-clean.**
- 2026-06-24 — **v2 Module V2: campaigns + CSV list uploader BUILT — a client runs many campaigns; suppression stays CLIENT-level by phone.** Why: clients run repeated lists over time and the operator needs to drop in a CSV and go (no more `scripts/import-csv.ts`), without ever re-texting someone who opted out under a prior campaign. **Schema:** new `campaigns` table (`client_id` FK, `name`, `status` draft|ready|tracing|scrubbing|sending|done|paused, nullable `message_template` that inherits the client's when null); `campaign_id int NOT NULL REFERENCES campaigns(id)` added to `contacts` + `campaign_runs` (DEFAULT 1 backfill → drop default → indexed, same loud-fail pattern as `client_id`). The pilot migrated in as **campaign 1 ("Tallahassee pilot") under client 1**; all 500 contacts + both campaign_runs backfilled to campaign 1 (verified live, 0 nulls). A `setval` on the campaigns sequence advances it past the manually-seeded id=1 so the uploader's auto-INSERT can't collide on 1. **trace_jobs / scrub_jobs are deliberately NOT campaign-scoped** — they're pinned by the exact `contact_ids` they submitted, so crash-recovery ingest must (and does) run client-wide regardless of which campaign is active. **LOAD-BEARING — suppression is CLIENT-level by phone, NOT campaign-level:** `getEligibleContacts` (and the matching counts in `getSendProgress`) now also exclude any phone in that client's `opt_outs` (compared on last-10 digits, same normalization as `findContactByPhone`), so a person who opted out under ANY campaign — even a brand-new contact row in a new campaign with `suppressed=false` — is excluded from EVERY campaign, current and future. The isolation fixture asserts exactly this (a fresh clean contact in a new campaign with an opted-out phone is excluded; a control with a never-opted-out phone is eligible). **Pipeline scoped by campaign:** trace/scrub/send/eligibility/dashboard take a `campaignId` (`ContactScope`); routes resolve it via `resolveCampaignForClient(clientId, ?campaignId=)` which **validates the campaign belongs to the resolved client** (so `?clientId=1&campaignId=<other client's>` can't point the pipeline at another tenant) and defaults to the client's lowest-id campaign — for client 1 that's the pilot, so a request with no campaignId behaves exactly as before V2. **CSV uploader:** pure `lib/csv-import.ts` (case-insensitive headers, Address required, dedupe within upload by address+zip) + `POST /api/campaigns` (multipart, admin-gated → creates a campaign + imports its contacts, sets status 'ready') + `GET` (list for the selector). **Operator UI:** `components/campaign-bar.tsx` (campaign `<select>` + upload form) on the dashboard; every dashboard read/action carries `clientId`+`campaignId`; counts + leads/reply feeds are per-campaign, the opt-out list stays client-level (matches the suppression semantics). **Verified:** `npm run schema` (55 stmts, migration confirmed live), `tsc` clean, `npm run build` green (`/api/campaigns` registered), **`npm test` = 166** (+6 csv-import parser tests), **`npm run test:isolation` = 26/26** (+4 client-level suppression), and a live end-to-end upload (created campaign, imported 2/4 with dup+no-address skipped, per-campaign dashboard counts correct, unauth → 401) — test campaign then deleted so the live DB is back to the pristine pilot (campaign 1, 500 contacts). **Deviations/sub-decisions:** (1) contact-selection helpers (`getEligibleContacts`/`getContactsForSkiptrace`/`getContactsForScrub`) moved from a positional `limit?` to a `ContactScope {campaignId?, limit?}` object (and use `LIMIT $1`-with-null + `($1::int IS NULL OR campaign_id=$1)` to stay single-query); `insertContact`/`createCampaignRun` now REQUIRE a campaignId (the column has no default — a forgotten one fails loudly). (2) orphan-job recovery in trace/scrub stays client-wide by design (see above). (3) CLI `npm run trace/scrub` gained an optional `--campaign=N` (omit = all the client's pending across campaigns); `import-csv` writes campaign 1. **Awaiting the focused client-level-suppression + campaign-scoping review.**
- 2026-06-24 — **v2 Module V1 client-isolation review pass — multi-tenant isolation HOLDS; no Critical/High; applied 5 Medium/Low, logged the rest.** A 3-reviewer read-only agent team (isolation / compliance / correctness, Sonnet) audited the refactor and challenged each other on "no data or suppression ever crosses a client boundary." **All three independently confirmed it HOLDS** — every lib query scopes by `client_id` in the WHERE + on every INSERT (incl. dashboard-db JOINs scoping the joined table); the webhook validates the Twilio signature BEFORE resolving the client, then routes strictly by `To`→owning client and acks-noop on an unknown number (dropped, never misrouted); `buildDeps(client)` binds every inbound effect to the owning client; eligibility is a non-wideable 5-condition AND; scrub/trace fail-closed + the reply suppression gate hit the right client's opt-outs; migration correct + Talan byte-unchanged + config fully env→record. **Applied (Medium/Low):** (1) schema CREATE-then-DROP index order (no zero-uniqueness window if apply-schema is interrupted mid-migration); (2) `import-csv --fresh` now client-scoped FK-order deletes instead of a global TRUNCATE (multi-tenant data-loss hazard) + client-scoped final count; (3) `.env.example` timezone fix (Chicago→New_York) + per-client-config note (env = fallback only); (4) documented `processInbound`'s per-request client-binding invariant; (5) documented the `getClientByInboundNumber` from_number-only limitation + the smoke-twilio env-sender caveat. **Logged, NOT actioned (out of V1 scope):** ⚠️ the biggest one — `?clientId=` accepts any authenticated operator's chosen client with **no per-client access control**, so once a 2nd client exists an operator could reach another client's data/sender. Dormant today (single shared admin password, only client 1 → `?clientId=2` 404s) and fixing it IS module V5 (client logins / scoped access), so it would violate V1 scope to build now — but it is a hard gate before onboarding client #2. Also logged: `app/page.tsx` client-1 landing default, the `scrubBatch traceQueueId` external-data mismatch (opt-in power param), `forward_phone` NULL-by-design. **Re-verified:** schema (idempotent) + tsc + build + 160 tests + 22/22 isolation fixture all green; live DB shows only the per-client unique indexes (legacy dropped), 1 client, 0 stray rows. **Module V1 is review-clean.**
- 2026-06-24 — **v2 Module V1: multi-tenant foundation BUILT — `clients` table + `client_id` on every table + every query scoped by client; Talan migrated in as client 1 with ZERO behavior change.** Why: V1 is the data-model refactor that touches every table/query, so doing it first makes every later v2 feature multi-tenant-native (build it after and you build it twice). **Schema:** new `clients` table holds per-client config (status, plan $, lead_guarantee, billing_day, `from_number`/`messaging_service_sid`, `biz_name`, `message_template`, `forward_phone`, send window/timezone/rate, `optout_confirmation`, `branding` jsonb). Every data table (`contacts`, `messages`, `opt_outs`, `leads`, `campaign_runs`, `trace_jobs`, `scrub_jobs`) got `client_id int NOT NULL REFERENCES clients(id)` + an index; the Session-4 idempotency unique indexes became **per-client** (`opt_outs(client_id, phone)`, `messages(client_id, twilio_sid)`) so the same phone/SID can exist independently under two clients. **Migration is idempotent + apply-schema-safe:** each `client_id` is added `DEFAULT 1` (backfills existing rows + validates the FK to the Talan row inserted first) then the default is **dropped** so a forgotten `client_id` on a future INSERT fails loudly instead of silently landing in client 1 (a tenant-isolation foot-gun). **Scoping:** every helper in `lib/db.ts`/`lib/inbox-db.ts`/`lib/dashboard-db.ts`/`lib/trace-jobs.ts`/`lib/scrub-jobs.ts` and the batch libs (`lib/skiptrace`, `lib/scrub`) takes a `clientId` and filters by it in the WHERE + sets it on every INSERT — no query reads or writes across clients. **Current-client resolution:** operator routes/pages default to client 1 with a `?clientId=` override (`lib/request-client.ts`); the **inbound webhook resolves the owning client by the `To` number** (`getClientByInboundNumber`) and refuses (acks + no-op) if no client owns it, so a message to an unknown number never touches anyone's data. **Config moved env→client record:** `renderMessage`, `sendOne`'s sender, the send window/rate, the lead-forward number, and the opt-out confirmation all come from the client row (account-level secrets — Twilio SID/token, Tracerfy key, DATABASE_URL, ADMIN_PASSWORD — stay in env). **Verified:** `npm run schema` applied (Talan = client 1, all existing rows backfilled, per-client indexes in place), `npm run build` + 160 unit tests green, and a 22-assertion **isolation fixture** (`npm run test:isolation`) proves eligibility/inbox/suppression/contact-lookup never cross clients (both directions), the webhook routes strictly by To→client, and changing client 1's `message_template` changes its render ONLY — with client 1 proven byte-unchanged + all fixtures cleaned up. **Awaiting the client-isolation agent-team review** (`sessions/v2-session-1-prompt.md` bottom) before V2.
  - **Deviations / sub-decisions:** (1) `renderMessage` changed from variant-based (A/B/C) to **template-driven** — it now consumes `client.message_template`; the unused B/C creative variants were dropped (the pilot always ran `AB_VARIANTS=A`). A `{...}` optional-clause convention reproduces Talan's single-segment address fallback generically (drop the clause when keeping it overflows one GSM-7 segment or a placeholder inside resolves empty); the `variant` column + round-robin assignment is KEPT in the campaign route for provenance but no longer drives copy (one template per client). The 500-row real-data single-segment + verbatim-opt-out tests now run against the Talan template, proving client 1's copy is byte-identical. (2) `lib/db.ts` dashboard-read helpers split into **`lib/dashboard-db.ts`** (client-scoped) to keep `db.ts` ≤500 lines (now 419) — same split pattern as `inbox-db`. (3) `lib/twilio` window/rate/sender + `lib/forward` accept per-client config with **env fallback** so the smoke scripts still work; `forwardLead` falls back to the legacy `TALAN_FORWARD_PHONE` env **only for client 1** (the real number is a secret and can't live in committed SQL — Talan's seeded `forward_phone` is NULL), scoped so it can never leak across clients. (4) **apply-schema gotcha (hit + fixed):** the naive `;`-splitter treats a semicolon anywhere — including inside a `--` comment — as a statement separator, which truncated the `clients` DDL; comments must contain no `;`. (5) **Pre-existing, flagged not fixed:** `lib/tracerfy.ts` is 536 lines (>500) — untouched this session, not introduced by V1; queue it for a later split.
- 2026-06-23 — **Scrub SELECTS pending-only + a credit pre-flight + scrub_jobs durability — credit-safety hotfix after the scrub re-billed already-clean contacts.** **Root cause:** `getContactsForScrub` filtered `suppressed=false` but NOT `scrub_status`, so a contact that scrubbed **clean** (suppressed stays false) was re-selected and **re-billed at Tracerfy every chunk**; the live `npm run scrub` burned the balance to 1 credit re-scrubbing the growing clean pile and ran out before ~175 high-id contacts were ever scrubbed. **Load-bearing fix (one line, both queries):** `AND scrub_status='pending'` — already-scrubbed (clean OR flagged) rows are never re-selected, so a re-run bills only genuinely-unscrubbed numbers (proven by a live fixture: a `scrub_status='clean'` row is excluded, a `'pending'` row included). **Credit safety:** `scrubBatch` (and the CLI runner up front, and the route) now read `getCredits()` + the pending-with-phone count and **refuse cleanly with `InsufficientCreditsError` BEFORE submitting** if the balance can't cover the batch (route → 402; CLI → exit 2 with "need N, have M") — no more dying mid-run on a 402 or silently re-billing to empty. Cost model: **1 credit per phone** (`SCRUB_CREDITS_PER_PHONE`), matching the live accounting. **Durability (mirrors trace_jobs):** new `scrub_jobs` table + `lib/scrub-jobs.ts` persist each scrub queue id + its `contact_ids` the instant after `submitScrub`, **before** the poll; `scrubBatch` is RESUMABLE — it re-ingests any orphaned `submitted` scrub job first (free re-read, scoped to that job's contacts), then scrubs only still-`pending` contacts. Per-contact verdicts are written with `markSuppressed` first (a crash leaves the contact suppressed=safe + already excluded from re-billing), and the orphaned-job re-ingest re-applies any half-written verdict for free, so a crash can never leave a billed contact unmarked-and-re-billable. **scrub-from-queue is opt-in only:** it scrubs the ENTIRE trace queue (not scoped to pending) so it would re-bill already-scrubbed phones — the default phone-list path bills exactly the pending contacts, so the resumable path never auto-uses scrub-from-queue. Pure logic (verdict `classify`, the pre-flight threshold, the cost constant, the error) lives in `lib/scrub-logic.ts` so it's unit-testable without the Neon driver (`lib/scrub` re-exports it; `lib/db.ts` kept ≤500 lines). **KEPT IN SCOPE — send path, inbound webhook, trace logic, and the fail-closed clean/flagged verdict all UNCHANGED** (only the SELECTION re-billed). **Verified, no credits spent:** 167 tests green (+14 scrub: pre-flight threshold + classify contract), build green, schema applied (`scrub_jobs`); live — fixture exclusion proof + `scrubBatch()` refusing with credits 1→1 (delta 0). **Cost to finish:** 175 pending-with-phone × 1 = **175 credits**; balance is 1, so top up to ≥175 (≈174 more, ~$3.50) then `npm run scrub` finishes the 175 cleanly (no re-billing) → final eligible ≈ 91 + clean-of-175.
- 2026-06-23 — **Persist the Tracerfy trace job id + make tracing RESUMABLE — durability hotfix after a paid batch was orphaned.** The live dashboard trace of the 475 pending contacts ran on Tracerfy (queue `103802`, paid) but a browser reload killed the serverless function before results were written back, and the queue id lived only in memory — the paid results were stranded and the contacts stayed `pending`. **Root cause:** the queue id was never persisted, so the multi-minute result-fetch was completely unprotected against a crash. **Fix:** a new `trace_jobs` table (`queue_id`, `status` submitted|ingested, `contact_ids` jsonb = the batch's ingest scope, tallies) persisted in `lib/trace-jobs.ts` (its own module so `lib/db.ts` stays ≤500 lines). `traceBatch` now (1) FIRST re-ingests any orphaned `submitted` job before considering a new trace, and (2) persists the queue id + contact ids the instant after `submitTrace` returns, **before** the poll — shrinking the unprotected window from the entire fetch to the millisecond between submit and that write. Re-ingest is free (re-reading a completed queue never re-charges) and routes through the SAME fail-closed `applyTraceRows`/`pickMobile`/`matchKey` path as a live trace, so a recovered run is byte-for-byte what the interrupted one would have produced. `POST /api/skiptrace` inherits resumability through `traceBatch` (no route change). **Recovery of 103802 (no new trace):** ran `scripts/ingest-trace-queue.ts` (`npm run ingest -- 103802`) — credits **501 → 501 (delta 0, proven)**; DB went `pending 475→0`, with the queue's 446 returned rows applying as **417 matched** (usable mobile) + **29 landline-only → fail-closed no_match**, plus the 29 inputs Tracerfy returned nothing for → no_match. Final: matched 441, no_match 59, with_phone 441, pending 0 (all 500 traced). The hotfix's "~470 with_phone" estimate over-counted — 29 of the 446 matches are landline-only and correctly are not textable. **Residual window (documented):** if `submitTrace` charges but its response is lost before the persist write, the queue id is unrecoverable programmatically (Tracerfy's queue list is the manual backstop) — unavoidable without a queue-list endpoint, and far smaller than the original bug. Also fixed a latent load-order bug in the trace/scrub CLI runners (`config()` then a static `@/lib/db` import is hoisted above the env load under tsx/ESM → "DATABASE_URL is not set") by dynamic-importing the lib after dotenv runs; bootstrap-only, no trace/scrub logic changed.
- 2026-06-23 — **Session 7 review pass (agent team) — both adversarial claims HOLD; applied 2 High + logged decisions.** A 3-reviewer read-only team (security/compliance/correctness, Sonnet) audited the Module 7 reply/inbox path. **No Critical.** Verdicts cross-validated: **"the reply endpoint can't text an arbitrary number" HOLDS** (the destination is only ever the DB row's `phone`, loaded by a validated integer `contactId`; no request field reaches `sendOne`), and **"a reply can never reach an opted-out person" HOLDS** (the gate is `await`ed before `sendOne`, and `replyRefusalReason` checks `contacts.suppressed` AND the `opt_outs` row **independently**, so either partial-failure crash direction, a phone-normalization mismatch, a null/missing contact, or `contactId` type coercion is still caught — compliance walked 7 explicit sequences). **Applied (High):** in `getInboxThreads` — (H1) order by `COALESCE(lm.created_at, l.created_at)` so a future lead-only/no-messages contact sorts by recency instead of sinking to the bottom; (H3) `needs_reply = lm.direction='inbound' AND NOT c.suppressed` so the raw flag never claims a suppressed (opted-out) contact needs a reply (the UI already masked it, but the data contract was wrong). Re-verified with a DB fixture (suppressed-STOP → `needs_reply=false`, clean inbound → `true`) + 153 tests + build. **Decision — H2 (no server-side reply idempotency) logged, NOT actioned:** the client `disabled`-while-sending guard is the only double-send protection, so a client-timeout-retry or two tabs could duplicate a reply — accepted for the single-operator MVP because it can only ever duplicate a message to a **non-suppressed** person (the gate re-runs on every call), never a TCPA break or a send to an arbitrary/opted-out number, and a naive server dedupe would wrongly drop a legitimately repeated reply (same logic that declined the Session 4 leads-uniqueness index). Also logged (not actioned): a sub-second **TOCTOU** STOP/reply race (inherent to non-transactional serverless; suppression is enforced on the next attempt), notes `""`-vs-`NULL` hygiene, failed-reply bubble visual prominence, the unchecked boolean cast, `getThread` round-trip parallelization, and the inbox-page raw-error echo (admin-only).
- 2026-06-23 — **Module 7 (inbox + reply + lead tracking) BUILT; the reply path enforces suppression in two places.** `POST /api/reply` is admin-gated and **never accepts a destination phone from the request** — it loads the contact by id and sends only to `contact.phone`, so the tool cannot be turned into an arbitrary-number texter. The suppression gate is a pure, unit-tested `lib/reply.ts` `replyRefusalReason(contact, optedOut)` that **fails closed**: a missing contact, no phone, `suppressed=true`, or an `opt_outs` row all return `recipient_suppressed` → the route 422s and `sendOne` is never called. The UI mirrors it (the reply box is disabled for suppressed contacts) but the server refusal is the real guarantee. Proven without texting anyone: 7 unit tests + a live-HTTP suppressed fixture → 422. **Decisions baked in:** (a) replies are NOT blocked by the campaign send window (1:1 conversational) — only a soft "off hours" UI hint, computed server-side; (b) manual replies may be multi-segment (a human is typing) — the campaign's single-segment rule does not apply, segment count is shown but never blocks; (c) every outbound reply (success or fail) is logged to `messages`. **Structure:** Module-7 DB helpers live in `lib/inbox-db.ts` (split so `lib/db.ts` stays ≤500 lines); `LEAD_STATUSES` lives in pure `lib/lead-status.ts` so client components import the funnel values without bundling the Neon driver into the browser. Agent-team review (`sessions/session-7-review.md`) still owed before the live send.
- 2026-06-22 — **Add an in-app inbox + reply + lead tracking (Module 7) BEFORE launch** — review of the build surfaced that there was no way to reply to a homeowner from the tool (only a ping to Talan) and no lead status tracking. Decided to build a dashboard inbox with conversation threads, a reply box that texts back **from the campaign number** (suppression-checked, stored-phone-only, logged), and `leads.status`/`notes`. This adds a new outbound path, so it gets an agent-team review; the live pilot (Session 6 Phase B) waits until it's in, so replies/tracking ship with launch. Replies are gated by suppression (never text an opt-out) but NOT by the campaign send window (a 1:1 reply is conversational/time-sensitive).
- 2026-06-22 — **Pilot runs ONE message — Jordan's exact wording, verbatim, with only `Reply STOP to opt out` appended.** Final: "Hey [NAME] busy season is here, we are working close by if you were interested in window cleaning services at [ADDRESS]. Reply STOP to opt out". Do NOT add a business name or alter the wording. Earlier "Type 2 to opt out" was rejected (the system only suppresses on STOP-family keywords, so a "2" reply would never opt anyone out — a TCPA break) and Jordan chose to append the standard STOP line (carriers filter no-opt-out 10DLC traffic). `[ADDRESS]` = situs street, **title-cased** (not caps). Overflow fallback drops only the "at [address]" clause. Single variant. Twilio confirmed off-trial + 10DLC approved. Still owed: Talan's blessing on the wording; address-in-message raises complaint risk (watch first batch).
- 2026-06-22 — **Session 4 review pass (agent team) — applied fixes + decisions.** A 3-reviewer read-only team (security/compliance/correctness, Sonnet) audited the webhook + forward path. Both adversarial claims were stress-tested: **"a forged request can do nothing" holds** (signature gate precedes every side effect); **"STOP is always honored"** held in standard cases but correctness found the real break. **CRITICAL fix — STOP-suppression retry recovery:** because the dedupe log commits before suppression and neon autocommits each statement separately, a crash between them could let a deduped Twilio retry strand a STOP un-suppressed. Fixed by re-applying the opt-out idempotently on the duplicate branch in `lib/inbound.ts` (re-suppress on retry; never re-send the confirmation or create a lead → zero-or-one confirmations, never two). **HIGH fix — `opt_outs` uniqueness:** added a unique index on `opt_outs(phone)` + `recordOptOut` is now `ON CONFLICT (phone) DO NOTHING`, making the recovery idempotent (no duplicate opt-out rows). **Cheap fixes:** `findContactByPhone` regex `'\D'`→`'[^0-9]'` (a JS template literal cooks `'\D'` to `'D'` and neon sends cooked text, so `'\D'` stripped literal `D`s — masked today by pre-normalized phones, but a latent STOP-match-miss risk); a warning when `TWILIO_WEBHOOK_URL` is unset (x-forwarded reconstruction can't be used to forge — that needs the auth token — but a host mismatch behind a non-Vercel proxy would reject legit Twilio requests). **Decision — declined a `leads` uniqueness index:** a `(contact_id, reply_text)` unique would drop a legitimately repeated lead (the same homeowner texting "yes" on two different days = two real pings Talan should get); `messages(twilio_sid)` is the correct idempotency boundary, and the lead crash-window loses a lead (not double-pings) while the inbound is still logged. **Refuted (false positive):** a reviewer's claim that "stop texting me" escapes `isOptOut` — empirically `isOptOut` matches "stop" as a bounded token anywhere and runs before classification (existing test confirms). Re-verified: 138 unit tests, build, `npm run smoke:webhook` (5/5), schema apply, plus crash-recovery + exactly-one-confirmation integration checks all green.
- 2026-06-22 — **Webhook auth = Twilio signature validation, gating everything (Session 4).** `/api/webhook/twilio` is public (Twilio calls it) so it has no admin cookie; its only auth is the `X-Twilio-Signature` header, validated with `twilio.validateRequest(authToken, signature, url, params)` over the exact public URL Twilio POSTed to. Invalid/missing/tampered signature → **403 before any DB write or forward** (proven by `npm run smoke:webhook`: valid accepted; forged, tampered-body, wrong-URL, missing all rejected). The public URL is taken from `TWILIO_WEBHOOK_URL` if set, else reconstructed from `x-forwarded-proto`/`x-forwarded-host` (Vercel's proxy rewrites `req.url`). A missing `TWILIO_AUTH_TOKEN` fails closed (500, never processes). Without this, anyone could forge an "interested" reply (spam Talan) or a fake STOP.
- 2026-06-22 — **Inbound idempotency = the inbound log is the dedupe gate (Session 4).** Twilio retries webhooks, so the same `MessageSid` must be processed once. `logInboundOnce` does `INSERT … ON CONFLICT DO NOTHING` against a new **partial UNIQUE index `messages(twilio_sid) WHERE twilio_sid IS NOT NULL`** (partial because failed outbound sends log a null sid); a conflict (null return) means "already seen" → stop before any opt-out/lead/forward. Chosen over a check-then-insert because it's race-safe against concurrent retries. Trade-off noted: it's also the gate, so a crash between the log insert and the side effect could strand processing on retry — acceptable because the writes are adjacent fast DB calls and `markSuppressed` is idempotent; the stated concern (Twilio re-delivery of an already-handled message) is fully covered.
- 2026-06-22 — **STOP precedence is absolute + sent via TwiML `<Message>` (Session 4).** In `lib/inbound.ts`, `isOptOut(Body)` is checked BEFORE `classifyInterest` and wins unconditionally — a reply that says "STOP, but how much?" is an opt-out, never a lead, never forwarded. On a matched contact: `recordOptOut` + `markSuppressed(id,'opt_out')` (so the send path's `suppressed=false` eligibility permanently excludes them) + **one** CTIA confirmation. The confirmation is returned as a TwiML `<Message>` (one send, no extra API call, can't double-fire). New env `TWILIO_ADVANCED_OPT_OUT=true` suppresses our confirmation if a Twilio Messaging Service with Advanced Opt-Out is ever enabled (Twilio would send it itself → avoid double-confirm). An orphan STOP (no contact) still records the opt-out by phone; no suppress (no row to flag).
- 2026-06-22 — **Forward channel = SMS ping only, and a failed ping never loses a lead (Session 4 — reaffirms the delivery decision below).** `lib/forward.ts` creates nothing itself; the `leads` row is written first (the dashboard's durable source of truth), then `forwardLead` sends one terse `sendOne(TALAN_FORWARD_PHONE, …)` ping (name + address + reply) and marks `forwarded=true`/`forwarded_at` on success. On any failure it logs and returns false — the lead stays on the dashboard with `forwarded=false`, surfacing exactly which pings didn't go through. No email, no Resend. `forwardLead` never throws, so a ping failure never affects the webhook's TwiML response.
- 2026-06-22 — **Lead delivery = dashboard + instant SMS ping to Talan; dropped Resend/email entirely** — Talan watches the dashboard (Module 5, the durable record), and an interested reply also fires a one-line text so hot leads don't wait for him to log in (speed-to-lead). Email added no value for a window-cleaning operator and was one more account to maintain. `lib/forward.ts` is SMS-only; because the lead row is created regardless, a failed ping never loses a lead. Resend removed from stack + env across CLAUDE.md/overview.md.
- 2026-06-22 — **Dashboard (Session 5, Module 5) — read-only UI over the hardened endpoints; no new backend logic.** `/dashboard` is a server component behind the same Session 1 admin gate (unauthed → 307 to `/`; Talan uses the shared admin password for the pilot — per-user login deferred). All reads go through one aggregator (`lib/dashboard.ts` → `getDashboardData`), shared by the initial server render AND a new **read-only** `GET /api/dashboard` (admin-gated) that the client polls (7s while a run is active, else 20s). The control buttons call ONLY the existing endpoints (`/api/skiptrace`, `/api/scrub`, `/api/campaign`) — no send/scrub/trace logic was added. **Start-send is double-gated:** the campaign endpoint still enforces `{confirm:true}` + send-window + active-run, AND the UI requires a typed `CONFIRM` in a modal that states "This will text N people" before it ever posts `{confirm:true}`; the send button is disabled outside the window / while a run is active. The endpoint's `confirmation_required`/`outside_send_window`/`campaign_already_running` responses are surfaced verbatim-ish. Inbound reply **disposition** tags (opt-out / interested / not_interested / neutral) are derived on the fly from the pure `lib/classify.ts` (no stored column, no side effect). **Send model = "run batch / continue"** — there's no pause endpoint; the send is resumable, so the operator re-runs to continue remaining eligible. Verified with dry-run only (no real list batch fired).
- 2026-06-22 — **RESOLVED + APPLIED: send-window timezone = `America/New_York` (Eastern)** (supersedes the CT-vs-ET open item below) — Tallahassee is Eastern; the `sms-copy.md` "CT" note was wrong. The `lib/twilio.ts` `DEFAULT_TIMEZONE` default is now `America/New_York` (changed in Session 5, Task 0 — was `America/Chicago`; still env-overridable via `SEND_TIMEZONE`). Keeps recipients in their true 10am–7pm local window and inside TCPA's 8am–9pm rule.
- 2026-06-22 — **Session 3 review pass (agent team) — applied fixes + decisions.** A 3-reviewer read-only team (security/compliance/correctness) audited the send path. Verdicts: compliance + correctness independently confirmed **no path texts an ineligible contact** and **no crash+retry double-texts** (the atomic per-contact claim holds even under concurrency). Security found the real Critical: the API routes were **unauthenticated**. Fixes applied: (1) **auth gate** (`isAuthed` admin cookie) on `POST/GET /api/campaign`, `POST /api/scrub`, and `POST /api/skiptrace` — verified end-to-end (401 unauthed, 200 authed); (2) routes now return a **generic error label**, never raw `err.message` (could leak Twilio/DB internals); (3) **concurrent-run guard** via a new `campaign_runs.finished_at` + `hasActiveCampaignRun()` (6-min staleness so a timed-out run never blocks forever) so two callers can't both blast the list and defeat pacing; (4) **send-window re-checked every loop iteration** (stops mid-run at the window close, not just at start); (5) **A/B variant assigned by attempt-index** (advances only on actually-sent contacts) so the realized split stays balanced when rows are skipped; (6) **overflow contacts drained** (claim→`failed`) so they leave the eligible pool instead of re-rendering forever; (7) `getSendProgress` gained an **`in_flight`** bucket so stuck `'sending'` rows are visible. **Decision — do NOT auto-reset stuck `'sending'` rows:** a row is `'sending'` exactly when a send may already have gone out, so resetting it to `'not_sent'` would convert the safe silent-skip into a **double-text**. Recovery stays a manual, Twilio-log-checked operation. Left as logged recommendations (Medium/Low): the CT-vs-ET send-window question (below), lazy DB-client init, `pending` vs `eligible` naming.
- 2026-06-22 — **OPEN for operator decision — send-window timezone (CT vs ET).** Default is `America/Chicago` per how `sms-copy.md` worded the window ("10am–7pm CT"), but Tallahassee is **Eastern**, so 10:00–19:00 CT = 11:00–20:00 ET for recipients — an hour late both ends, and texts land as late as 8pm local. Still inside TCPA's 8am–9pm recipient-local rule (legal), but not the intended 7pm wall. **Recommendation: set `SEND_TIMEZONE=America/New_York`** (and confirm 10am–7pm ET with Talan) before the live send. Not changed unilaterally — it's a client-facing decision.
- 2026-06-22 — **Send pacing + window (Session 3):** pace from `SEND_RATE_PER_HOUR` (default **60/hr** = one send every 60s) and only send within a configurable local window, default **10:00–19:00 America/Chicago** (`SEND_WINDOW_START_HOUR`/`SEND_WINDOW_END_HOUR`/`SEND_TIMEZONE`). Window is `[start, end)`. Chosen per `sms-copy.md` ("10am–7pm CT, no early/late sends"); CT (not ET) matches how the copy/window were specced even though Tallahassee is Eastern — revisit if Talan wants true ET. A real (non-dry-run) campaign POST refuses outside the window; the smoke test ignores the window because it only texts Jordan.
- 2026-06-22 — **A/B variant tracking (Session 3):** added `contacts.variant` (A|B|C), assigned **deterministic round-robin by eligible-list position** at send time and recorded per contact so positive-reply rate can be compared per variant later. Active cells come from `AB_VARIANTS` (default `A,B,C`); set `AB_VARIANTS=A,B` for 2 balanced cells if the post-scrub eligible count is small (per the `sms-copy.md` A/B plan — 2 fat cells can beat 3 thin ones). Everything else (pacing, window) is identical across cells.
- 2026-06-22 — **No-double-text via atomic claim (Session 3):** the send loop flips a contact `not_sent → 'sending'` in a single conditional `UPDATE ... WHERE send_status='not_sent' RETURNING id` and only sends if that claim won the row. A crash mid-send leaves the row `'sending'` (never re-selected as eligible) — we fail toward NOT texting rather than risk a double-text. `'sending'` rows are a manual-inspection state, not auto-retried. `send_status` now: `not_sent | sending | sent | failed`.
- 2026-06-22 — **Hold the full 500 trace/scrub run; build the whole app first**, then do the live run as the pilot step (Session 6 order: import→trace→scrub→smoke send→full send). Module 2's gate passed live on one record, which is enough to trust the integration — spending on 500 real records waits until we're ready to actually send.
- 2026-06-22 — Add a **`scrub_status`** column as the send-eligibility guard (built in Session 3) — "never text an unscrubbed number" must be provable in the eligibility query, not assumed from run-order. Scrub writes `scrub_status` ('clean'/'flagged'); the send path requires `scrub_status='clean'`. No backfill needed since the full scrub hasn't run yet.
- 2026-06-22 — Tracerfy trace input is **address-based, not APN** (verified live, Session 2) — `POST /v1/api/trace/` takes `address_column`/`city_column`/`state_column`/`zip_column` (+ name + mail columns). Our `contacts` (name + situs address) map directly; the parcel/APN endpoint is separate and unused. Highest-risk unknown resolved — no replan.
- 2026-06-22 — Tracerfy `normal` trace **requires a mailing address** (`mail_address_column`); we set mail = situs address (Session 2) — the live API 400s without it. Our list is homestead-filtered owner-occupied, so mailing address equals the situs address; the mapping is correct for this list and would only diverge for absentee owners (already excluded).
- 2026-06-22 — Trace results map back to `contact_id` by normalized **address+city+state** (`matchKey`), not an echoed ref id (Session 2, verified live) — Tracerfy's queue rows carry no passthrough id; we key on `UPPER(address)|UPPER(city)|UPPER(state)`, unique within our 5 zips. (Earlier draft keyed on zip; switched because we match on the row's own address/city/state fields.)
- 2026-06-22 — **Trace results are inline JSON; scrub results are a CSV download** (Session 2, verified live). `GET /v1/api/queue/:id` (NO trailing slash) returns a bare JSON array of row objects (cols incl. `primary_phone`, `primary_phone_type`, `mobile_1..5`, `landline_1..3`, `email_1..5`) with no status field — completion is detected by row count reaching the uploaded count. `GET /v1/api/dnc/queue/:id` returns `{pending, download_url, clean_download_url, ...}`; the per-phone flags live in the downloaded CSV. So the client parses trace inline and downloads+parses the scrub CSV.
- 2026-06-22 — SMS phone pick = best **mobile** only (Session 2) — use `primary_phone` when `primary_phone_type` is `Mobile`, else first non-empty `mobile_N`; if only landlines, treat as no usable mobile → no-match → suppress. We text mobiles only.
- 2026-06-22 — Scrub flag encoding is `Y`/`N` (and `is_clean` `Yes`/`No`) in CSV columns `national_dnc,state_dnc,dma,litigator,is_clean` (Session 2, verified live) — any of the four `Y` → suppress (`litigator` flag → reason `litigator`, else `dnc`). Verified: smoke record was Federal+State DNC `Y` → `suppress_reason='dnc'`.
- 2026-06-22 — `/trace/` submit must be **multipart/form-data** with `json_data` as a stringified field; scrub / analytics / queue reads are JSON GETs (Session 2, verified live — JSON to `/trace/` returns 415).
- 2026-06-22 — Test runner = Node's built-in `node:test` run through `tsx` (`npm test` = `tsx --test lib/*.test.ts`) — zero extra dependencies (tsx is already in devDependencies), keeps the dep tree minimal per CLAUDE.md. Chosen over `vitest`/`jest` for Module P's pure-logic unit tests; reuse it for future lib tests.
- 2026-06-22 — Build sequentially (one coding-agent session per module), reserve a parallel agent-team for the security/compliance review pass on the sensitive modules (suppression, STOP, send path) — modules are a dependency chain that don't parallelize for building, but the suppression/STOP logic benefits from multiple independent reviewers.
- 2026-06-22 — DB = Neon Postgres via the Vercel Marketplace integration (driver: `@neondatabase/serverless`) — Vercel retired first-party "Vercel Postgres" and migrated those DBs to Neon in late 2024, so Neon is now the native Postgres-on-Vercel option (auto-provisions `DATABASE_URL`/`POSTGRES_*` into the project = deploy + DB in one place). The old `@vercel/postgres` driver is deprecated/unmaintained; new projects use `@neondatabase/serverless`.
- 2026-06-22 — Single aged Twilio 10DLC number for pilot, no rotation — keep MVP simple; rotation is a scale concern, not a pilot one.
- 2026-06-22 — Tracerfy for both skip trace AND DNC/litigator scrub — one API, scrub includes federal DNC + state DNC + DMA + TCPA-litigator flags in one pass.
- 2026-06-22 — Source list from Leon County certified tax roll (free), filtered to owner-occupied (homestead exemption) single-family in 5 target zips — free, clean owner-occupied signal vs. paying 10¢/record.
- 2026-06-22 — Test list = random 500 from 5 zips (32312, 32309, 32308, 32317, 32311), entity owners stripped — pilot to validate conversion before spending on a full pull.
- 2026-06-22 — MVP triage is keyword/classifier + human handoff, not full AI conversation — prove the funnel first; AI qualifier is a fast-follow module.

## Compliance note (read before sending)
This pilot sends cold SMS to homeowners who have not opted in. The Tracerfy scrub removes DNC-registered and known-litigator numbers, which reduces risk, but does not constitute consent. TCPA/Florida FTSA exposure is real and is being accepted knowingly for the pilot. Operating discipline that keeps risk lowest: (1) never send to a scrub-flagged number, (2) honor STOP instantly and permanently, (3) keep message content soft, identified, and single-segment with opt-out language, (4) pace sends. These are enforced in the build, not optional.
