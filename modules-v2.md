# LeadFlow v2 — Multi-tenant build plan

_Started 2026-06-24. v1 (the Talan pilot) is built + live. v2 turns it into the product._

## The model (drives every decision)
- **Agency / done-for-you.** YOU operate LeadFlow and run campaigns for clients. Clients don't run
  anything — they get a branded dashboard to watch leads come in.
- **Billing:** flat **$2,500/month** per client retainer with a **minimum 50 leads/month guarantee.**
- **Central metric:** *leads this month vs. the 50 guarantee*, per client. This is the heartbeat of the
  whole app — the operator cockpit and the client view both revolve around it.

## Build order (one module = one Claude Code session, generated one at a time)

| # | Module | Why / what |
|---|--------|-----------|
| V1 | **Multi-tenant foundation** ✅ **DONE (2026-06-24)** | `clients` table (name, status, plan $, lead_guarantee=50, billing_day, branding) + a `client_id` on every table (contacts, messages, leads, opt_outs, campaign_runs, trace_jobs, scrub_jobs) + scope EVERY query by client. Talan migrated in as client #1 (behavior identical). Per-client config (Twilio number, message copy, forward contact, send window/rate, opt-out copy) moved env→client record; webhook routes by To→client. Built + green (npm run schema/build/test, 160 unit tests, 22-assertion isolation fixture) AND **review-clean** — the 3-reviewer client-isolation pass found no Critical/High; 5 Medium/Low fixes applied. One logged gate: `?clientId=` needs per-client access control before a 2nd client (that's V5). **Ready for V2.** |
| V2 | **Campaigns + list uploader** ✅ **DONE (2026-06-24)** | `campaigns` table (per-client; status + nullable message_template) + `campaign_id NOT NULL` on `contacts`/`campaign_runs`; pilot migrated in as **campaign 1 under client 1** (500 contacts + 2 runs backfilled, behavior unchanged). **CSV uploader** (`lib/csv-import.ts` + `POST /api/campaigns` + `components/campaign-bar.tsx`) creates a new campaign and imports its list — replaces `scripts/import-csv.ts` as the product path. Trace/scrub/send/eligibility/dashboard scoped by campaign (`resolveCampaignForClient` validates the campaign belongs to the client). **Suppression stays CLIENT-level by phone** — `getEligibleContacts` excludes any phone in the client's `opt_outs`, so an opt-out under one campaign excludes that phone from ALL campaigns (asserted by the isolation fixture). Green: schema/build, **166 tests**, **28/28 isolation** (client-level-suppression + cross-client campaign-resolution), live upload verified + cleaned up. **Review-clean** — the 3-reviewer focused pass (compliance/correctness/security) found no Critical/High; both load-bearing claims HOLD; 6 cheap Medium/Low hardening fixes applied. **Ready for V3.** |
| V3 | **Guided pipeline + de-finicky** ✅ **DONE (2026-06-24)** | One-click **trace → scrub → send** to completion via a **client-side driver** (`components/pipeline-runner.tsx`) that re-invokes the existing batch endpoints one batch at a time and loops until each stage drains — kills the babysitting + avoids the 300s timeout; resumable from DB state. Each send batch STILL goes through `getEligibleContacts` + atomic `claimForSend` (V2 opt_out re-check) — suppression holds on every batch. **Send-batch stall fixed:** a run now spans the whole driven send (new `last_batch_at` heartbeat); it closes (`finished_at`) only on the batch that drains the eligible set, and the active-run guard (`getActiveCampaignRun`) lets the driver continue its OWN run by `runId` while 409-blocking a second operator — `done` flag tells the driver when to stop. **"undefined sent/failed" fixed** (responses carry real counters; driver coalesces to 0). **Live send-rate control:** `PATCH /api/client` persists `send_rate_per_hour`; the send loop reads it fresh each batch → effect on the next batch, no redeploy. Green: build, **173 tests** (+7 pipeline math), **28/28 isolation**, schema applied live, HTTP smoke + an end-to-end driven multi-batch (Twilio magic numbers) proving run-closes / no-double-send / second-operator-blocked, then cleaned up (DB pristine). Logged limitation: shared-admin lets two tabs co-drive one run (no double-text — the claim guarantees it; per-session ownership is V5). **Review owed (focused send-driver pass), then V4.** |
| V4 | **Lead-guarantee cockpit (operator)** ✅ **DONE (2026-06-24)** | The agency control room: `app/page.tsx` (admin-authed landing) → `components/cockpit-view.tsx` shows every client (sorted **behind-pace first**) with **leads-this-cycle / lead-guarantee** + a progress bar, a **behind / on-track / met ✓** pace flag, this cycle's sent count + reply rate + opt-out rate, and days-left; clicking a client opens their scoped `/dashboard?clientId=N` (back-link added). Cross-client aggregate = `lib/cockpit.ts getCockpitData()` — **operator-only by design** (summary COUNTS only, scoped by `client_id` AND the `[cycleStart,cycleEnd)` window; no contact-level data; no client-facing consumer). Pure cycle/pace math in `lib/billing-cycle.ts`: **cycle = billing_day→same-day-next-month in UTC, null billing_day = calendar month, day>month-length clamps to last day**; `paceFlag` = behind when leads < guarantee×(elapsed/length). Did NOT touch send/suppression/eligibility. Green: build, **183 tests** (+10 billing-cycle), **28/28 isolation**, new **`npm run test:cockpit`** (this-cycle-only lead count + pace), live DB pristine. **Light-review-clean** (single read-only reviewer): no Critical/High; aggregate operator-only + correctly scoped, drill-downs stay scoped, cycle/pace math holds. **Ready for V5.** |
| V5 | **Client dashboard + login** ✅ **DONE (2026-06-24)** — **closes the V1 access gate** | Real per-user login replaces the shared `ADMIN_PASSWORD`: `users` table (operator/client roles, scrypt-hashed passwords), an httpOnly+secure HMAC-signed session cookie, basic login throttling. **The V1 `?clientId=` gate is CLOSED** — every request resolves its client from the session via the one pure chokepoint `resolveClientIdForUser` (operator → any; **client user → their own client_id only, foreign id → 403**); operator routes/pages use `requireOperator`. Branded read-mostly **client dashboard** (`/client` + scoped `/api/portal`): the client's own leads feed + progress to their 50/mo guarantee. Send/suppression/eligibility untouched. Green: build, **199 tests** (+16 auth/access), **28/28 isolation**, **`npm run test:cockpit`**, new **`npm run test:access`** (proves a client-2 user can't reach client-1 by any vector) + a wired HTTP smoke. **Awaiting the agent-team SECURITY review (access-control / auth / correctness).** |
| V6 | **Lead-target auto-pause + track-only billing** ✅ **DONE (2026-06-25)** | Reframed from "billing" to **deliver-then-stop**. Per-client `lead_target` (NULLABLE → falls back to `lead_guarantee`) + `target_period` ('week'\|'month'). The send route (`app/api/campaign`) consults `getTargetStatus` (`lib/auto-pause.ts`, pure period math in `lib/lead-target.ts`) BEFORE any claim/send, read fresh each batch: once `leads-this-period >= target` it refuses (`paused`/`target_met`, done:true) and the pipeline shows "Target met (N/T this period) — paused until <date>"; resumes on target-raise or period rollover. **SERVER-enforced, layered ON TOP of suppression/eligibility — purely additive** (target-not-met never makes a suppressed/opted-out contact eligible; target-met never touches eligibility). Week = ISO Mon–Mon UTC; month = the V4 billing cycle. **Track-only billing (NO Stripe):** `client_invoices` (one row per client per cycle, idempotent on `(client_id, period_start)`, due→invoiced→paid) marked from the cockpit via `POST /api/billing`; collection is manual. Surfaced on the cockpit (auto-pause badge + leads/target + next-bill-date + invoice status/buttons), the operator dashboard (badge), and the client portal (target-met note). Green: build, **208 tests** (+9 lead-target), **28/28 isolation**, **28 access**, **cockpit**, new **`npm run test:auto-pause`** (stops EXACTLY at target with contacts remaining, off-by-one safe, raise/rollover resume, week+month, suppression untouched, billing materialize/mark/scope + cockpit surfacing). Talan unchanged (lead_target NULL → 50/month). **Awaiting the focused send-path review.** |
| S | **Litigator-only scrub (TCPA Litigator List)** — *spec: `sessions/scrub-litigator-swap.md`* | Replace the full Tracerfy DNC scrub with a **litigator-only** scrub via **TCPA Litigator List** (`type=tcpa`, ~$0.001/number, ~20× cheaper). Decouples the litigator filter (keep — removed 0 in the pilot) from the DNC-*registry* scrub (drop — it cost ~40–50% of the list). Built as a **pluggable scrub-provider seam** (`SCRUB_PROVIDER` env, default `tcpa-litigator`; Tracerfy scrub stays available but inactive). **Fail-closed contract preserved byte-for-byte** (clean ONLY on an explicit clean verdict; error/credit-exhaustion ⇒ stays pending, never clean); STOP/opt-out suppression + eligibility UNTOUCHED. Independent of V6/V7 — recommend building it **before V7** (higher value: cost + volume). Operator must set up the TCPA Litigator List account + API creds first. |
| N | **No-scrub send mode** ✅ **DONE (2026-06-25)** — *spec: `sessions/no-scrub-mode.md`* | Per-campaign `campaigns.scrub_mode` ('vendor' default \| 'none'). On a `'none'` campaign the scrub stage is a passthrough (`lib/scrub-passthrough.ts`) that marks the campaign's matched + with-phone + `pending` contacts `scrub_status='clean'` in ONE scoped UPDATE with NO vendor call/credits/scrub_jobs (mirrors `getContactsForScrub` minus the vendor call). `app/api/scrub` branches on `scrub_mode` ('none' → passthrough, else `scrubBatch` byte-unchanged; same response shape so `pipeline-runner.tsx` is untouched). Set via `POST /api/campaigns` (`scrubMode`, default 'vendor') or `PATCH /api/campaigns {campaignId, scrubMode}` (operator-only, client-scoped). **Eligibility + opt-out/STOP suppression UNCHANGED** — passthrough sets `scrub_status` only; the `NOT EXISTS opt_outs` check is independent, so an opted-out contact stays excluded even when marked clean. Default `'vendor'` keeps Talan's pilot byte-identical. Green: build, `npm test` = 208, `test:isolation` = 28/28, `test:access`, `test:cockpit`, `test:auto-pause`, new **`npm run test:passthrough` = 24/24**; schema applied live; DB pristine. **Owes a single-reviewer correctness pass.** Unblocks the 2,500 launch (remaining: deploy + seed users). |
| V7 | **UX redesign + LeadFlow branding** | Make the operator + client UIs look like the product they are. |
| Later | AI auto-responder/qualifier · analytics & ROI · multi-number per client · onboarding flow |

## Principle
Do the **data-model refactor (V1) first.** It touches every table and query, so every feature built
after it is multi-tenant-native. Build features before it and you build them twice.

## Notes carried from v1
- Compliance core (DNC/litigator scrub, instant STOP, suppression, signed webhook) is built + reviewed
  — v2 must preserve it per client (scoping must never let one client's suppression/opt-outs leak or
  be bypassed).
- Known v1 bugs folded into V3: send-batch run not closing, "undefined" counter, inbox orphan-reply gap.
