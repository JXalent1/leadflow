# Status

_Last updated: 2026-06-29_

## Current phase

**Conversational AI lead-qualifier (GHL-style) — BACKEND BUILT, review-gated, PR OPEN, NOT merged (2026-06-29, Claude Code).** A new autonomous OUTBOUND path that replicates Lance's GoHighLevel SMS AI: reads **intent** (not keywords), replies in a fully human voice, qualifies, sets the "we'll reach out" expectation, and **captures + forwards** hot leads — without ever texting an opted-out/suppressed contact. **BACKEND ONLY — no UI touched** (the AI config UI ships with the UI stream; for now the operator flips `ai_enabled` per client via `updateClientConfig`). **Compliance design:** the deterministic `isOptOut` / `isConfiguredOptOut("2")` / suppression gate in `lib/inbound.ts` runs FIRST and is **never model-dependent**; the AI path is wired AFTER it (and after the `twilio_sid` dedupe gate), so the AI **never runs on, or texts, an opted-out contact** and fires **at most once per inbound**. Every AI reply goes through the EXISTING suppression-checked reply gate (`replyRefusalReason` → `sendOne` → log) to the **stored contact phone only** — never a model-supplied number. **Any AI/API error or timeout → caught → fall back to the keyword path** (inbound already logged; no crash, no double-send). Auto-sends are gated to the client's **send window** (quiet hours); the conversation is **turn-capped (default 5)**; non-serious leads get a **3-strike** dismiss; a qualified lead (interest + service + wants-call) creates **exactly one hot lead + one forward** carrying a rich summary (service, timing, price-ask) and hands off (a human owns it, AI stops). **Schema (idempotent):** `clients.ai_enabled bool DEFAULT false` + `ai_services/ai_offer/ai_persona/ai_location text`; `contacts.ai_status` ('handed_off'|'dismissed') + `ai_strikes int`. Global kill switch env `AI_RESPONDER_ENABLED` (+ `ANTHROPIC_API_KEY`, optional `AI_RESPONDER_MODEL` default `claude-opus-4-8`, `AI_RESPONDER_MAX_TURNS`). **New modules:** `lib/ai-responder.ts` (PURE, DB-free orchestrator + system-prompt builder), `lib/ai-client.ts` (the real Claude call via `@anthropic-ai/sdk`, structured output, low effort), `lib/ai-db.ts` (per-contact AI state + history/turn-count helpers, client-scoped), `lib/ai-responder-wire.ts` (assembles real deps + the gated send for the webhook). `ai_enabled` ships **OFF for all clients**; Talan byte-unchanged. **Green:** `tsc` clean, `npm run build` green, `npm test` = **279** (+21: pure-core + processInbound wiring — suppressed→no reply, STOP/"2"→never AI + always suppress, turn cap stops the loop, AI error→fallback+logged-once+no double-send, qualified→exactly one lead+one forward). New **`npm run test:ai`** live-DB fixture (MOCKED Claude + Twilio, no spend/no sends) for the operator (needs DATABASE_URL). **PR opened into `main` — DO NOT MERGE until the agent-team review passes** (compliance / correctness / security: can-never-text-opted-out, stored-phone-only, no-crash/no-double-send). **Operator before enabling:** set `ANTHROPIC_API_KEY` + `AI_RESPONDER_ENABLED=true` in Vercel, run `npm run schema`, then flip a client's `ai_enabled`.

### Prior phase
**Revamp R1 — "Fresh" design system + retheme login/cockpit/dashboard BUILT (2026-06-28, Claude Code) — DEPLOYED.** Replaced the V7 indigo/slate look with the **"Fresh"** identity: warm, rounded, friendly, **teal** accent. **The accent is a re-themable token** — `app/globals.css :root` defines `--brand`/`--brand-strong`/`--brand-fg`/`--brand-tint`/`--brand-tint-fg` (default LeadFlow teal) and `tailwind.config.ts` maps a `brand` color family to them, so every kit component reads `bg-brand`/`text-brand-strong`/`ring-brand-tint` — never a literal teal. **Overriding `--brand*` on any wrapper re-themes its whole subtree** (the R3 white-label mechanism; proven in the compiled CSS). Neutrals slate→**warm stone**; cards **16px** (`rounded-2xl`), controls `rounded-xl`; Inter kept but **two weights (400/500)** + **sentence case everywhere** (ALL-CAPS stat labels de-capped); new teal **droplet wordmark**. Rethemed the whole shared kit (`components/ui/*`, **same props**: Badge `indigo`→`brand`, ProgressBar `indigo`→`brand`, SegmentedToggle `brand` active with `indigo` kept as a legacy alias so the out-of-scope `campaign-bar.tsx` still compiles) and applied it to **login + operator cockpit (leading service icon per client card) + operator dashboard**. **NO app logic / routes / queries / suppression / eligibility / send path / auth / component props touched — presentation only.** **Green:** `tsc` clean, `npm run build` green, `npm test` = **258**, `test:isolation`/`test:access`/`test:cockpit`/`test:auto-pause`/`test:passthrough`/`test:optout`/`test:forward` ALL pass. **Drive-by fix:** corrected a pre-existing typo in the `isolation`+`access` fixtures (the 2026-06-27 INCIDENT-fix moved the throwaway tenant to `C2=900002` but 4 assertions still checked the owned/resolved id against the old literal `2`; the arbitrary *requested*-id `2`s were already correct) — those 2 fixtures were RED on clean HEAD before this. **Deployed** `vercel --prod`. **Next: R2 (inbox + campaign/run screens), then R3 (white-label client portal — per-client `--brand` + logo + name, maybe custom domain).**

### Prior phase
**Multi-recipient lead forwarding BUILT (2026-06-27, Claude Code).** A client's `forward_phone` may now hold SEVERAL numbers (comma/semicolon/whitespace-separated) so each lead pings more than one person (e.g. the operator + the client owner). Small ADDITIVE change to lead delivery — suppression / eligibility / classification / the send path / the inbound signature gate are UNTOUCHED. New pure `lib/forward-phones.ts parseForwardPhones` (split, trim, dedupe by last-10) + `isProbablyPhone`; `forwardLead` pings EACH recipient via `sendOne` (sequential, never throws), marks `forwarded` if AT LEAST ONE succeeds, logs each per-recipient failure with its code; total failure → false, lead stays on the dashboard as today. **A single number is byte-identical to before.** `buildLeadPing` text unchanged. Talan/client-1 `TALAN_FORWARD_PHONE` fallback stays (scoped to client 1) and may itself be a list. **UI:** the create/edit form's forward field is a textarea ("comma-separate multiple numbers") with non-blocking validation; `updateClientConfig` already writes `forward_phone` as free text (no schema change). **Green:** `tsc` clean, `npm run build` green, `npm test` = **250** (+11), new **`npm run test:forward`** = 16/16 live-DB (MOCKED `sendOne`: two recipients → two pings + `forwarded=true`; one-fail-one-success → still true; all-fail → false + lead stays; single → one ping), `test:isolation`/`test:access`/`test:cockpit`/`test:auto-pause`/`test:passthrough`/`test:optout` all pass, DB pristine.

### Prior phase
**2nd client onboarding + per-client opt-out keyword + Add/Edit client UI BUILT (2026-06-27, Claude Code).** The operator can now onboard + configure a 2nd client (Jeremy, Orlando powerwashing) end-to-end from the cockpit, with his own Twilio number, his own message copy, and a `Reply "2" to opt out` instruction the system **actually honors**. **Schema:** nullable `clients.optout_keyword` (+ `optout_instruction`); NULL = STOP-only (Talan unchanged). **Compliance core:** pure `isConfiguredOptOut(body, keyword)` matches the keyword ONLY as the EXACT whole normalized body (so for `'2'` only `"2"`/`" 2 "`/`'"2"'`/`"2."` opt out, NEVER a "2" inside other text); `lib/inbound.ts` decision is `isOptOut(body) || (keyword && isConfiguredOptOut(...))`, checked BEFORE classification with STOP's unconditional precedence. **`isOptOut` (STOP family) is UNCHANGED + always authoritative — the keyword is purely additive.** `lib/sms.ts renderMessage` gained a per-client opt-out line (`optOutInstructionFor`) so a "2" client never gets a contradictory second STOP line; **Talan render is BYTE-IDENTICAL** (proven). **UI:** `components/client-form.tsx` (`+ New client` / per-card `Edit` on the cockpit) with a live message preview (real `renderMessage` + `segmentInfo` + per-client opt-out line) + opt-out-keyword field + a client-login-creation field; wired to `POST`/`PATCH /api/clients` (+ `lib/clients.ts createClient`/`updateClientConfig`). **Did NOT touch** `getEligibleContacts`/`claimForSend`/the send window/the inbound signature gate/`recordOptOut`/`markSuppressed`. **Green:** `tsc` clean, `npm run build` green (`/api/clients` registered), `npm test` = **239** (+31), `test:isolation` 28/28, `test:access`/`test:cockpit`/`test:auto-pause`/`test:passthrough` all pass, new **`npm run test:optout`** (16 live-DB assertions: throwaway client keyword='2' → "2"/STOP suppress, "2 services" does not, STOP-only config does not, Talan stays NULL) self-cleaned → live DB pristine. Schema applied live (67 stmts). **This supersedes/closes the 2026-06-22 "Type 2 to opt out was rejected" concern** (see overview.md). This is the prerequisite for the conversational-AI responder prompt.

### Prior phase
**Send-rate cap RAISED 1000 → 20000/hr + batch cap 50 → 250 (2026-06-25, Claude Code) — DEPLOYED.** The operator (experienced, A2P-compliant 10DLC sender) can now set `send_rate_per_hour` up to **20,000** and the pipeline actually achieves it. Surgical change, **no safety weakened**: `setClientSendRate` clamps via the new shared pure `clampSendRate` in `lib/pipeline.ts` (`MAX_SEND_RATE_PER_HOUR = 20000`, integer, min 1); `sendBatchSize` still `≈ rate/20` but clamped `[1, 250]` so high rates materialize without a batch nearing the 300s function limit (worst case ~179s at 5000/hr, faster above) and the realized rate ≈ target; the UI rate input `max` now reads the shared constant. **Untouched (load-bearing):** atomic `claimForSend` (no-double-send), the per-batch send-window re-check, opt-out/suppression/eligibility, and the send-confirm modal. **Green:** `tsc` clean, `npm run build` green, `npm test` = **211** (pacing tests updated: new clamps + `batchSize ≤ 250` + realized-rate ≈ target + `clampSendRate` accepts ≤20000 / rejects ≤0 + non-integers), `test:isolation` 28/28, `test:access` / `test:cockpit` / `test:auto-pause` / `test:passthrough` all pass. **Deployed** `vercel --prod` → `https://leadflow1-seven.vercel.app`. Operator: set the Rate/hr field to whatever your Twilio 10DLC throughput supports (≤20,000), **Save rate**, then **Run pipeline** — realized speed is still bounded by how fast Twilio accepts sends, but the app no longer throttles below your number.

**V7 phase 1 (operator launch UI redesign + no-scrub toggle) BUILT + DEPLOYED (2026-06-25, Claude Code).** The launch is now **fully point-and-click** — no command needed. Built a shared Tailwind-only **UI kit** (`components/ui/`: Button, Card, StatTile, Badge, Field/Input, Select, SegmentedToggle, ProgressBar, AppHeader, wordmark + inline SVG icons; Inter font; **indigo-600** accent, light theme) and redesigned the three launch screens — **login, operator cockpit, operator dashboard** — into a consistent, branded product UI. **Functional add (the only behavior change):** a **no-scrub toggle** on the upload form (`components/campaign-bar.tsx`) wired to the EXISTING `scrubMode` param on `POST /api/campaigns` (segmented "Standard" `vendor` vs "No scrub — send whole list" `none`, default Standard) + a per-campaign mode badge on the selector. **NO send / eligibility / suppression / auth / cycle / billing / API behavior changed** beyond surfacing that param; every safety (send-confirm modal, send-window gate, disabled states) preserved. **Green:** `tsc` clean, `npm run build` green, `npm test` = **208**, `test:isolation` = **28/28**, `test:access` / `test:cockpit` / `test:auto-pause` / `test:passthrough` all pass. **Acceptance proven:** a throwaway create with the toggle path asserted `scrub_mode='none'`, then deleted — live DB pristine. **Deployed** `vercel --prod` → live at `https://leadflow1-seven.vercel.app` (login → cockpit → dashboard all redesigned). **Now-click-only launch:** log in → on the client's dashboard, Upload new list → pick the 2,500 CSV, set **DNC scrub = No scrub**, Create campaign + import → set Rate/hr (~300) + Save rate → Run pipeline (type CONFIRM). The look is subjective — expect operator tweak requests; the shared kit makes iterating fast. **Phase 2 (next):** redesign the client portal (`/client`) + inbox (`/inbox`) with the same kit.

**Prod login FIXED → ✅ GO for the 2,500 launch (2026-06-25, Claude Code).** Root cause was an EMPTY `users` table (the operator's earlier `seed:users` never wrote a row); prod was confirmed to read the SAME Neon DB as `.env.local` via a live throwaway-login probe (303 → `/`). Seeded both users with the operator-supplied passwords: operator `#40 jordan@xalent.ai` (role=operator) + client `#41 Texexteriors@gmail.com` (role=client, client_id=1). **Verified for real (not assumed):** `verifyPassword(typed pw, storedHash)` == **true** for both, AND a live prod `POST /login` returns **303 → `/`** for the operator and **303 → `/client`** for the client. No code changed (data/config fix only). DB clean: exactly 2 users, 1 client, 1 campaign, 0 stray. **The operator can now sign in to `https://leadflow1-seven.vercel.app/login`.** Remaining = the campaign itself (operator's manual go): fund Tracerfy ~$50, upload the 2,500 CSV as a `scrub_mode='none'` campaign, set rate ~300/hr, Run — runbook in `launch-readiness.md`.

### Prod login diagnosis — DONE (for the record)
Symptom: prod `/login` → "Incorrect email or password" for `jordan@xalent.ai`. `users` table was EMPTY. Wrong-DB theory ruled out by a throwaway-user prod-login probe (prod authenticated a user created in the `.env.local` DB → same DB; Vercel marks `DATABASE_URL` Sensitive so `vercel env pull` returns it empty, can't byte-compare). `upsertUser` already upserts the hash; email looked up `lower(email)`. Fix was simply to RUN the seed with creds.

### v2 DEPLOYED to prod phase — DONE
**v2 DEPLOYED to prod (2026-06-25, Claude Code) → GO pending 2 operator env/seed steps.** Committed `8626f84` (V1–V6 + Module N), pushed to `origin/main`, `vercel --prod` → READY at `https://leadflow1-seven.vercel.app`. Prod HTTP smoke healthy: `/login` is the v2 LeadFlow login (not the old admin-password gate), `/`→307→`/login`, operator routes 401, webhook (no sig) 403; schema applied to the Neon DB (65 stmts, idempotent). **Module N correctness gate: CLEAN** — an independent read-only reviewer confirmed all 3 invariants HOLD (passthrough writes `scrub_status` only, no vendor call/scrub_jobs; `getEligibleContacts`/`claimForSend` unchanged + opt-out exclusion independent of scrub flag; route branches `none`→passthrough else `scrubBatch`) with NO Critical/High. **Two operator actions remain before login works (I could not do them — they need secrets I must not invent):** (1) **`SESSION_SECRET` is MISSING in prod** — V5 login fails closed without it (login POST 500s); operator must `vercel env add SESSION_SECRET production` (≥32 random) **and redeploy** (new env only applies to a fresh deploy); (2) **0 users seeded** — operator sets `OPERATOR_EMAIL/PASSWORD` + `CLIENT1_EMAIL/PASSWORD` and runs `npm run seed:users`. Full runbook in `launch-readiness.md`. Live DB pristine (1 client, campaign 1 'vendor', 0 users). **One-line: NO-GO until SESSION_SECRET + seed users; everything code/deploy-side is GO.**

### Module N (no-scrub send mode) BUILT phase — DONE (review-clean)
**Module N (no-scrub send mode) BUILT (2026-06-25, Claude Code) → correctness pass CLEAN (see current phase).** Per-campaign `campaigns.scrub_mode text NOT NULL DEFAULT 'vendor'` ('vendor'|'none'). On a `'none'` campaign the scrub stage is a **passthrough** (`lib/scrub-passthrough.ts` `passthroughScrubBatch`) — one scoped UPDATE marks the campaign's matched + with-phone + still-`pending` contacts `scrub_status='clean'` with **NO Tracerfy call, NO credits, NO scrub_jobs row**; mirrors `getContactsForScrub`'s predicate exactly minus the vendor call. `app/api/scrub` branches on `campaign.scrub_mode` ('none' → passthrough, else the existing `scrubBatch` byte-unchanged); the response shape is identical so `components/pipeline-runner.tsx` is untouched. Mode is set on create (`POST /api/campaigns` optional `scrubMode`, default 'vendor') or flipped (`PATCH /api/campaigns {campaignId, scrubMode}`, operator-only + client-scoped). **Eligibility + opt-out/STOP suppression UNCHANGED** — passthrough touches `scrub_status` only; the `NOT EXISTS opt_outs` check is independent, so an opted-out contact stays excluded even after being marked clean. Green: `tsc` clean, build green (registers the `/api/campaigns` PATCH + the 'none' scrub branch), `npm test` = 208, `test:isolation` = 28/28, `test:access` pass, `test:cockpit` pass, `test:auto-pause` pass, **new `npm run test:passthrough` = 24/24** (passthrough marks clean w/ no vendor call + no scrub_jobs; opted-out contact clean-but-still-excluded; no-phone stays pending; 'vendor' routes to scrubBatch; idempotent + limit-respecting; setter validates + is client-scoped; client 1 byte-unchanged). Schema applied live (65 stmts; pilot campaign backfilled to 'vendor'); live DB pristine after (1 client, campaign 1 'vendor', 0 stray). **Deviation:** the explicit live HTTP smoke was substituted by `test:passthrough` (same live-DB code path; login is a server action + 0 users are seeded, so a scripted HTTP login is brittle — the route is a thin auth+branch wrapper proven by tsc+build). **Next: the single-reviewer correctness pass, then this unblocks the 2,500 launch** (remaining launch blockers are deploy-to-prod + seed users — see `launch-readiness.md`).

### Launch-readiness check (2026-06-25) — superseded by Module N above for item E
**VERDICT was ❌ NO-GO.** Full report in `launch-readiness.md`. Three hard blockers: (1) **Module N (no-scrub mode) was NOT built** — NOW BUILT (see above) — no `scrub_mode` column, no passthrough helper, no `scrub` route branch, no `test:passthrough`; the scrub route only ever calls the vendor `scrubBatch`, and eligibility hard-requires `scrub_status='clean'`, so a no-scrub campaign cannot send (and the only way to make it sendable today spends Tracerfy credits). **Run `sessions/no-scrub-mode.md` first.** (2) **Prod is at V1** — local `HEAD` = `origin/main` = `5a19d5c`; all of V2–V6 + Module N are uncommitted (79 files), so the deployed `leadflow1` app has none of login/campaigns/cockpit/auto-pause. Must commit+push+deploy after Module N. (3) **0 users seeded** — V5 login replaced the shared admin password; nobody can log in until `npm run seed:users` runs (locally + prod) with `SESSION_SECRET` set. Everything else is GREEN: `tsc` clean, build green, `npm test` = 208, `test:isolation` = 28/28, `test:access` pass, `test:cockpit` pass, `test:auto-pause` pass, `smoke:webhook` 5/5; send window = 10:00–19:00 America/New_York `[start,end)` with live-rate control; eligibility + opt-out suppression intact (`scrub_status='clean'` AND `NOT EXISTS opt_outs`, independent). Live DB pristine (1 client, pilot campaign, 500 contacts/91 sent, 0 users, 0 invoices, 5 opt_outs) — no throwaway rows created. **Operator to-dos before launch:** build Module N → deploy the tree → seed users + set `SESSION_SECRET` in Vercel → fund Tracerfy ~$50 for the 2,500 skip-trace → raise send rate to ~300/hr (confirm within Twilio 10DLC daily cap) → set client 1 from_number/forward_phone + create the `scrub_mode='none'` campaign. Need ~300/hr to clear 2,500 inside the 9h window.

### (v2 Module V6) BUILT phase — DONE (pre-review)
**v2 Module V6 (lead-target auto-pause + track-only billing) BUILT → awaiting the focused send-path review.** Deliver-then-stop: each client has a per-period lead TARGET (`lead_target`, nullable → falls back to `lead_guarantee`; `target_period` 'week'|'month'). The **send route is SERVER-enforced** — `app/api/campaign` consults `getTargetStatus` (`lib/auto-pause.ts`) before any claim/send, read fresh each batch; once `leads-this-period >= target` it refuses (`paused`/`target_met`) and the pipeline shows "Target met — paused until <date>", resuming on target-raise or period rollover. The gate is **layered ON TOP of suppression/eligibility and purely additive** (never makes a suppressed/opted-out contact sendable; never touches eligibility). Week = ISO Mon–Mon UTC; month = the V4 billing cycle. **Track-only billing (NO Stripe):** `client_invoices` (one row per client per cycle, due→invoiced→paid) marked from the cockpit (`POST /api/billing`); next-bill-date + status surfaced there. Talan unchanged (lead_target NULL → 50/month). Green: build, `npm test` = 208 (+9 lead-target), `npm run test:isolation` = 28/28, `npm run test:access` = 28, `npm run test:cockpit` pass, new **`npm run test:auto-pause`** proves the server gate stops EXACTLY at the target (off-by-one safe, contacts still remaining), resumes on raise/rollover, holds for week+month, never weakens suppression, and billing materializes/marks/scopes + surfaces. Live DB pristine. **Next: the focused send-path review** (stops exactly at target, no over-send/off-by-one, resumes on rollover, server-enforced, suppression intact).

### (v2 Module V5) BUILT phase — DONE (pre-review)
**v2 Module V5 (client login + client dashboard — SECURITY-CRITICAL) BUILT.** Replaced the single shared `ADMIN_PASSWORD` gate with **real per-user login** (a `users` table; operator vs. client roles; scrypt-hashed passwords; an httpOnly+secure, HMAC-signed session cookie) AND a clean, read-mostly, branded **client dashboard** (`/client` → `components/client/portal-client.tsx`: the client's own leads feed + progress to their 50/mo guarantee, polling the scoped `/api/portal`). **THE V1 `?clientId=` ACCESS-CONTROL GATE IS CLOSED:** the resolved client for EVERY request now comes from the logged-in user's session via the single pure chokepoint `resolveClientIdForUser` (`lib/access.ts`) — an **operator** may act on any client (`?clientId=` honored), a **client user is hard-locked to their own client_id** and any foreign id → 403; the cookie can't be forged (signature-verified, then the user is RE-LOADED from the DB so role/client_id are authoritative). Every operator route + page now uses `requireOperator` (401 unauth / 403 client-role); `/login` is the single entry; role routing sends operator→cockpit, client→`/client`. Did NOT touch send / suppression / eligibility. **Green:** tsc clean; `npm run build` (registers `/login`, `/client`, `/api/portal`; no DB driver in client bundles); **`npm test` = 199** (+16 auth/access/throttle); **`npm run test:isolation` = 28/28**; **`npm run test:cockpit`** pass; new **`npm run test:access`** fixture PROVES a client-2 user can't reach client-1 by any vector (resolveClientIdForUser denies every foreign id, forged/tampered tokens fail, scrub of scoped portal data, passwords are scrypt-only) + a wired **HTTP smoke** (operator routes 401/403/200 by role, client 403 on every pipeline route, `/api/portal?clientId=2` 403 for a client-1 user, all page redirects by role) — live DB pristine after (0 users, 1 client). Schema applied live (`users` + unique lower(email)); seeding is `npm run seed:users` from env-provided passwords (no committed creds). **Next: run the agent-team SECURITY review (access-control / auth / correctness).** Any cross-client access by a client user, or an auth weakness, is Critical.

### (v2 Module V4) BUILT + LIGHT-REVIEW-CLEAN phase — DONE
**v2 Module V4 (operator lead-guarantee cockpit) BUILT + LIGHT-REVIEW-CLEAN → ready for V5.** The new landing after admin login is the **operator cockpit** (`app/page.tsx` → `components/cockpit-view.tsx`): every client at a glance, centered on **leads-this-cycle / lead-guarantee** with a **behind / on-track / met** pace flag, plus this-cycle sent count + reply rate + opt-out rate and days-left; **behind clients sort first** (the actionable bit). Clicking a client opens their existing scoped dashboard (`/dashboard?clientId=N`); a "← All clients" link returns. The cross-client aggregate (`lib/cockpit.ts` `getCockpitData`) is **operator-only by design** (the operator owns all clients) and limited to per-client SUMMARY counts — no contact-level data crosses a boundary, and no client-facing route consumes it (V5 adds per-client logins). Pace/cycle math is pure + unit-tested (`lib/billing-cycle.ts`). **Cycle = billing_day→same-day-next-month in UTC; null billing_day = calendar month; day>month-length clamps to the last day** (decided 2026-06-24, see overview.md). Did NOT touch send / suppression / eligibility. Green: tsc clean, build (`/` 138 B client, no DB driver leaked), **`npm test` = 183** (+10 billing-cycle), **`npm run test:isolation` = 28/28**, new **`npm run test:cockpit`** fixture (seeds client 2 with this-cycle + prior-cycle leads + health rows → proves the cockpit counts ONLY this cycle per client, the pace flag is right, and client 1 is unchanged), live DB confirmed pristine (1 client, 0 stray rows). **Light review (single read-only reviewer) PASSED — no Critical/High; confirmed the aggregate is operator-only + correctly scoped, per-client drill-downs stay scoped, and the cycle/pace math holds incl. short-month clamping.** **Next: generate the V5 prompt (client dashboard + per-client login).** The carried V1 `?clientId=` access-control gate is now the HARD prerequisite V5 solves before a 2nd client goes live.

### (v2 Module V3) BUILT + REVIEW-CLEAN phase — DONE
**v2 Module V3 (guided pipeline + de-finicky) BUILT + REVIEW-CLEAN → ready for V4.** The 2-reviewer focused send-driver pass (correctness / compliance, Sonnet) found **no Critical, no High**; both load-bearing claims HOLD — **no double-send** (the atomic `claimForSend` is the sole gate; row-locking means one writer per contact regardless of batch count / runId adoption / two tabs) and **suppression re-checked on every batch** (a STOP mid-run is excluded from the next batch AND refused at claim time in the current one). Applied 1 cheap Medium (correctness M1 — a try/finally so an exception always closes the run, so the stall can't reappear via an error path); logged the rest (two-tab co-drive = no double-text, V5; the deliberate "block second operator" 409-halt; cosmetic schema self-doc). Re-verified: tsc/build/**173 tests**/**28-assertion isolation** all green. One-click **Run** drives trace→scrub→send to completion via a client-side driver looping the existing batch endpoints (no babysitting, no 300s timeout, resumable). Both Talan-pilot send bugs fixed (the run-not-closing stall + "undefined sent/failed"); live send-rate control via `PATCH /api/client` (read fresh each batch, no redeploy). **Next: generate the V4 prompt (operator lead-guarantee cockpit).** The V1 `?clientId=` access-control gate + the shared-admin co-drive limitation both still stand before a 2nd client / real concurrency (V5).

### (v2 Module V2) BUILT phase — DONE
**v2 Module V2 (campaigns + CSV uploader) BUILT** (pre-review). A client now runs many campaigns; the pilot is campaign 1 under client 1 (behavior unchanged). A CSV uploader creates a new campaign + imports its list (replaces the import script). Trace/scrub/send/eligibility/dashboard are scoped by campaign; **suppression stays CLIENT-level by phone** — eligibility excludes any phone in the client's `opt_outs`, so an opt-out under one campaign excludes that phone from all campaigns (proven by the isolation fixture's new client-level-suppression checks). Green: schema (migration verified live), build, 166 tests, 26/26 isolation; live upload end-to-end verified + cleaned up (DB back to pristine pilot). **Next: run the focused review (client-level suppression + campaign scoping), then generate the V3 prompt (guided pipeline + de-finicky).** The V1 `?clientId=` access-control gate still stands before onboarding a 2nd client (that's V5).

### (v2 Module V1) Current phase — DONE
**v2 Module V1 (multi-tenant foundation) BUILT + REVIEW-CLEAN.** Model confirmed: agency / done-for-you, **$2,500/mo per client + 50-leads/month guarantee**, operator (Jordan) runs campaigns, clients get a branded dashboard. Build plan in `modules-v2.md`. **V1 is in + reviewed:** `clients` table + `client_id` on every table + every query scoped by client; Talan migrated as client 1 (behavior byte-identical); per-client config moved env→record; webhook routes by To→client. The 3-reviewer client-isolation pass found **no Critical/High** and confirmed no data/suppression crosses a client boundary; 5 cheap Medium/Low fixes applied + re-verified (schema/build/160 tests/22-assertion isolation all green). **One logged gate for later:** `?clientId=` has no per-client access control yet — must be solved before onboarding a 2nd client (that's module V5, client logins). **Next: generate the V2 prompt — campaigns + CSV uploader** (`modules-v2.md` row V2), one prompt at a time as in v1.

## (v1) Current phase — DONE
**App built (Modules 1–5 + 7) + Session 6 Task 0 (copy locked) done; Vercel env vars set.** **Module 7 (inbox + reply + lead tracking) is BUILT and agent-team review-clean** — `/inbox` with conversation threads, an admin-gated reply box that texts homeowners back from the campaign number (suppression-checked, stored-phone-only, logged), and `leads.status`/`notes`. The review confirmed both adversarial claims HOLD (no arbitrary-number send; no reply to an opted-out person) with no Critical; 2 High fixes applied + re-verified. Remaining: **→ Session 6 deploy + self-test → live pilot.** Copy locked; Twilio off-trial + 10DLC approved. **Durability hotfix (2026-06-23) DONE:** orphaned trace queue 103802 recovered for free (credits 501→501; all 500 now traced, 441 with_phone, pending 0) and the bug fixed — trace job ids persist (`trace_jobs`) + `traceBatch` is resumable, so a crash can no longer strand paid results. **Scrub re-billing hotfix (2026-06-23) DONE:** scrub now selects `scrub_status='pending'` only (no re-billing of clean contacts) + a credit pre-flight refuses before spending + `scrub_jobs` makes scrub resumable; proven with 0 credits spent. **Next concrete step: top up Tracerfy to ≥175 credits (have 1), then `npm run scrub` to finish the 175 pending → final eligible ≈ 91 + clean-of-175.**

## Completed
- 2026-06-25 (Claude Code) **v2 Module V6 (lead-target auto-pause + track-only billing) — DONE. Awaiting the focused send-path review.** Reframed "billing" → **deliver-then-stop**. **Schema:** `clients.lead_target int` (NULLABLE → falls back to `lead_guarantee`) + `clients.target_period text NOT NULL DEFAULT 'month'`; new `client_invoices` (client_id, period_start/end, amount_cents, status due|invoiced|paid, invoiced_at/paid_at) with a unique `(client_id, period_start)` (idempotent materialize). **Pure math** `lib/lead-target.ts`: `effectiveLeadTarget` (null→guarantee, floored ≥0), `currentTargetPeriod` (month → V4 billing cycle; **week → ISO Mon 00:00 UTC → next Mon, exclusive**), `toTargetPeriod`, `nextPeriodLabel` — 9 unit tests. **The gate** `lib/auto-pause.ts getTargetStatus(client, now)` counts `countLeadsInPeriod` (new in `lib/db.ts`, half-open [start,end)) and sets `met = target>0 && leads>=target`. **Server enforcement:** `app/api/campaign` POST, right after the active-run lookup and BEFORE run-create/claim/send, calls getTargetStatus(client) (read fresh each batch) — if met it closes any own active run and returns `{ paused:true, reason:'target_met', done:true, message }` (200) so the pipeline driver stops cleanly; the gate sits ON TOP of suppression/eligibility and is purely additive. `components/pipeline-runner.tsx` shows the paused message. **Billing** `lib/billing.ts` (ensureInvoiceForCurrentCycle / getCurrentInvoice / markInvoice / listInvoices, all client-scoped; timestamps normalized to ISO since neon returns Date) + operator-only `POST/GET /api/billing`. **Surfacing:** cockpit (`lib/cockpit.ts` rows gain target/leadsThisPeriod/autoPaused + planAmountCents/nextBillDate/invoiceStatus/invoiceId; `components/cockpit-view.tsx` shows an auto-pause badge + `components/cockpit-billing.tsx` mark-invoiced/paid buttons), operator dashboard (`lib/dashboard.ts autoPause` + a badge in `dashboard-client.tsx`), client portal (`lib/portal.ts targetMet/targetResetsOn` + a note in `portal-client.tsx`). `setClientLeadTarget` in `lib/clients.ts` + `PATCH /api/client` now also accepts `leadTarget`/`targetPeriod` (the input WIDGET is deferred to V7). **Did NOT touch** the suppression/eligibility/claim path or the inbound webhook — auto-pause only ADDS a stop. All files ≤500 (campaign route 445, db 448). **Verified:** `npx tsc --noEmit` clean; `npm run build` green (registers `/api/billing`; no DB driver in client bundles); **`npm test` = 208** (+9); **`npm run test:isolation` = 28/28**; **`npm run test:access` = 28**; **`npm run test:cockpit`** pass; new **`npm run test:auto-pause`** (26 assertions) PROVES: target=2 → 0/1 not met, **2 met while 4 eligible contacts STILL remain** (stop is the gate, not an empty pool), opted-out contact excluded met-or-not (suppression intact), eligibility identical met-or-not (purely additive), raise 2→3 resumes, next-month rollover resumes (prior leads excluded), **half-open boundary** (a lead at the cycle end counts only in the NEXT period), week period met-then-rolls-over, billing materialize/idempotent/cross-client-scoped/mark invoiced+paid, cockpit autoPaused+nextBillDate(Jul 1)+invoice status, and **client 1 lead_target NULL → effective target = guarantee 50** (Talan unchanged). Live DB pristine after (1 client, 0 invoices). **Deviations / sub-decisions:** (1) lead_target NULLABLE (not NOT-NULL-DEFAULT) to match the nullable-inherit pattern + avoid a re-apply clobber; (2) week = ISO Mon–Mon UTC (noted in overview.md); (3) gate returns 200 `done:true` (not 4xx) so the existing driver loop stops without an error path; (4) added `client_invoices` cleanup to the isolation/access/cockpit fixtures (defensive — only the cockpit read could ever materialize, and it doesn't, but the FK makes the delete order matter); (5) backend setter + PATCH support shipped, the UI input deferred to V7. **Out of V6 scope (untouched):** Stripe/payment processing, the V7 visual redesign. **Next: the focused send-path review.**
- 2026-06-25 (Claude Code) **v2 Module V5 SECURITY-REVIEW HARDENING BATCH — APPLIED. All 5 logged Medium/Low items from the V5 review, all on the auth/access path, zero behavior risk. V5 now fully review-clean.** (1, Med/auth) `lib/auth.ts verifyPassword` now bounds-checks the scrypt N/r/p read from the stored hash — N∈[16384, 2^20], r∈[8,32], p∈[1,4]; anything outside → false (a DB-write compromise can't downgrade the KDF or DoS login via absurd cost params). (2, Low/auth) raised the `SESSION_SECRET` runtime minimum from 16→32 chars in `getSecret()` (matches `.env.example`; all test/fixture secrets were already ≥32, so no breakage). (3, Low/auth) `app/actions.ts` login now runs scrypt against a module-level `DUMMY_HASH` on the no-such-user path so response timing can't distinguish "unknown email" from "wrong password" (no account enumeration). (4, Low/access) added 2 assertions to `scripts/access-control-fixture.ts` (now 28, was 26): `resolveCampaignForClient(client 1, <client-2 campaign>)` returns client 1's OWN campaign and NEVER the foreign id, and `/client` is operator-unreachable (the page's `isOperator → redirect` guard). (5, Low/correctness) added `import "server-only"` to `lib/{session,guard,db,users,portal,cockpit}.ts` as a build-time guard against accidental client-bundle import. **tsx infra note:** the tsx-run fixtures import these server-only libs directly, and the published `server-only` package throws by default outside a React-Server build, so added a tiny empty shim `shims/server-only.ts` mapped via tsconfig `paths` (`"server-only": ["./shims/server-only.ts"]`) — tsx resolves the import to a no-op while `next build` keeps its own enforcing alias (verified: build green + clean client bundles). **Did NOT do (correctly deferred):** the durable login rate-limiter (in-memory is fine pre-scale) and the client-portal `tel:` link (a product question for the operator). **Verified all green:** `npx tsc --noEmit` clean, `npm run build` green, **`npm test` = 199**, **`npm run test:isolation` = 28/28**, **`npm run test:access` = 28** (both new assertions pass), **`npm run test:cockpit`** pass; live DB pristine. **Next: generate the V6 prompt (billing).**
- 2026-06-25 (Claude Code) **v2 Module V5 SECURITY REVIEW (3-reviewer agent team, read-only Sonnet) — COMPLETE. NO Critical, NO High. The V1 access-control gate is confirmed CLOSED; auth is sound. Applied 0 (none qualified); logged Medium/Low.** Three independent reviewers (access-control / auth / correctness) audited the per-user-login + closed-gate refactor and challenged each other on "a logged-in client can do nothing but read their own leads" and "the session can't be forged or escalated." **All confirmed both HOLD.** **Access-control:** `resolveClientIdForUser` (`lib/access.ts`) is the SOLE client-resolution chokepoint; every operator route + page goes through `requireOperator` (403s a client by role before resolution) + it; a client user resolves ONLY to their own client_id (foreign `?clientId=` → 403) and can reach NOTHING but `GET /api/portal` (locked) + the `/client` page (session client_id, no param). Body-ids are DB-scoped (`getContactById`/`setLeadStatus`/`getThread`/`isPhoneOptedOut` all filter `AND client_id=`), `resolveCampaignForClient` rejects a foreign campaignId (falls back to the resolved client's own), the webhook resolves tenant by To-number (not a param), and cookie forge/replay are blocked (HMAC + DB reload). No cross-client vector found. **Auth:** scrypt (salted, constant-time `timingSafeEqual`, fail-safe on malformed hash), HMAC-signed cookie (httpOnly + secure-in-prod + sameSite=lax + 8h, signature verified before any trust, expiry enforced), `SESSION_SECRET` fail-closed (no fallback), fresh token minted at login (no fixation), session re-loads the user from DB each request (revocation + authoritative role/client_id), generic login error (no enumeration distinguishable from the response), no hash/secret/PII leakage in responses or logs. **Correctness:** role routing correct (unauth→/login; client→/client off operator surfaces; operator→/ off /client) with NO SSR data fetched before the role redirect; the client portal is read-only (zero controls, only `GET /api/portal`, type-only imports so no DB driver in the browser bundle) and scoped to the client's own leads; Talan/client-1 send/window/rate/suppression/eligibility byte-unchanged (V5 added only auth-gating); seed script sound (env creds, hashed, idempotent); no dead refs to the removed `isAuthed`/`ADMIN_PASSWORD`/`clientIdFromRequest`; logout clears the cookie. **Re-verified:** `npm run build` green, **`npm test` = 199**, **`npm run test:isolation` = 28/28**, **`npm run test:access`** all-pass. **LOGGED (Medium/Low — NOT applied per the apply-C/H-only directive; concrete patches recorded for a follow-up):** (M, auth) **scrypt cost-param hardening** — `verifyPassword` parses N/r/p from the stored hash with no bounds; add a min/max check (e.g. N∈[16384,2^20], r∈[8,32], p∈[1,4]) so a DB-write compromise can't downgrade the KDF or DoS login (not reachable from the HTTP API; defense-in-depth). (L, auth) raise the `SESSION_SECRET` runtime minimum from 16→32 chars (`.env.example` already says 32). (L, auth) the no-such-user login path skips scrypt → a minor account-enumeration TIMING side-channel; optionally hash against a dummy. (L, auth/access/correctness) the in-memory login throttle is per-serverless-instance (documented) — a durable limiter (Upstash/DB) before scale. (L, access) the operator no-`?clientId=` default silently targets client 1 — fine today (operator owns all), but consider an audit log when it fires. (L, access — fixture gaps) add to `scripts/access-control-fixture.ts`: (a) `resolveCampaignForClient(client 1, <client-2 campaign>)` never returns the client-2 campaign, (b) the `/client` page is unreachable by an operator. (L, access — doc) note in `app/api/portal/route.ts` why it uses `requireUser` (not `requireOperator`). (L, correctness) add `import "server-only"` to the server-only libs (`lib/session.ts`, `lib/guard.ts`, `lib/db.ts`, `lib/users.ts`, `lib/portal.ts`, `lib/cockpit.ts`) as a build-time guard against accidental client bundling. (Product, not security) the client portal shows each lead's phone as a `tel:` link — intentional (the client needs to call their lead, correctly scoped to their own) — confirm with Talan. **Recommended cheap follow-ups (await go-ahead):** the scrypt-bounds check, the two fixture assertions, and the `server-only` guards — all zero-behavior-risk hardening. **Module V5 is security-review-clean — ready to generate the V6 prompt (billing).**
- 2026-06-24 (Claude Code) **v2 Module V5 (client login + client dashboard — SECURITY-CRITICAL) — DONE. Awaiting the agent-team security review.** Closed the long-carried V1 access-control gate and shipped the client-facing dashboard. **Auth:** new `users` table (id, email, password_hash, role 'operator'|'client', client_id NULL-for-operator, created_at; unique on lower(email)). Passwords hashed with **scrypt** (Node core KDF, per-password random salt, constant-time verify — `lib/auth.ts`); sessions are a **stateless HMAC-SHA256-signed cookie** `b64(payload).b64(sig)` (httpOnly, secure in prod, sameSite=lax, 8h) — signature stops forgery, and on every request `getSessionUser` VERIFIES then RE-LOADS the user from the DB so role/client_id are authoritative (revocation + no stale role). `SESSION_SECRET` is required (fail-closed). Basic in-memory login throttle (5 failures / 15 min; documented best-effort on serverless). Login (`app/actions.ts`) is email+password with a single generic error (no account enumeration); `/login` is the only entry; logout clears the cookie. **THE CLOSED GATE:** the resolved client for EVERY request now flows through ONE pure chokepoint `resolveClientIdForUser(user, requestedId)` (`lib/access.ts`): operator → requested `?clientId=` (else default 1); **client user → their own client_id ONLY, any foreign id → null → 403.** Every operator API route + page swapped `isAuthed`/`clientIdFromRequest` for `requireOperator` (`lib/guard.ts`, 401 unauth / 403 client-role) + `resolveClientIdForUser`; `lib/request-client.ts` was reduced to PARSE-ONLY (`requestedClientId`) — a bare param can no longer select a client. Role routing: operator→cockpit (`/`) + full dashboards/inbox/controls; client→`/client` only (operators are redirected off `/client`, clients off `/`, `/dashboard`, `/inbox`). **Client dashboard:** `/client` (`app/client/page.tsx` + `components/client/portal-client.tsx`) is read-mostly, branded with the client's name, showing their leads feed (name/address/phone/reply/time) + progress to the guarantee (X/50 this cycle, same cycle math as the cockpit), polling the scoped `GET /api/portal`. `/api/portal` is the ONLY route a client role can use; it resolves the client from the session (foreign `?clientId=` → 403) and returns ONLY client-safe data (`lib/portal.ts`, client_id-scoped). **Untouched:** send / suppression / eligibility / the signature-gated inbound webhook. All files ≤500 lines (campaign route 409; new libs all <170). **Verified:** `npx tsc --noEmit` clean; `npm run build` green (registers `/login`, `/client`, `/api/portal`; client bundles carry no DB driver); **`npm test` = 199** (+16: scrypt round-trip/malformed-hash, token sign/verify/tamper/expire/wrong-secret, the access rules incl. the closed gate, throttle lockout); **`npm run test:isolation` = 28/28** (unchanged); **`npm run test:cockpit`** pass; new **`npm run test:access`** (26 assertions) PROVES a client-2 user can't reach client-1 by any vector — `resolveClientIdForUser(c2user, {undefined,1,2,3,999})` is only ever `{2,null}` and NEVER 1, a forged client→operator token fails verification, a different-secret token fails, the portal returns only client-2 leads (no client-1 lead id leaks), passwords are scrypt-only — then cleans up (client 1 unchanged); plus a wired **HTTP smoke** against a dev server with minted cookies (GET /api/dashboard 401/403/200 by role; POST /api/skiptrace, /api/reply, PATCH /api/client all 403 for a client; /api/portal 401 no-cookie, 200 own, 403 `?clientId=2`, 200 `?clientId=1`; page redirects: `/`→/login unauth & →/client for client, `/dashboard`&`/inbox`→/client for client, `/client`→/login unauth & →/ for operator). Live DB pristine after every run (0 users, 1 client). Schema applied live (`npm run schema` = 59 stmts incl. `users` + `idx_users_email_lower`). **Deviations / sub-decisions:** (1) **scrypt** (Node core) chosen over bcrypt/argon2 — vetted, memory-hard, NO new dependency; (2) **stateless signed cookie** (no sessions table) but DB-authoritative on every read (revocation + fresh role) — the spec's "cookie carries uid+role+client_id" with the DB as source of truth; (3) extracted `lib/constants.ts` (DEFAULT_CLIENT_ID) so the pure `lib/access.ts` + its unit test don't import `lib/db` (which throws without DATABASE_URL); `lib/clients.ts` re-exports it; (4) the client dashboard is its own surface (`/client` + `/api/portal` + `lib/portal.ts`) rather than reusing the operator `/api/dashboard` (which exposes pipeline counts) — keeps the client read-mostly + minimal; (5) users are NOT seeded in SQL (passwords must be hashed) — `npm run seed:users` upserts operator + Talan client from env passwords (no committed creds); the operator runs it on prod with chosen passwords; (6) added `SESSION_SECRET` (+ seed-user envs) to `.env.example`, marked `ADMIN_PASSWORD` deprecated. **Out of V5 scope (untouched):** billing (V6), the full visual redesign (V7), OAuth/SSO/password-reset (the client view is clean + basic-branded, not the pretty pass). **Next: the agent-team SECURITY review (access-control / auth / correctness) — any cross-client access by a client user, or an auth weakness, is Critical.**
- 2026-06-24 (Claude Code) **v2 Module V4 (operator lead-guarantee cockpit) — DONE + light-review-clean.** Built the agency control room: the operator's new landing after admin login. **What it shows:** a card per client, sorted **behind-pace first**, each with leads-this-cycle / lead-guarantee + a progress bar, a **behind / on-track / met ✓** badge, this cycle's **sent count + reply rate + opt-out rate** (quick campaign-health read), and **days left in the cycle**. Click a card → `/dashboard?clientId=N` (the existing scoped dashboard); the dashboard header gained a "← All clients" back-link. **Cross-client aggregate is OPERATOR-ONLY by design** — `lib/cockpit.ts getCockpitData()` is the one place an aggregate spans clients (the operator owns all of them); it returns per-client SUMMARY counts only (leads / sent / inbound / opt-outs this cycle), no contact-level data, and is reached ONLY from `app/page.tsx` behind `isAuthed()`. No client-facing route consumes it (per-client logins = V5). **Pure, unit-tested cycle + pace math** in `lib/billing-cycle.ts`: `currentCycle(now, billingDay)` finds the cycle CONTAINING now (UTC boundaries; null billing_day = calendar month; day>month-length clamps to the last day, e.g. 31→Feb 28); `paceFlag` = behind when leads < guarantee×(daysElapsed/cycleLength), on_track when ≥, met when leads ≥ guarantee (day-0 reads on_track, not behind). New `listClients()` in `lib/clients.ts` (operator-only). **Did NOT touch** send / suppression / eligibility / the inbound webhook — V4 adds no send surface. All files ≤500 lines (cockpit 125, billing-cycle 111, cockpit-view 119). **How I verified:** `npx tsc --noEmit` clean; `npm run build` green (`/` 138 B client, no DB driver in the browser bundle); **`npm test` = 183** (+10 billing-cycle: calendar-month vs set billing_day, before/after-anchor, billing_day=31 short-month clamp, expectedLeads, paceFlag behind/on-track/met/day-0); **`npm run test:isolation` = 28/28** (unchanged — V4 added no cross-client path to a client surface); new **`npm run test:cockpit`** fixture seeds a temp client 2 with 5 this-cycle + 3 prior-cycle leads + 10 outbound/2 inbound/1 opt-out this cycle, asserts against a FIXED clock that the cockpit counts ONLY this cycle's 5 leads, the window is Jun 1..Jul 1, pace = behind, sent=10 / reply=20% / opt-out=10%, client 1 is present (operator aggregate) and behind sorts first — then cleans up (live DB back to 1 client, 0 stray rows). **Light review (single read-only Explore reviewer) PASSED — no Critical/High:** confirmed the cycle math (incl. boundary inclusivity start-inclusive/end-exclusive + short-month clamp), the pace edge cases (day-0, zero-guarantee), the SQL scoped by client_id AND the [start,end) window with no leakage, the aggregate reachable ONLY from the operator-authed landing (grep proved no client-facing consumer), and the per-client dashboard/inbox still single-client scoped. **Deviations / sub-decisions:** (1) defined the billing cycle in UTC for determinism (per-client send TZ is a separate concern — noted in overview.md); (2) calendar month for null billing_day (Talan's billing_day is NULL) — acceptable v1 per the spec; (3) new files `lib/billing-cycle.ts` (+`.test.ts`), `lib/cockpit.ts`, `components/cockpit-view.tsx`, `scripts/cockpit-fixture.ts` + `npm run test:cockpit`; (4) the cockpit replaces the old `/` contact-counts placeholder as the landing. **Out of V4 scope (untouched):** client logins / `?clientId=` access control (V5), billing collection (V6), visual redesign (V7). **Next: generate the V5 prompt (client dashboard + per-client login).**
- 2026-06-24 (Claude Code) **v2 Module V3 FOCUSED SEND-DRIVER REVIEW PASS (agent team) — COMPLETE. No Critical, no High; both load-bearing claims HOLD. Applied 1 cheap Medium; logged the rest.** A 2-reviewer read-only team on Sonnet (correctness / compliance, `Explore`) audited the driven send pipeline and challenged each other on "the driver can never double-text and can never text an opted-out phone, even across many batches." **Both independently confirmed both HOLD.** **Correctness:** every campaign-POST return path traced — a run closes (`finished_at`) exactly when the eligible set drains or the window shuts mid-batch, and `createCampaignRun` stamps `last_batch_at=now()` so the staleness guard (`COALESCE(last_batch_at, started_at) > now()-6min`) self-heals any crash-orphaned run (the old permanent stall is GONE — explicitly refuted); `getActiveCampaignRun`'s `ORDER BY id DESC LIMIT 1` + the strict `requestedRunId===active.id` check lets the driver continue its OWN run while 409-blocking a fresh/foreign caller; **no double-send path exists** — `claimForSend`'s single-statement atomic `UPDATE … WHERE send_status='not_sent' RETURNING id` is the sole gate before any Twilio call, Postgres row-locking elects one writer per contact (two tabs sharing a runId → loser gets `skippedClaimed`; tab-close leaves `sending` rows that `getEligibleContacts` never re-selects), the `done` flag + `MAX_STAGE_ITERATIONS` bound the loops, the "undefined" counter is fixed on every real-send path (driver coalesces `Number(data.sent ?? 0)`), resumability is correct (idempotent trace/scrub `pending` loops; send adopts `activeRunId` or starts fresh after staleness), and `PATCH /api/client` is admin-gated + double-clamped with the rate re-read fresh each batch (`sendBatchSize` keeps a paced batch ≤180s even at rate 1000). **Compliance:** the driver has NO send path of its own — it only POSTs `/api/campaign`, which ALWAYS runs `getEligibleContacts` (client-scoped `NOT EXISTS opt_outs`, last-10-digit) + the same opt_outs re-check in the atomic `claimForSend`; a STOP mid-run writes a normalized client-scoped `opt_outs` row (webhook signature-gated → `recordOptOut` + `markSuppressed`, idempotent incl. the duplicate-MessageSid recovery) that the next batch excludes AND that refuses any already-fetched contact at claim time — **no window across batches can text a STOPped phone** (refuted); the run-lifecycle extraction + rate change touch NO suppression logic; trace/scrub stay fail-closed under looping (no-match→suppress, flag→suppress-first-then-flag, credit-exhaustion throws before submit so nothing is marked clean); scoping stays client_id+campaign_id on every query incl. the `limit:1` probe; window enforced both per-iteration and per-batch. **Applied (Medium, correctness M1):** `app/api/campaign/route.ts` — wrapped the batch send + finalize in a try/finally so an EXCEPTION (DB error in `runSend`, Twilio throw, finalize-write failure) always closes the run (`finishCampaignRun`, delta 0) instead of stranding it open to 409-block the next Run; drops the ≤6-min self-heal blackout to zero and can't re-introduce the stall via an error path (already-sent contacts stay `sent`, so a re-Run safely resumes the remaining `not_sent`). **Logged, NOT actioned:** (L1) two tabs sharing `activeRunId` co-drive one run — NO double-text (the claim guarantees it), surprising timing only; per-session ownership is module V5. (L2) the driver halts on a `campaign_already_running` 409 with "reload to resume" rather than auto-adopting the handed-back `runId` — **deliberately kept** (auto-adopt would defeat the "block a second concurrent operator" acceptance criterion; legit resume already adopts `activeRunId` from a fresh snapshot, and reload re-fetches it). (cosmetic) `opt_outs`/`campaign_runs` get `client_id`/`campaign_id` via `ALTER` not in the `CREATE TABLE` body — self-doc only; FK + NOT NULL still enforced in the same apply. **Re-verified after the fix:** `npx tsc --noEmit` clean, `npm run build` green, **`npm test` = 173**, **`npm run test:isolation` = 28/28**. **Module V3 is review-clean — ready to generate the V4 prompt (operator lead-guarantee cockpit).**
- 2026-06-24 (Claude Code) **v2 Module V3 (guided pipeline + de-finicky) — DONE. Focused send-driver review still owed.** Turned the babysat pile-of-buttons into a one-click guided pipeline + fixed the two Talan-pilot send bugs + added live send-rate control. **Auto-driving pipeline:** new `components/pipeline-runner.tsx` is a CLIENT-SIDE driver — the operator hits **Run** once and it re-invokes the EXISTING batch endpoints (`/api/skiptrace`, `/api/scrub`, `/api/campaign`) one small batch at a time, looping each stage until it drains, then advancing — so no single call hits the 300s function limit and there's no manual re-clicking. Resumable: all state is in the DB, so closing the tab + clicking Run again resumes (adopts an already-open run via `activeRunId`). **Compliance UNCHANGED (load-bearing):** every send batch still flows through `getEligibleContacts` + the atomic `claimForSend` (with the V2 client-level opt_outs re-check) — auto-resume never bypasses suppression/eligibility, and a STOP landing mid-run excludes that phone from the rest of the run (the claim's opt_out NOT EXISTS fails). I did NOT touch the eligibility/suppression LOGIC — only the driving + UX around it. **Stall fix:** a run now spans the WHOLE driven send (not one HTTP batch). `POST /api/campaign` accepts a `runId`; the run is created on the first batch, heartbeated each batch (new `touchCampaignRun` → new `campaign_runs.last_batch_at` column), and closed (`finishCampaignRun` sets `finished_at`) ONLY on the batch that drains the eligible set (cheap `getEligibleContacts(limit:1)` probe) or when the window shuts mid-batch. The active-run guard (`getActiveCampaignRun`, staleness from `COALESCE(last_batch_at, started_at)`) lets the driver CONTINUE its OWN run (matched by id) while returning 409 + the active `runId` to a fresh caller — second operator blocked, driver never blocks itself. The response carries a `done` flag so the driver knows when to stop. **Counter fix:** every send response returns real `sent`/`failed`; the driver accumulates `Number(data.sent ?? 0)` so the banner can never read "undefined". **Live send-rate control:** new `PATCH /api/client` (`setClientSendRate`, clamped [1,1000]) persists `clients.send_rate_per_hour`; the send loop reads it fresh on every batch → a change takes effect on the NEXT batch with NO redeploy (proven live: PATCH 90 → the next dry-run reported `ratePerHour:90`). The driver sizes each batch from the live rate (`sendBatchSize` ≈ rate/20 clamped [1,50], so a paced batch stays well under 300s) and paces the gap BETWEEN batches (`clientPacingDelayMs`) so the realized rate matches the configured one. **Structure (all files ≤500 lines):** run-lifecycle helpers extracted from `lib/db.ts` (483→430) into new `lib/campaign-runs.ts`; pure client-safe math in new `lib/pipeline.ts` (+`lib/pipeline.test.ts`, 7 tests); `lib/dashboard.ts` exposes `activeRunId`; `components/campaign-controls.tsx` slimmed to manual-stage overrides (trace/scrub/dry-run — NO SMS) since the real send now lives in the driver; `components/dashboard-client.tsx` renders `PipelineRunner` + the slimmed controls. **How I verified:** `npx tsc --noEmit` clean, `npm run build` green (registers `ƒ /api/client`; /dashboard 6.69 kB, no DB driver in the client bundle), **`npm test` = 173** (+7 pipeline batch-size/pacing), **`npm run test:isolation` = 28/28**, `npm run schema` applied live (57 stmts incl. `last_batch_at`); **HTTP smoke** against the dev server (unauth PATCH/POST → 401, PATCH rate=90 → 200 then the next dry-run shows ratePerHour 90 = live, invalid rate=0 → 400, confirm send on the all-sent pilot → `done:true eligible:0 sent:0 failed:0` = real zeros, texted no one, rate restored to 60); and an **end-to-end driven multi-batch** on a throwaway campaign with Twilio MAGIC test numbers (`+15005550006`, API-simulated, nothing delivered): batch1 (limit 1) → `done:false` + runId, run OPEN; a second caller with no runId → **409 campaign_already_running** (+ the active runId handed back); batch2 (own runId) → `done:true`, run CLOSES; batch3 → `done:true eligible:0`; DB confirmed both contacts `sent` exactly once (**no double-send**), exactly 2 outbound messages, a single run closed with `sent_count=2` + heartbeated — then the temp campaign fully cleaned up. **Live DB pristine after:** 1 client, campaign 1, 500 contacts (91 sent), 2 closed runs, rate 60. **Deviations / sub-decisions:** (1) extracted `lib/campaign-runs.ts` (db.ts would exceed 500); (2) `finishCampaignRun` is now ADDITIVE (`sent_count += delta`) so a multi-batch run accumulates per-batch sent counts; (3) added `PATCH /api/client` + `lib/pipeline.ts` + `components/pipeline-runner.tsx`; (4) the snapshot exposes `activeRunId` for resume — **logged limitation:** with the single shared admin password two tabs/operators could both adopt the same run and co-drive (pacing ~2×), but the atomic claim still guarantees NO double-text; per-session run ownership is module V5. **Out of V3 scope (untouched):** operator cockpit (V4), client logins (V5), billing (V6), visual redesign (V7). **Next: the focused send-driver review, then the V4 prompt.**
- 2026-06-24 (Claude Code) **v2 Module V2 FOCUSED REVIEW PASS (agent team) — COMPLETE. No Critical, no High; the load-bearing guarantee HOLDS both ways. Applied 6 cheap Medium/Low hardening fixes; logged the rest.** A 3-reviewer read-only team on Sonnet (compliance / correctness / security) audited the campaigns + client-level-suppression change and challenged each other on "an opted-out phone can never be texted by any campaign" + "no upload or query crosses a client boundary." **All three independently confirmed both HOLD.** Compliance: the `NOT EXISTS (opt_outs …)` exclusion is in BOTH `getEligibleContacts` and `getSendProgress`, client-scoped (`o.client_id`), independent of the contact row's `suppressed` flag, last-10-digit-normalized on both sides, applied across ALL campaigns; every send path (campaign loop, reply route, STOP confirmation, lead forward) was traced and none can reach an opted-out homeowner; STOP/scrub fail-closed still intact per client; the adversarial "new campaign, same opted-out phone, suppressed=false" sequence is excluded (and asserted live in the fixture). Correctness: migration correct + idempotent (campaign 1 = pilot, contacts + campaign_runs backfilled, NOT NULL + index, sequence advanced, no `;`-in-comment), `resolveCampaignForClient` REJECTS a campaign owned by another client (falls back to the requester's default), client-1/no-campaignId behavior unchanged, per-campaign counts mirror the eligibility predicate, `LIMIT null` / `$1::int IS NULL` patterns correct on neon, orphan-job recovery safely client-wide. Security: POST+GET `/api/campaigns` admin-gated (401 first), CSV validated (malformed/empty/missing-Address handled), `insertContact` fully parameterized (no SQLi), contacts land ONLY under the server-resolved client+new campaign (CSV can't supply client_id/campaign_id), the selector/params can't reach another client's campaign; V2 introduces NO new cross-client path beyond the already-logged v1 `?clientId=` gate. **Applied (Medium/Low, all re-verified):** (1) **compliance L2** — `claimForSend` now re-checks the client's opt_outs in the atomic UPDATE, so a STOP arriving in the race window between `getEligibleContacts` and the claim can't be texted (closes the TOCTOU; eligibility is still the primary gate); (2) **compliance M1** — `recordOptOut` normalizes the phone to last-10 before insert so the suppression key + unique index are canonical regardless of caller; (3) **correctness #1** — added a `setval` for the `clients` sequence (mirrors campaigns) to kill a latent PK-collision foot-gun when client creation is wired; (4) **correctness #3** — `getRecentInbound` back to LEFT JOIN + `OR contact_id IS NULL` so an orphan inbound STOP/reply isn't silently dropped from the dashboard feed; (5) **security S1** — upload size cap (10 MB → 413) + row cap (50k → 400) so one oversized CSV can't blow the 300s ceiling and strand a half-imported campaign; (6) **correctness #4** — added 2 isolation assertions proving `resolveCampaignForClient(client 1, <client-2 campaign>)` falls back to client 1's campaign and a client-2 campaign resolves within client 2. **Logged, NOT actioned (out of scope / pre-existing):** compliance M2 (a future messaging-service-only client with no `from_number` wouldn't resolve inbound STOP — onboarding/V-later; documented in code); compliance L1 (`opted_out` is a row count not `DISTINCT phone` — the per-client unique index makes dupes impossible, so it's display-only); correctness #2 (`pending` == `eligible` in `getSendProgress` — pre-existing since Session 3, cosmetic duplicate card); security L2 (csv-parse error detail echoed to the operator — admin-only, matches existing pattern); security L3 (CSV formula-injection on a hypothetical future spreadsheet export — no export exists); and the **carried v1 gate**: `?clientId=` has no per-client access control (must be solved before client #2 — that's module V5). **Re-verified after fixes:** `npm run schema` (56 stmts, idempotent), `npx tsc --noEmit` clean, `npm run build` green (`/api/campaigns` registered), **`npm test` = 166**, **`npm run test:isolation` = 28/28** (the V2 26 + 2 cross-client campaign-resolution), live DB confirmed pristine (1 client, campaign 1, 500 contacts, 0 stray rows). **Module V2 is review-clean — ready to generate the V3 prompt (guided pipeline + de-finicky).**
- 2026-06-24 (Claude Code) **v2 Module V2 (campaigns + CSV list uploader) — DONE. (Then review-passed above.)** Turned the single-list app into a campaigns product: a client runs many campaigns over time, the operator drops in a CSV and goes, and suppression stays CLIENT-level so no campaign can ever re-text someone who opted out. **Schema (`db/schema.sql`, idempotent, applied via `npm run schema` — 55 stmts):** new `campaigns` table (`client_id` FK, `name`, `status` draft|ready|tracing|scrubbing|sending|done|paused default 'draft', nullable `message_template` that inherits the client's when null, index on client_id); pilot seeded as **campaign 1 "Tallahassee pilot" under client 1** + a `setval` advancing the sequence past id=1 so the uploader's auto-INSERT can't collide. `campaign_id int NOT NULL REFERENCES campaigns(id)` added to `contacts` + `campaign_runs` (DEFAULT 1 backfill → DROP default → indexed). **Verified live:** campaign 1 present; all **500 contacts + 2 campaign_runs backfilled to campaign 1, 0 nulls, default dropped, sequence at 1**. trace_jobs/scrub_jobs deliberately NOT campaign-scoped (pinned by contact_ids; crash-recovery must run client-wide). **LOAD-BEARING suppression change:** `getEligibleContacts` + `getSendProgress`'s eligible/pending now ALSO exclude any phone in the client's `opt_outs` (compared on last-10 digits) — so a person opted out under ANY campaign is excluded from EVERY campaign, even a brand-new clean contact row with the same number. **Pipeline scoped by campaign:** added a `ContactScope {campaignId?, limit?}` to the contact-selection helpers; `getContactCounts`/`getSendProgress`/`hasActiveCampaignRun`/dashboard-db feeds take a campaignId; `insertContact`/`createCampaignRun` now REQUIRE a campaignId (no column default → forgotten one fails loudly); `traceBatch`/`scrubBatch`/`getPendingScrubCount` take a campaignId; routes resolve it via new `lib/campaigns.ts resolveCampaignForClient(clientId, ?campaignId=)` which **validates the campaign belongs to the resolved client** (blocks `?clientId=1&campaignId=<other client's>`) and defaults to the client's lowest-id campaign (client 1 → pilot, so no-campaignId == pre-V2 behavior). **CSV uploader:** pure `lib/csv-import.ts parseContactsCsv` (case-insensitive headers, Address required, dedupe within upload by address+zip, import summary) + `app/api/campaigns/route.ts` (admin-gated; POST multipart {name,file} → createCampaign + insertContact per row + status 'ready'; GET lists campaigns for the selector). **Operator UI:** `components/campaign-bar.tsx` (campaign `<select>` switch + upload form) on the dashboard; `DashboardClient`/`CampaignControls` thread `clientId`+`campaignId` into every read/action; counts + leads/reply feeds per-campaign, opt-out list client-level (matches suppression semantics); dashboard header shows client + selected campaign. CLI `npm run trace/scrub` gained `--campaign=N` (omit = client-wide); `import-csv` writes campaign 1. **How I verified:** `npm run schema` (migration confirmed live), `npx tsc --noEmit` clean, `npm run build` green (`ƒ /api/campaigns` registered, /dashboard 5.36 kB — no DB driver in the browser bundle), **`npm test` = 166** (+6 new `lib/csv-import.test.ts`: well-formed parse, address+zip dedup, no-address skip, case-insensitive/optional cols, missing-Address error, empty-file error), **`npm run test:isolation` = 26 assertions** (the V1 22 + **4 NEW client-level-suppression checks**: a fresh clean contact in a NEW campaign with an opted-out phone is EXCLUDED while its `suppressed` flag is still false, a control with a never-opted-out phone IS eligible, and the dup is excluded client-wide too — client 1 still proven byte-unchanged, all fixtures incl. the new campaigns cleaned up). **Live end-to-end:** uploaded a 4-row CSV → created a new campaign, imported 2 (1 address+zip dup + 1 no-address skipped), `?clientId=1&campaignId=N` dashboard scoped correctly (campaign 1 total=500, new campaign total=2, eligible=0 untraced), dry-run scoped, unauth POST/GET → 401 — then the test campaign was deleted so the **live DB is back to the pristine pilot (campaign 1, 500 contacts)**. All touched/new files ≤500 lines (db.ts 468). **Deviations:** (1) contact-selection helpers moved positional `limit?` → `ContactScope` object (single-query via `LIMIT $1`-null + `$1::int IS NULL OR campaign_id=$1`); (2) orphan trace/scrub job recovery stays client-wide by design (documented in both libs); (3) new `lib/campaigns.ts` + `lib/csv-import.ts` + `components/campaign-bar.tsx` + `app/api/campaigns/route.ts`; (4) apply-schema's `;`-in-comments gotcha bit twice (a comment said "over time; each" and another quoted ';') — both fixed. **Next: the focused client-level-suppression + campaign-scoping review.**
- 2026-06-24 (Claude Code) **v2 Module V1 CLIENT-ISOLATION REVIEW PASS — COMPLETE. No Critical, no High; the load-bearing guarantee HOLDS. Applied 5 cheap Medium/Low fixes; logged the rest.** A 3-reviewer read-only agent team on Sonnet (isolation / compliance / correctness) audited the multi-tenant refactor and challenged each other on "no data or suppression ever crosses a client boundary." **All three independently confirmed it HOLDS:** every lib query filters by `client_id` in the WHERE AND sets it on every INSERT (verified table-by-table incl. the dashboard-db JOINs which scope the joined table too); the webhook validates the Twilio signature BEFORE resolving the client, then routes STRICTLY by `To`→owning client (`getClientByInboundNumber`) and acks-noop on an unknown number (dropped, never misrouted); `buildDeps(client)` closes every effect over the owning client's id so an inbound STOP/lead can't touch another client; eligibility is a 5-condition AND that can't widen; scrub/trace fail-closed + the reply suppression gate are scoped to the right client's opt-outs; the migration is correct (Talan=client 1, backfill, NOT NULL, per-client unique indexes, legacy global indexes dropped) and Talan renders **byte-identical** (schema `message_template` == `TALAN_MESSAGE_TEMPLATE`, enforced on the real 500-row CSV); config is fully env→record in live routes (env only as fallback for smoke scripts). **Applied (Medium/Low, all re-verified):** (1) schema — CREATE the per-client unique index BEFORE dropping the legacy global one (no zero-constraint window if apply-schema is interrupted mid-migration); (2) `import-csv --fresh` is now client-scoped FK-order deletes (was a global TRUNCATE = multi-client data-loss hazard) + the final COUNT is client-scoped; (3) `.env.example` `SEND_TIMEZONE` America/Chicago→America/New_York + noted per-client config now lives in the `clients` table (env = fallback only); (4) documented the per-request client-binding invariant on `processInbound`; (5) documented the `getClientByInboundNumber` from_number-only matching limitation (Talan uses a bare number, correct today) + the smoke-twilio env-sender caveat. **Logged, NOT actioned (out of V1 scope):** ⚠️ **`?clientId=` has no per-client access control** — any authenticated operator could pass `?clientId=N` to reach another client's data/sender; **dormant today** (one shared admin password, only client 1 exists → `?clientId=2` 404s) but **must be resolved before a 2nd client is onboarded** (it's exactly module V5, client logins — building it now would violate V1 scope); also the `app/page.tsx` client-1 landing default, the `scrubBatch traceQueueId` external-data-mismatch power param, and `forward_phone` NULL-by-design (env fallback for client 1). **Re-verified after fixes:** `npm run schema` (idempotent, 45 stmts), `npx tsc --noEmit` clean, `npm run build` green, **`npm test` = 160**, **`npm run test:isolation` = 22/22**, and the live DB confirms only the per-client unique indexes remain (legacy dropped), 1 client, 0 stray rows. **Module V1 is review-clean — ready to generate the V2 prompt (campaigns + CSV uploader).**
- 2026-06-24 (Claude Code) **v2 Module V1 (multi-tenant foundation) — DONE. Awaiting client-isolation review.** Turned the single-client app multi-tenant with ZERO behavior change for Talan. **Schema:** new `clients` table (status, plan $, lead_guarantee, billing_day, `from_number`/`messaging_service_sid`, `biz_name`, `message_template`, `forward_phone`, send window/timezone/rate, `optout_confirmation`, `branding` jsonb); Talan migrated in as **client 1** (verified live: biz_name NULL, from_number +18508213720, window 10–19 America/New_York, rate 60, lead_guarantee 50, plan 250000, the verbatim approved template + opt-out copy). Every data table (`contacts`, `messages`, `opt_outs`, `leads`, `campaign_runs`, `trace_jobs`, `scrub_jobs`) got `client_id int NOT NULL REFERENCES clients(id)` + index (existing rows backfilled to 1 via `DEFAULT 1`, then default DROPPED so a forgotten client_id fails loudly); the Session-4 unique indexes are now **per-client** (`opt_outs(client_id, phone)`, `messages(client_id, twilio_sid)`). **Scoping:** added a `clientId` arg to EVERY helper in `lib/db.ts`/`lib/inbox-db.ts`/new `lib/dashboard-db.ts`/`lib/trace-jobs.ts`/`lib/scrub-jobs.ts` and the batch libs (`lib/skiptrace traceBatch`, `lib/scrub scrubBatch`) — filtered in the WHERE + set on every INSERT; no query reads/writes across clients. **Current-client resolution:** operator routes/pages default to client 1 with a `?clientId=` override (`lib/request-client.ts`); the **inbound webhook resolves the owning client by the `To` number** (`getClientByInboundNumber`) and acks-without-processing if no client owns it. **Config env→record:** `renderMessage` (now template-driven off `client.message_template`), `sendOne`'s sender, send window/rate, the lead-forward number, and the opt-out confirmation all come from the client row (account-level secrets stay in env). **Verified:** `npm run schema` applied (45 stmts; Talan=client 1, all 500 contacts + 101 messages + 5 opt_outs + 2 runs + 1 trace_job backfilled to 1, 0 nulls, per-client indexes confirmed, legacy global-unique indexes dropped); `npx tsc --noEmit` clean; `npm run build` passes (all routes + `/dashboard`/`/inbox` register, client bundle has no DB driver); **`npm test` = 160 unit tests green** (sms.test rewritten for the template renderer — Talan's 500-row single-segment + verbatim-opt-out assertions retained as the client-1-unchanged proof; +generic-template + template-change tests; removed the dead B/C variant tests); **`npm run test:isolation` = 22/22 assertions** proving eligibility/inbox/suppression/contact-lookup never cross clients (both directions), the webhook routes strictly by To→client (unknown number → null/refuse), and changing client 1's `message_template` changes its render ONLY — with client 1 proven byte-unchanged and ALL fixtures cleaned up (DB back to 1 client, 0 non-client-1 rows). **Deviations:** (1) `renderMessage` variant→template refactor (dropped unused B/C; `{...}` optional-clause convention reproduces Talan's address fallback; variant column kept for provenance, no longer drives copy); (2) split `lib/db.ts` dashboard-reads into `lib/dashboard-db.ts` (keep db.ts ≤500 → now 419); (3) `lib/twilio`/`lib/forward` take per-client config with env fallback (smoke scripts still work); `forwardLead` falls back to `TALAN_FORWARD_PHONE` env for **client 1 only** (real number is a secret, can't be in committed SQL; Talan's seeded `forward_phone` is NULL); (4) apply-schema gotcha fixed (no `;` allowed inside SQL comments — its `;`-splitter is naive); (5) scripts gained `--client=N` (default 1); new `scripts/isolation-fixture.ts` + `npm run test:isolation`. **Pre-existing (flagged, NOT fixed — out of scope):** `lib/tracerfy.ts` is 536 lines (>500), untouched this session. **Next: the V1 client-isolation agent-team review.**
- 2026-06-24 ✅ **Pilot campaign FULLY SENT — all 91 out (0 failed).** Now collecting replies (1 opt-out so far; 0 leads yet). Fixed the opt-out confirmation copy (removed the business name per Jordan) + redeployed; manually closed an orphaned campaign_run that was blocking "Start send" (the run-close bug). **Pivoting to v2 planning — started `roadmap.md`** (stabilize/de-finicky, UX, multi-tenant product, lead-engine economics, analytics) + prep for a ~2,500-person run tomorrow. Known bugs queued for v2: send-batch run not closing (the stall), "undefined sent/failed" counter, inbox orphan-reply gap, no live send-rate control, auto-messages (opt-out confirmation) weren't surfaced for approval.
- 2026-06-24 🚀 **PILOT LAUNCHED — campaign sending live to the 91.** First batch: **49/91 sent, 0 failed** at the raised `SEND_RATE_PER_HOUR`. **Opt-out path proven with a REAL recipient:** Daniel Roorda (8505459204) replied "Stop" → honored instantly (OPTED OUT=1, opt_outs row, reply feed tagged opt-out). Early opt-out rate ~2% (1/49) — healthy for cold SMS. STOP path had also been pre-verified via a signed request to the live webhook (forged→403, signed STOP→200 + suppression). **Known cosmetic bug (v2 backlog):** the campaign batch-result banner shows "undefined sent, undefined failed" (display only — progress/counts are correct). Remaining: re-click Start send to finish the other 42; then capture delivery/reply/positive-reply/opt-out metrics.
- 2026-06-23 (Claude Code) **Scrub re-billing hotfix DONE — selection bug fixed + credit pre-flight + scrub_jobs durability. Proven with 0 credits spent.** **PART 1 (the leak):** `lib/db.ts getContactsForScrub` now filters `AND scrub_status='pending'` in BOTH queries, so a clean contact (suppressed stays false) is never re-selected/re-billed. **Live fixture proof (no credits):** inserted a `scrub_status='clean'` row + a `'pending'` row → `getContactsForScrub` **excludes the clean**, **includes the pending**; temp rows cleaned up. **PART 2 (credit safety + durability):** (a) **credit pre-flight** — `scrubBatch`, the route, and the CLI runner read `getCredits()` + pending-with-phone count and **refuse BEFORE submitting** if the balance can't cover the batch (`InsufficientCreditsError`; route→402, CLI→exit 2 "need N, have M"). Cost = **1 credit/phone** (`SCRUB_CREDITS_PER_PHONE`). **Live proof (no credits):** `scrubBatch()` threw `InsufficientCreditsError(have 1, need 175 for 175)` before any submit; **credits 1→1 (delta 0)**. (b) **`scrub_jobs` table + `lib/scrub-jobs.ts`** mirror the trace_jobs fix — persist the scrub queue id + `contact_ids` the instant after `submitScrub` (before the poll); `scrubBatch` is RESUMABLE (re-ingest any orphaned `submitted` job free, scoped to its contacts, THEN scrub only still-`pending`). Per-contact verdict writes `markSuppressed` first so a crash leaves the contact suppressed=safe; the free re-ingest re-applies any half-written verdict → a billed contact can't be left unmarked-and-re-billable. (c) **scrub-from-queue stays opt-in** (it bills the whole trace queue, not scoped to pending) — the default phone-list path bills exactly the pending set. **Structure:** pure logic (`classify`, `creditsCoverScrub`, `SCRUB_CREDITS_PER_PHONE`, `InsufficientCreditsError`) split into `lib/scrub-logic.ts` so it unit-tests without the Neon driver and `lib/db.ts` stays ≤500 (now 499). **KEPT IN SCOPE:** send path, inbound webhook, trace logic, and the fail-closed clean/flagged verdict all UNCHANGED. **Verified:** `tsc` clean, **`npm test` = 167 green** (+14 scrub: pre-flight threshold + classify contract; no regressions), `npm run build` passes (routes unchanged), `npm run schema` applied (20 stmts incl. `scrub_jobs` + index); DB back to 500 contacts, 0 leftover temp rows, 0 spurious scrub_jobs. **EXACT COST TO FINISH:** 175 pending-with-phone × 1 = **175 credits**; balance is **1**, so **top up to ≥175 (≈174 more, ~$3.50)**, then `npm run scrub` finishes the 175 cleanly (no re-billing) → final eligible ≈ **91 + the clean share of the 175**.
- 2026-06-23 (Cowork) **Scrub re-billing bug found (credit-safety) — hotfix written; do NOT scrub until fixed.** Root cause in `lib/db.ts getContactsForScrub`: filters `suppressed=false` but NOT `scrub_status='pending'`, so a contact that scrubs **clean** (suppressed stays false) is re-selected and **re-scrubbed every chunk** → re-billing. Live `npm run scrub` re-billed the clean pile (~$5 wasted, balance → 1 credit); the per-chunk clean climb 35→91 was the SAME contacts re-counted (true unique clean = **91**), and ~175 high-id contacts were never reached (genuinely unscrubbed, never billed — NOT recoverable-for-free). DB now: scrub_status clean=91, flagged=175, pending=234 (175 with phone unscrubbed); **eligible=91**. Fix = one line (`AND scrub_status='pending'`) + credit pre-flight + `scrub_jobs` durability (mirrors the trace fix). Wrote `sessions/hotfix-scrub-rebilling.md`. The 91 clean are correct + sendable. Decision pending: launch with 91 now, or fix + top up ~$5 + finish the 175 → ~150 eligible.
- 2026-06-23 (Claude Code) **Durability hotfix DONE — orphaned queue 103802 recovered for free + the bug fixed so it can't recur.** **PART 1 (recover, no new trace):** wrote `scripts/ingest-trace-queue.ts` (`npm run ingest -- 103802`) — re-reads the already-complete queue and applies results through the SAME fail-closed mapping the live route uses (`lib/skiptrace` `ingestTraceQueue` → `getTraceResults` parse → `matchKey UPPER(addr)|UPPER(city)|UPPER(state)` → best-mobile pick; idempotent, only touches `pending`). **Proved 0 credits spent: getCredits before/after = 501 → 501 (delta 0).** DB went `pending 475 → 0`: the queue returned 446 rows → **417 matched** (usable mobile) + **29 landline-only → fail-closed no_match**; the 29 inputs Tracerfy returned nothing for → no_match too. **Final: matched 441, no_match 59, with_phone 441, pending 0 (all 500 traced).** (The hotfix's "~470 with_phone" over-counted — 29 of the 446 matches are landline-only and correctly aren't textable. 0 address-mismatch drops — matchKey is solid.) **PART 2 (fix the bug):** new `trace_jobs` table + `lib/trace-jobs.ts` persists each submitted queue id + its `contact_ids` (ingest scope) the instant after `submitTrace`, **before** the result poll. `traceBatch` is now RESUMABLE — it FIRST re-ingests any orphaned `submitted` job (free re-read, scoped to that job's contacts), THEN traces only still-`pending` contacts; `POST /api/skiptrace` inherits this (no route change). The crash-vulnerable window shrank from the entire multi-minute fetch to the ms between submit and the persist write (residual window documented in overview.md; Tracerfy's queue list is the manual backstop). Also fixed a latent load-order bug in the trace/scrub CLI runners (static `@/lib/db` import hoisted above `config()` → "DATABASE_URL is not set") via dynamic import after dotenv — bootstrap only, no trace/scrub logic touched. **Kept in scope:** send path, scrub compliance logic, and inbound webhook all UNCHANGED. **Verified:** `npx tsc --noEmit` clean, **`npm test` = 153 green** (no regressions), `npm run build` passes (routes unchanged), `npm run schema` applied (18 stmts incl. `trace_jobs` + its index); `trace_jobs` shows queue 103802 `status='ingested'` (417/58) and **0 outstanding orphaned jobs**. All files ≤500 lines (`lib/db.ts` 492 — trace-job helpers deliberately split into `lib/trace-jobs.ts`). **Next: dashboard → run scrub on the 441 matched (~441 scrub credits vs 501 — fits) → Twilio webhook + self-test → pilot send.**
- 2026-06-23 (Cowork) **Durability bug found during the live trace + recovery located — needs a hotfix before continuing.** The dashboard skip-trace of the 475 pending contacts ran on Tracerfy (queue `103802`: 475 rows, **446 matches, ~446 credits spent, status done**) but a browser reload killed the function before results were written back — DB still shows those 475 as `pending`, 0 phones (verified live). Root cause: the Tracerfy job id is never persisted (`campaign_runs` empty, no queue/job column), so an interrupted run orphans paid-for results. **Recoverable for free** — re-reading a completed queue does NOT re-charge. Wrote `sessions/hotfix-trace-recovery.md`: a Claude Code prompt to (1) ingest queue 103802 into contacts without re-tracing, and (2) persist the trace job id + make skiptrace resumable. ⚠️ Do NOT click "Run skip trace" again (would re-charge ~446 credits; only ~501 left).
- 2026-06-23 (Claude Code) **Session 7 REVIEW PASS (agent team) — COMPLETE. Both adversarial claims HOLD; 2 High applied + re-verified; 1 High + the Mediums/Lows logged.** A 3-reviewer read-only team on Sonnet (security / compliance / correctness, read-only `Explore` agents) audited `app/api/reply/route.ts`, `app/api/leads/route.ts`, `app/api/inbox/route.ts`, `lib/reply.ts`, `lib/inbox-db.ts`, `lib/db.ts`/`lib/twilio.ts` as consumed, and the inbox UI. **No Critical found by anyone.** **Cross-validated verdicts (the two load-bearing claims):** "the reply endpoint can't text an arbitrary number" **HOLDS** (security: `contactId` is `Number()`+`isInteger`+`>0` validated, the destination is only the DB row's `phone`, no request field reaches `sendOne`'s `to`); "a reply can never reach an opted-out person" **HOLDS** (security + compliance: the gate is correctly `await`ed BEFORE `sendOne`, and the **dual independent check** in `replyRefusalReason` — `suppressed` flag AND `opt_outs` row, evaluated separately — catches both partial-failure crash directions, phone-normalization mismatch, null/missing contact, and `contactId` type coercion; compliance walked 7 explicit sequences). Every outbound reply (success AND failure) is logged to `messages`; the STOP-after-reply path suppresses in both `contacts.suppressed` and `opt_outs`, so a later reply is refused. **HIGH fixes APPLIED + re-verified (both in `lib/inbox-db.ts` `getInboxThreads`):** (H1) inbox list ordering — `ORDER BY lm.created_at NULLS LAST` sent a lead-only/no-messages contact to the bottom regardless of recency (latent today since every lead has an inbound, but a wrong contract) → now `ORDER BY COALESCE(lm.created_at, l.created_at) DESC NULLS LAST, c.id DESC` (exposed `l.created_at` from the lateral join); (H3) `needs_reply` was `true` for an opted-out contact whose last message was a STOP (UI masked it, but the raw flag was wrong for any other consumer) → now `COALESCE(lm.direction = 'inbound' AND NOT c.suppressed, false)`. **DB-fixture check confirmed:** a suppressed-STOP contact now reports `needs_reply=false` while a clean inbound contact reports `true`; the new ORDER BY executes cleanly. **Lead decision — H2 (no server-side reply idempotency) logged, NOT actioned:** the client `disabled`-while-sending guard is the only double-send protection, so a client-timeout-then-retry or two tabs could duplicate a reply — but this can only ever duplicate a message to a **non-suppressed** person (the gate re-runs every call), so it's a minor UX annoyance, never a TCPA break or a send-to-arbitrary-number; a naive server dedupe would wrongly block a legitimately repeated reply. Same reasoning the Session 4 team used to decline the leads-uniqueness index. **Logged recommendations (not actioned — Medium/Low):** M1 notes `""`-vs-`NULL` hygiene; M2 failed-reply bubble not visually distinct (only `· failed` in the subtext); M3 unchecked boolean cast of `needs_reply` (fine with the Neon driver); L1 `getThread` 3 sequential round-trips (could `Promise.all`); security Lows — raw DB error echoed into the inbox page JSX (admin-only; matches the existing dashboard-page pattern), no `notes` length cap, `SELECT *` in `getContactById`; and the inherent sub-second **TOCTOU** STOP/reply race (flagged Low by both security and compliance — acceptable for a non-transactional serverless MVP; suppression is still enforced on the next attempt). **Re-verification after the fixes:** `npx tsc --noEmit` clean, **`npm test` = 153 green**, `npm run build` passes (registers `ƒ /api/{reply,leads,inbox}` + `ƒ /inbox`, 3.6 kB client — no DB driver leaked to the browser), plus the DB-fixture `needs_reply`/ordering check above. **Module 7 is review-clean; next is Session 6 (deploy + self-test + live pilot).**
- 2026-06-23 (Claude Code) **Session 7 (Module 7: inbox + reply + lead tracking) — DONE. Agent-team review still owed.** Built the in-app reply path so an interested homeowner can be answered from the campaign number (the prior design only pinged Talan). **Schema:** `leads.status text NOT NULL DEFAULT 'new'` (new|contacted|quoted|scheduled|won|lost) + `leads.notes text`, idempotent `ALTER … ADD COLUMN IF NOT EXISTS`, applied via `npm run schema` (verified live: status NOT NULL default `'new'`, notes nullable). **Reply endpoint `POST /api/reply` (the compliance-critical part):** admin-gated (401 unauthed); body `{contactId, body}` — **never accepts a destination phone from the request**, loads the contact and sends ONLY to `contact.phone`; **refuses with 422 `recipient_suppressed`** if the contact is missing / has no phone / `suppressed=true` / has an `opt_outs` row (fail closed); on pass `sendOne(contact.phone, body)` → `recordMessage(outbound)`; failed sends still logged (`status='failed'`). The suppression decision is a pure `lib/reply.ts` `replyRefusalReason(contact, optedOut)` (single source, unit-tested). Replies are intentionally NOT blocked on the campaign send window (1:1 conversational) and may be multi-segment. **`POST /api/leads`** (admin-gated): `{leadId, status?, notes?}` → `setLeadStatus`, status validated against the allowed set (400 `invalid_status` otherwise), returns the updated lead. **Read-only `GET /api/inbox`** (admin-gated): conversation list, or `?contactId=` for one full thread (client refresh after a reply). **DB helpers** in new `lib/inbox-db.ts` (split out so `lib/db.ts` stays ≤500 lines): `getInboxThreads` (one row per contact with any inbound or a lead — name/phone/address, last message + direction + time, `needs_reply`, `suppressed`, lead status), `getThread`, `getContactById`, `isPhoneOptedOut`, `setLeadStatus`. **UI** `/inbox` (admin-gated, unauthed → 307 `/`): conversation list (name, last-message preview, time, **needs-reply** badge; suppressed marked + reply box disabled), thread view (inbound vs outbound distinct) with a reply box (segment count shown, never blocks) + a lead status dropdown/notes posting to `/api/leads`; Inbox link from the dashboard header + each leads-table row links to its thread; the dashboard leads table now shows lead `status`. **How I verified (no real person texted):** `npm run build` passes (registers `ƒ /api/{reply,leads,inbox}` + `ƒ /inbox`, 3.6 kB client — no DB driver leaked into the browser bundle); `npx tsc --noEmit` clean; **153 unit tests green** (+7 new `lib/reply.test.ts` proving the gate refuses suppressed/opted-out/missing/no-phone and clears a clean contact); **live HTTP proof** against the dev server — unauthed `/inbox` → 307, unauthed `POST /api/{reply,leads}` + `GET /api/inbox` → 401; with the admin cookie, `POST /api/reply` to an inserted **suppressed fixture** → **422 `recipient_suppressed` (sendOne never reached)**, to a missing contact → 422 (fail closed), `POST /api/leads` valid → 200 (status=quoted, notes saved), invalid status → 400; all fixtures cleaned up. All files ≤500 lines. **Deviations:** (1) added read-only `GET /api/inbox` (beyond the scope file list) for client refresh — reads only. (2) split Module-7 DB helpers into `lib/inbox-db.ts` (the additions pushed `lib/db.ts` over 500 lines). (3) extracted the suppression gate into pure `lib/reply.ts` (testable without DB/Twilio; the route calls it). (4) `LEAD_STATUSES` in pure `lib/lead-status.ts` so the client dropdown imports it without bundling the Neon driver. (5) off-hours hint computed server-side + passed to the reply box (replies are not window-blocked). **Next: run `sessions/session-7-review.md` (agent-team review of the reply send path), then Session 6 deploy + pilot.**
- 2026-06-23 (Cowork) **Pre-launch code review → adding Module 7 (inbox + reply + lead tracking) before the live send.** Inspected the built code: leads are created + Talan is SMS-pinged + the dashboard shows leads/replies **read-only**, but there is **no in-app way to reply to a homeowner** (the only outbound paths were the campaign blast, the STOP confirmation, and the ping-to-Talan) and **no lead status tracking**. Decided with Jordan to build it now. Wrote `sessions/session-7.md` (dashboard inbox + conversation threads; a `POST /api/reply` reply box that texts back from the campaign number — admin-gated, **suppression-checked, stored-phone-only, logged**; `leads.status`/`notes` + `POST /api/leads`), `sessions/session-7-prompt.md`, and `sessions/session-7-review.md` (agent-team review of the reply send path). Resequenced launch: Module 7 + its review run before Session 6's live send. Updated `modules.md`, `overview.md`.
- 2026-06-23 (Claude Code) **Session 6 Task 0 (approved pilot copy) + deploy prep — DONE. No real send triggered.** **Copy locked in `lib/sms.ts` variant A** = Jordan's exact wording, verbatim, ONLY `Reply STOP to opt out` appended (no trailing period, no business name, nothing else changed): "Hey [NAME] busy season is here, we are working close by if you were interested in window cleaning services at [ADDRESS]. Reply STOP to opt out". **`renderMessage` now takes `address`** (added to its `Contact` type); **[NAME] + [ADDRESS] title-cased** (county data is ALL CAPS → e.g. "7445 BUCK LAKE RD" → "7445 Buck Lake Rd"); blank/entity name → "Hey there". **Single-segment auto-fallback** drops ONLY the "at [address]" clause when the with-address version would exceed one GSM-7 segment. Variants B/C left untouched (future-A/B reference; not used while `AB_VARIANTS=A`). Fixed the opt-out guardrail to accept the period-less verbatim line. **Campaign route** now passes `c.address` into `renderMessage`. **Tests (`npm test` = 146 green):** a new test loads all 500 real rows from `data/tallahassee_test_500.csv` and asserts **every render is single GSM-7 segment AND ends with `Reply STOP to opt out`**; the longest real addresses (28-char `CHARRINGTON FOREST BLVD`) stay single-segment; on the real list the fallback fires for only **2/500** rows (498 keep the address); plus title-case + no-biz-name + verbatim-opt-out + fallback-drops-only-address tests. `npm run build` passes; `npx tsc --noEmit` clean. **Deploy prep (trace/scrub timeout path):** extracted the per-batch fail-closed logic into **`lib/skiptrace.ts` (`traceBatch`)** and **`lib/scrub.ts` (`scrubBatch`)**; both API routes now call these (behavior unchanged, single-sourced so the CLI can't diverge from the reviewed compliance logic). Added **`scripts/run-skiptrace.ts` (`npm run trace`)** and **`scripts/run-scrub.ts` (`npm run scrub`)** — batched, idempotent, resumable CLI runners that loop over the list in chunks against the prod DB, so trace/scrub never hit a serverless function timeout (the send route is already batched/resumable). Both take `--batch`/`--max`/`--delay` flags; trace stops cleanly on insufficient credits with already-traced rows committed. **Note on Vercel timeouts:** the platform default is now 300s on all plans (incl. Hobby), so the routes' `maxDuration=300` is fine; the CLI runners are the preferred pilot path regardless (no timeout, visible per-chunk progress, resumable). No `vercel.json` needed. **Still owed before live send:** Talan's blessing on the wording; real `ADMIN_PASSWORD` in Vercel; self-test pass (Phase A step 5).
- 2026-06-22 (Cowork) Wrote the **Session 6 runbook** (`sessions/session-6.md` + `-prompt.md`): Task 0 = lock the approved copy in `lib/sms.ts`; Phase A = safe Vercel deploy + Twilio webhook + self-test (text the number); pre-send gate checklist; Phase B = live pilot run order (trace→scrub→dry-run→smoke→confirm-gated paced send→capture metrics). **Copy decided with Jordan — his exact wording, verbatim, ONLY "Reply STOP to opt out" appended (no business name, nothing else):** "Hey [NAME] busy season is here, we are working close by if you were interested in window cleaning services at [ADDRESS]. Reply STOP to opt out". **Rejected "Type 2 to opt out"** (system only suppresses on STOP-family keywords); Jordan chose to append the standard STOP line (carriers filter no-opt-out 10DLC traffic). Renderer must title-case [ADDRESS]/[NAME] (data is ALL CAPS) + overflow fallback drops only the address clause; single variant. Twilio confirmed off-trial + 10DLC approved. Recorded in `overview.md` + `sms-copy.md`; updated `modules.md`. **Still owed before the live send:** Talan's blessing on final wording; real `ADMIN_PASSWORD` in Vercel; self-test pass.
- 2026-06-22 (Cowork) **Session 5 (Module 5: Dashboard UI) — DONE. No review team (UI only, no new backend logic).** **Task 0 applied:** `lib/twilio.ts` `DEFAULT_TIMEZONE` `America/Chicago`→**`America/New_York`** (Eastern; still env-overridable via `SEND_TIMEZONE`), decision updated in `overview.md`, `npm run build` re-run green. Built the admin-gated `/dashboard` (server component; unauthed → 307 to `/` login): **count cards** (total / with-phone / scrubbed-clean / eligible / sent / pending / in-flight / failed / suppressed / opted-out / leads), a **send-progress bar** (sent vs known backlog + window OPEN/CLOSED state), a **reply feed** (recent inbound joined to contacts, with a disposition tag derived on the fly from `lib/classify.ts`), the **leads table** (Talan's primary surface — name, address, reply, received time, `forwarded`/`forwarded_at` ping status; a `forwarded=false` row flags a failed ping), and an **opt-out list**. **Controls call ONLY the existing endpoints:** Run skip trace (`POST /api/skiptrace` w/ optional `{limit}` input), Run scrub (`POST /api/scrub`), Dry-run send (`POST /api/campaign {dryRun:true}` → shows eligible + per-variant split), and **Start send (`POST /api/campaign {confirm:true}`) behind an explicit in-UI confirm modal** ("This will text N people" + a typed `CONFIRM`); the send button is disabled outside the window / while a run is active, and the endpoint's `confirmation_required`(400)/`outside_send_window`(409)/`campaign_already_running`(409) responses are surfaced. Polls the read-only `GET /api/dashboard` (7s active / 20s idle). **Files:** `app/dashboard/page.tsx`, `app/api/dashboard/route.ts` (read-only aggregated GET), `lib/dashboard.ts` (read-only aggregator shared by server render + polling), `components/{dashboard-client,count-cards,send-progress,campaign-controls,leads-table,reply-feed,opt-out-list}.tsx` + `components/dashboard-utils.ts`; additive **read-only** `lib/db.ts` helpers (`getDashboardExtraCounts`, `getRecentLeads`, `getRecentInbound`, `getRecentOptOuts` + row types). **No new write/mutation logic; no agent-team review.** **How I verified (dry-run only — no real list batch):** `npm run build` registers `ƒ /dashboard` + `ƒ /api/dashboard`; 139 unit tests green; served-page checks — unauthed `/dashboard` → 307 to `/`, unauthed `GET /api/dashboard` → 401, authed snapshot → `{total:500, eligible:0, window "10:00–19:00 America/New_York"}` (trace/scrub deferred so eligible=0), dry-run → eligible + per-variant split sends nothing, real send w/o confirm → 400 `confirmation_required`. All files ≤ 500 lines. **Deviations:** (1) added the read-only `GET /api/dashboard` + `lib/dashboard.ts` aggregator (beyond the scope file's exact file list) so the initial server render and the client polling share ONE shape — reads only. (2) inbound disposition tags derived live from `lib/classify.ts` (no stored column). (3) send model = "run batch / continue" (no pause endpoint; resumable). (4) added an "Open dashboard →" link on the authed `/` home page.
- 2026-06-22 (Cowork) Wrote **Session 5** prompts — `sessions/session-5.md` + `sessions/session-5-prompt.md` (admin-gated dashboard: count cards, send-progress bar, reply feed, **leads table as Talan's primary surface**, control buttons calling the existing endpoints with a **confirm-gated** start-send; reads only, no new backend logic; **no agent-team review pass** — UI only). Folded the outstanding `SEND_TIMEZONE`→`America/New_York` fix in as Task 0. Updated `modules.md`, `status.md`, `handoff.md`.
- 2026-06-22 (Cowork) **Session 4 REVIEW PASS (agent team) — COMPLETE. 1 Critical + 1 High fixed + re-verified; cheap Low fixes applied; rest logged.** A 3-reviewer read-only team on Sonnet (security / compliance / correctness, read-only `Explore` agents) audited `app/api/webhook/twilio/route.ts`, `lib/inbound.ts`, `lib/forward.ts`, the new `lib/db.ts` helpers, and `lib/classify.ts` as consumed, and cross-checked `getEligibleContacts`. The two adversarial claims were challenged across reviewers. **Cross-validated verdicts:** "a forged request can do nothing" **HOLDS** (all three confirmed: signature validation at route.ts gates every DB write/forward; 403 on invalid/missing, 500 on missing token, before `processInbound`). "STOP is always honored" held in all standard cases (standalone / mixed-with-interest / orphan / casing / punctuation) — but correctness found the real break (the Critical below). **CRITICAL (fixed) — STOP suppression could be stranded on a partial-failure retry:** the dedupe log (`logInboundOnce`) commits BEFORE `recordOptOut`/`markSuppressed`, and neon runs each statement as a separate autocommit HTTP request, so a crash after the log but before suppression would let a deduped Twilio retry return `{kind:'duplicate'}` and leave a STOP un-suppressed + still textable (TCPA fail). **Fix:** on the duplicate branch in `lib/inbound.ts`, re-apply the opt-out idempotently (`applyOptOut` → `recordOptOut` + `markSuppressed`) so a retry re-suppresses even after a mid-process crash, while still NOT re-sending the confirmation or creating a lead (exactly-once → zero-or-one, never two). **HIGH (fixed) — `opt_outs` had no unique constraint:** required to make the recovery idempotent. Added `CREATE UNIQUE INDEX idx_opt_outs_phone ON opt_outs(phone)` + `recordOptOut` now `INSERT … ON CONFLICT (phone) DO NOTHING`. **Cheap fixes also applied:** (1) `findContactByPhone` regex `'\D'` → `'[^0-9]'` — a JS template literal cooks `'\D'` to `'D'` and neon sends the cooked text, so `'\D'` stripped literal `D`s, not non-digits (masked today since stored phones are pre-normalized 10-digit, but a latent compliance-tail risk: a formatted stored phone → STOP match-miss → no suppression); both security+compliance flagged it, empirically confirmed. (2) Warn when `TWILIO_WEBHOOK_URL` is unset (the x-forwarded-* reconstruction is safe on Vercel and can't be used to forge — that needs the auth token — but a host/proto mismatch behind another proxy would REJECT legit Twilio requests; prefer the explicit env in prod). **Cross-challenge resolutions (documented):** compliance's "stop texting me not caught by isOptOut" claim was **REFUTED** empirically (`isOptOut('stop texting me')`/`'please stop texting me'`/`'stop calling me'` all return true — it matches "stop" as a bounded token anywhere, and runs before classification; an existing test already covered it). Correctness's leads-uniqueness recommendation was **DECLINED** with reasoning: a `(contact_id, reply_text)` index would drop a legitimately repeated lead (e.g. "yes" texted twice on different days with different MessageSids → two real pings Talan should get); the `messages(twilio_sid)` gate is the correct idempotency boundary, the lead crash-window loses a lead (not double-pings), and the inbound is still logged so the reply isn't lost. **Logged recommendations (not actioned):** the leads defense-in-depth above; `TWILIO_ADVANCED_OPT_OUT` is already documented in `.env.example`. **Post-fix verification by the correctness reviewer:** confirmed all fixes correct and closed every finding; two cheap quality nits it raised were folded in (a stale `logInboundOnce` docstring; an `emitConfirmation=false`+duplicate STOP test). **Re-verification:** `npm test` = **139 unit tests green** (added duplicate-STOP-recovery + orphan + emitConfirmation-false-retry tests; updated the two duplicate-MessageSid tests for the new recovery behavior), `npm run build` passes + registers `ƒ /api/webhook/twilio`, `npm run smoke:webhook` 5/5 (forged/tampered/wrong-URL/missing → 403; valid accepted), `npm run schema` applies cleanly (14 stmts incl. the 2 new unique indexes). Two extra integration checks (shared in-memory fake DB) proved: STOP+interest → suppress + exactly one confirmation + no dup on retry + never forwarded; and a simulated crash-after-log → deduped retry RE-APPLIES suppression (STOP not stranded).
- 2026-06-22 (Cowork) **Session 4 (Module 4: inbound webhook — STOP + triage + forward) — DONE. Review pass still owed.** Built `app/api/webhook/twilio/route.ts` (POST), `lib/inbound.ts` (decision core), `lib/forward.ts` (lead SMS ping), `lib/inbound.test.ts` (13 tests), `scripts/smoke-webhook.ts` (+ `npm run smoke:webhook`). Additive `lib/db.ts` helpers: `findContactByPhone`, `recordOptOut`, `markLeadForwarded`, `logInboundOnce` (also widened `recordMessage.contactId` to `number|null` for orphan/confirmation logging). **Auth = Twilio signature:** the route validates `X-Twilio-Signature` with `twilio.validateRequest` over the public URL (`TWILIO_WEBHOOK_URL` override, else reconstructed from `x-forwarded-*`) + posted params, and returns **403 before any DB write/forward** on invalid/missing/tampered signatures (missing `TWILIO_AUTH_TOKEN` → 500, fail closed). **Processing order (in `lib/inbound.ts`):** match sender (`findContactByPhone(normalizePhone(From))`) → **`logInboundOnce` = atomic idempotency gate** (INSERT…ON CONFLICT DO NOTHING on the new partial UNIQUE index `messages(twilio_sid)`; a duplicate MessageSid stops before any side effect) → **STOP first, unconditionally** (`isOptOut` → `recordOptOut` + `markSuppressed(id,'opt_out')` + one TwiML `<Message>` confirmation; STOP beats classification even when the body also has interest words; orphan STOP records the opt-out by phone, no suppress) → else `classifyInterest`: `interested` + matched → `createLead` + `forwardLead` (+ `markLeadForwarded` on send success; a failed ping leaves the lead with `forwarded=false`, still on the dashboard); `not_interested`/`neutral`/orphan-interested → logged only. **Imported `isOptOut`/`classifyInterest` from `lib/classify.ts` and `sendOne` from `lib/twilio.ts` — did NOT rebuild.** **How I tested (no DB/Twilio needed):** 13 `node:test` unit tests on `processInbound` with injected fakes cover STOP precedence (incl. STOP+interest mixed, orphan STOP, confirmation-once, `emitConfirmation=false`), idempotency (duplicate MessageSid → no opt-out/lead/forward, on both STOP and interested bodies), and classification routing (interested → lead+forward, failed-ping → lead survives `forwarded=false`, not_interested/neutral/orphan → log only) — **137 tests total green**. **Signature gate proven** via `npm run smoke:webhook`: a correctly-signed request is accepted; forged, tampered-body, wrong-URL, and missing-signature requests are all rejected (5/5). `npm run build` typechecks the route and registers `ƒ /api/webhook/twilio`. **Deviations:** (1) added `lib/inbound.ts` (not in the scope file's file list) to make the decision logic unit-testable with fakes — keeps the route thin (transport only) and avoids module-mocking; logged as a decision. (2) Added a partial UNIQUE index `idx_messages_twilio_sid_unique ON messages(twilio_sid) WHERE twilio_sid IS NOT NULL` to `db/schema.sql` (the atomic dedupe gate) — apply with `npm run schema` before the live webhook test. (3) STOP confirmation sent via **TwiML `<Message>`** (single send, no second API call); new env `TWILIO_ADVANCED_OPT_OUT=true` suppresses it if a Twilio Messaging Service with Advanced Opt-Out is ever enabled (Twilio would send it → avoid double-confirm). (4) New env `TWILIO_WEBHOOK_URL` (optional public-URL override for signature validation behind a proxy). **Owed before the live webhook test:** `npm run schema` (adds the unique index), `TALAN_FORWARD_PHONE` in `.env.local` (own phone works as a stand-in), and configuring the Twilio inbound webhook URL (Session 6 / a tunnel for local). **Next: run `sessions/session-4-review.md` (agent-team security/compliance/correctness).**
- 2026-06-22 (Cowork) **Dropped Resend/email; lead delivery = dashboard + instant SMS ping to Talan** (decided with Jordan). `lib/forward.ts` is SMS-only; lead row is the durable record so a failed ping never loses a lead. Removed Resend from CLAUDE.md (stack + env), overview.md (stack + scope + decision), modules.md (Module 4 detail), and all three Session 4 prompt files. Also confirmed the send-window timezone = `America/New_York` (Eastern) and recorded it in overview.md.
- 2026-06-22 (Cowork) Wrote **Session 4** prompts — `sessions/session-4.md` (inbound webhook: Twilio **signature validation**, instant+permanent STOP via `isOptOut`, `classifyInterest` triage, lead forwarding to Talan via SMS + optional Resend email, idempotent on Twilio retries, all inbound logged), `sessions/session-4-prompt.md`, and `sessions/session-4-review.md` (agent-team security/compliance/correctness review). Confirmed the **send-window timezone decision: `America/New_York`** (Tallahassee is Eastern, not Central — the `sms-copy.md` "CT" note was wrong; keeps recipients in their true 10am–7pm local window and inside TCPA's 8am–9pm rule). Updated `modules.md`, `status.md`, `handoff.md`.
- 2026-06-22 **Session 3 REVIEW PASS (agent team) — COMPLETE. Critical + High fixes applied + re-verified.** A 3-reviewer read-only team on Sonnet (security / compliance / correctness) audited `lib/twilio.ts`, `app/api/campaign/route.ts`, `app/api/scrub/route.ts`, `getEligibleContacts()` + the Session 3 db helpers, and `lib/sms.ts` as consumed. **Cross-validated verdicts:** compliance + correctness independently confirmed **no path can text an ineligible (suppressed/unscrubbed/opted-out/already-sent) contact** and **no crash+retry can double-text** (the atomic per-contact claim holds even under concurrency — concurrency is a spend/pacing risk, not a double-text one). **The one Critical was auth:** every API route was unauthenticated. **Fixed (Critical+High), all re-verified:** (1) **CRITICAL** — added the `isAuthed` admin-cookie gate to `POST/GET /api/campaign`, `POST /api/scrub`, `POST /api/skiptrace`; verified end-to-end with curl (401 unauthed on all; authed dryRun→200 w/ eligible+perVariant, authed GET→200 w/ in_flight, authed real-send-without-confirm→400). (2) **HIGH** — routes return a generic error label, never raw `err.message` (raw errors can carry Twilio/DB internals). (3) **HIGH** — concurrent-run guard: new `campaign_runs.finished_at` + `hasActiveCampaignRun()` (6-min staleness, so a timed-out run never blocks future runs) returns 409 if a run is in flight (also distinguishes a completed run from a timed-out one for accounting). (4) **HIGH** — send window re-checked every loop iteration (`stoppedForWindow`), so a run that starts late stops at the window close instead of texting past it. (5) **HIGH** — A/B variant assigned by an attempt-index that advances only on actually-sent contacts, so the realized split stays balanced when rows are skipped; `perVariant` now counts assignments (matches the DB `variant` writes). (6) **HIGH/MED** — overflow contacts are drained (claim→`failed`) so they leave the eligible pool instead of re-rendering+re-skipping forever (also keeps the eligible count honest). (7) **HIGH(visibility)** — `getSendProgress` gained an `in_flight` bucket so stuck `'sending'` rows surface. **Lead decision (recorded in `overview.md`): do NOT auto-reset stuck `'sending'` rows** — a `'sending'` row may already have been texted before the crash, so resetting it to `'not_sent'` would convert the safe silent-skip into a **double-text**; recovery stays a manual, Twilio-log-checked op. **Cheap Low fixes also applied:** scrub null-phone guard (`if (!c.phone) continue`), `localHour` NaN now throws instead of silently using server/UTC time, failure `messages.status` stores `'failed'` (code logged separately), pacing-skip + `limit`-semantics documented. **Left as logged recommendations (Medium/Low):** the **CT-vs-ET send-window** question (recommend `SEND_TIMEZONE=America/New_York` — Tallahassee is Eastern; 10–19 CT = 11–20 ET — pending Talan's call), lazy DB-client init, `pending` vs `eligible` naming. `npm run schema` (12 stmts), `npm run build`, 124 unit tests, and `npm run smoke:twilio` (re-sent, delivered) all pass post-fix; eligibility + active-run guard + in_flight re-verified with a fixture.
- 2026-06-22 **Session 3 (Module 3: Twilio send engine + suppression + `scrub_status` guard) — DONE. Smoke gate PASSED live.** Built `lib/twilio.ts` (lazy SDK client from env; `sendOne` returns a typed `SendResult` instead of throwing; auth token never logged; `getSenderField` prefers `TWILIO_MESSAGING_SERVICE_SID` over `TWILIO_FROM_NUMBER`; pacing from `SEND_RATE_PER_HOUR` (default 60/hr → `pacingDelayMs`); `withinSendWindow`/`sendWindowLabel`/`localHour` for a `[10:00,19:00) America/Chicago` window, all env-overridable), `app/api/campaign/route.ts` (POST runs/resumes a paced send over `getEligibleContacts`; `{dryRun}` reports eligible count + per-variant split and sends nothing; a real send requires `{confirm:true}` AND `withinSendWindow()`; per contact: round-robin A/B variant → `renderMessage` → `withinSingleSegment` assert (skip+log overflow, never 2 segments) → **atomic claim** `not_sent→sending` → `sendOne` → `recordMessage(outbound)` → `send_status='sent'|'failed'`, paced between sends; GET returns `{eligible, sent, pending, failed, suppressed, opted_out}`), and `scripts/smoke-twilio.ts` + `npm run smoke:twilio` (sends ONE message to `SMOKE_TO_NUMBER` via `renderMessage`, prints SID/status; does not touch the DB). **`scrub_status` guard:** added `contacts.scrub_status` ('pending'|'clean'|'flagged', default 'pending') + `contacts.variant` to `db/schema.sql` (idempotent `ALTER ... ADD COLUMN IF NOT EXISTS`) applied via `npm run schema`; `app/api/scrub/route.ts` now writes `scrub_status='clean'` on a clean verdict and `'flagged'` on any flag/suppress/scrub_error; `getEligibleContacts()` now requires `phone IS NOT NULL AND suppressed=false AND scrub_status='clean' AND send_status='not_sent'` (a matched-but-unscrubbed contact is 'pending' → **not eligible**). Added additive db helpers `setScrubStatus`, `claimForSend`, `setVariant`, `setSendStatus`, `createCampaignRun`, `finishCampaignRun`, `getSendProgress` (existing signatures untouched; `getEligibleContacts` gained an optional `limit`). **Imported `renderMessage`/`withinSingleSegment` from `lib/sms.ts` — did NOT rebuild templating.** **Live smoke PASS:** SID `SM5a038a0cc2526ea8f316000a0fc40063`, body `"Hi Jordan, this is Talan Window Cleaning - we clean windows for homes in 32301. Would a free quote be useful? Reply STOP to opt out."` (132 chars, 1 GSM-7 segment), from `+18508213720` → `+16199933683`, status polled to **`delivered`** (errorCode null). **Verified with a temporary fixture (inserted + cleaned up):** eligibility selects ONLY clean/eligible rows (pending-scrub, suppressed, already-sent, no-phone all excluded); the atomic claim is idempotent (claim once → true, re-claim → false) so a re-run never double-texts, and a claimed row drops out of eligible. `npm run build` + 124 unit tests pass. **Deviations:** (1) added `contacts.variant` + a `'sending'` transient `send_status` (not in the original schema comment) — the claim sets `'sending'` before the network call so a crash fails toward NOT texting; `'sending'` rows are a manual-inspection state, not auto-retried. (2) Added env vars `SMOKE_TO_NUMBER`, `BIZ_NAME`, `SEND_RATE_PER_HOUR`, `SEND_WINDOW_*`, `SEND_TIMEZONE`, `AB_VARIANTS`, `TWILIO_MESSAGING_SERVICE_SID` (documented in `.env.example`). (3) Installed the `twilio` SDK (6.0.2) — the only new dependency. **Pre-flight still owed before a REAL list send:** confirm the Twilio account is off trial + `+18508213720` has A2P 10DLC approved, and get Talan's sign-off on `sms-copy.md`.
- 2026-06-22 Decided to **defer the full 500 trace/scrub run** and build the rest of the app first, and to **add a `scrub_status` guard** in Session 3 (provable "scrub passed" per contact; send gates on `scrub_status='clean'`). Wrote `sessions/session-3.md` (Twilio send engine + suppression + scrub_status + A/B + send window + smoke-to-own-phone gate), `sessions/session-3-prompt.md`, and `sessions/session-3-review.md` (agent-team compliance/security/correctness review of the send path). Updated `modules.md`, `overview.md` (both decisions).
- 2026-06-22 **Session 2 (Module 2: Tracerfy skip-trace + scrub) — DONE. Smoke gate PASSED live.** Built all four files: `lib/tracerfy.ts` (typed client: `getCredits`, `submitTrace`, `getTraceResults`, `submitScrub`, `getScrubResults`; Bearer auth, submit→poll model, every call wrapped in typed `TracerfyError`, key never logged), `app/api/skiptrace/route.ts` (POST; traces only `skiptrace_status='pending'` so re-runs are idempotent; writes phone/phone_type + `matched`; **fails closed** on no-match → `no_match` + `suppressed=true`/`suppress_reason='no_match'`; credit pre-flight guard), `app/api/scrub/route.ts` (POST; suppresses on any of the 4 flags — `litigator` else `dnc`; **fails closed** on missing/ambiguous → `scrub_error`; a number stays eligible ONLY on an explicit clean verdict), `scripts/smoke-tracerfy.ts` + `npm run smoke:tracerfy` (one-record trace→scrub round trip, prints raw + parsed). Added additive db helpers `setTraceResult`, `getContactsForSkiptrace`, `getContactsForScrub` (existing signatures untouched). **`npm run build` + `tsc --noEmit` pass.** **Live smoke PASS on record #1 (Robert Brown, 7445 BUCK LAKE RD):** trace matched → mobile `8505450056` (`primary_phone_type=Mobile`); scrub CSV returned `national_dnc=Y,state_dnc=Y,dma=N,litigator=N,is_clean=No` → verdict `suppressed=true, suppress_reason='dnc'` (correct). Request shape was corrected through four live responses before this pass: `415` (→ `/trace/` needs multipart, not JSON), `400` (→ `mail_address_column` required; mapped mail=situs), `402` (→ insufficient credits — credits since loaded, balance now ~998), then the clean match. **Verified-live shapes (in `overview.md`):** trace results are **inline JSON** from `GET /queue/:id` (no trailing slash; cols incl. `primary_phone`,`primary_phone_type`,`mobile_1..5`,`landline_1..3`); scrub results are a **CSV download** from `GET /dnc/queue/:id` (`download_url`); flags encoded `Y/N`; phone pick = best mobile only (landline-only ⇒ no-match/suppress); result→contact map = `UPPER(address)|UPPER(city)|UPPER(state)`. **Deviation from spec/instruction:** spec assumed an async CSV download for *both* trace and scrub and a possible APN input — live behavior: trace is inline JSON (only scrub is CSV), address-based input (no APN), and `mail_address` is required. **Compliance note for Session 3:** after trace, matched contacts are `suppressed=false` *before* scrub runs — Module 2 suppresses the flagged ones, but the send path (Session 3) MUST run only after scrub and treat "not yet scrubbed" as ineligible (no schema flag added — kept in scope; flagged in handoff). **Not yet run: the full 500** (awaiting go-ahead; ~500 trace + ~matched scrub credits needed, balance ~998).
- 2026-06-22 **Module P (pure logic: classifier + SMS templating) — COMPLETE (agent team).** Built by a single Claude Code session running an **agent team** (lead + 2 Sonnet teammates with non-overlapping file ownership): teammate "classifier" → `lib/classify.ts` (`isOptOut`, `classifyInterest`); teammate "templating" → `lib/sms.ts` (`renderMessage`, `isNonHumanName`, `segmentInfo`, `withinSingleSegment`); lead set up the test runner and ran the compliance review. **Pulled `lib/classify.ts` and `lib/sms.ts` forward out of Modules 3–4 so they're done and tested ahead of the spine.** `isOptOut` covers the full CTIA set (STOP/STOPALL/STOP ALL/UNSUBSCRIBE/CANCEL/END/QUIT/OPTOUT/OPT OUT/REMOVE) with documented fail-safe over-matching and word-boundary tokenization (no false-trigger on "stopping"/"endpoint"). `renderMessage` guarantees every output ends with "Reply STOP to opt out." and stays single-segment GSM-7; entity/empty names fall back to "Hi there". **Test runner = `node:test` via `tsx` (`npm test` = `tsx --test lib/*.test.ts`), zero new deps.** 124 unit tests green (86 classify + 80 sms... 38 overlap counts aside, suite reports 124 total), `npm run build` passes. Lead compliance review confirmed all four acceptance checks. **Deviation:** sms-copy.md creative used em dashes (not GSM-7) + ran long; `renderMessage` adapts to ASCII hyphens + trimmed copy to fit one segment — as-implemented copy recorded in `sms-copy.md`, **still needs Talan sign-off** before Session 3.
- 2026-06-22 **Session 1 (Module 1: Scaffold + data model + DB) — COMPLETE.** Next.js 14.2 (App Router, TS, Tailwind) deployable skeleton. `db/schema.sql` (contacts, messages, opt_outs, leads, campaign_runs + the three `contacts` indexes) applied to Neon via `scripts/apply-schema.ts`. `lib/db.ts` exports the `@neondatabase/serverless` client + typed helper signatures (`getEligibleContacts`, `insertContact`, `markSuppressed`, `recordMessage`, `createLead`, plus `getContactCounts` for the stub). `scripts/import-csv.ts` imported all **500** rows into `contacts` (phone null, skiptrace_status 'pending'); idempotent — re-run inserts 0 / skips 500 (verified). `app/page.tsx` admin gate (single password from `ADMIN_PASSWORD`, httpOnly cookie) verified end-to-end in a headless browser: unauthed→login form; wrong pw→rejected, error shown, no data leak; correct pw→dashboard shows "500 contacts loaded, 0 with phones, 0 suppressed". `.env.example` documents every var. `npm run build` passes. **Deviations:** (1) added `scripts/apply-schema.ts` + `npm run schema` (not named in the spec) — the 0.10.x `neon()` HTTP function has no `.query()`, so file-sourced statements run via a fabricated tagged-template call. (2) Added `ADMIN_PASSWORD=leadflow-dev` to the gitignored `.env.local` for testing — **change for prod / set in Vercel.**
- 2026-06-22 Sourced Leon County certified tax roll (111k parcels), filtered to owner-occupied single-family in 5 target zips, sampled clean 500-record test list (`data/tallahassee_test_500.csv`).
- 2026-06-22 project-prep: created overview.md, status.md, handoff.md, RULES.md.
- 2026-06-22 dev-prep: created CLAUDE.md, modules.md, sessions/session-1.md.
- 2026-06-22 Decided DB = Neon Postgres (via Vercel Marketplace integration; driver `@neondatabase/serverless` — old `@vercel/postgres` is deprecated) and build approach (sequential per-module + parallel review team on sensitive modules); locked DB choice across CLAUDE.md, overview.md, session-1.md; wrote finalized Session 1 kick-start prompt (`sessions/session-1-prompt.md`).
- 2026-06-22 Infra stood up: pushed repo to GitHub (`JXalent1/leadflow`), created Vercel project (`leadflow1`), connected Neon DB (`neon-yellow-mountain`) to it (env vars injected into Vercel for Dev/Preview/Prod). First deploy is a 404 placeholder — expected until Session 1 ships the app.
- 2026-06-22 Adopted a parallel build plan: kept the Tracerfy→Twilio→webhook spine sequential, but extracted the two pure-logic pieces (reply classifier `lib/classify.ts` + SMS templating `lib/sms.ts`) into **Module P**, an agent-team session that runs alongside Session 2. Wrote `sessions/session-2.md` + `sessions/session-2-prompt.md` (Tracerfy, grounded in the real async API: trace→poll→scrub-from-queue, 4 DNC/litigator flags) and `sessions/session-pure-logic.md` (the parallel team session). Updated `modules.md` (added Module P + Parallelization section; M3/M4 now consume the pulled-forward libs).

## In progress
- **Session 6 (deploy + live pilot) — next.** Module 7 is built and review-clean (agent-team pass complete). Run `sessions/session-6-prompt.md`: Vercel env + deploy, point the Twilio webhook, self-test, gate checklist, then the paced pilot send.

## Deferred (on purpose)
- ~~**The full 500 trace run.**~~ **DONE 2026-06-23** via the queue-103802 recovery (all 500 traced; 441 matched/with_phone; credits 501 unchanged by the recovery).
- **Scrub of the 175 remaining pending — BLOCKED ON CREDITS.** The scrub re-billing bug is now FIXED (selects pending-only + credit pre-flight + `scrub_jobs` resumable). Current DB: scrub_status clean=**91**, flagged=**175**, pending=**175** (all with phone); the 91 clean are correct + sendable now. To finish: **top up Tracerfy to ≥175 (have 1; ≈174 more, ~$3.50)**, then `npm run scrub` — it pre-flights (refuses cleanly if short) and bills exactly the 175, no re-billing. Then smoke send → full send. (Or launch with the 91 clean now and finish later — either way the 91 are correct.)

## Next up — Session 6 (deploy + pilot)
0. ✅ **Session 7 (inbox + reply + lead tracking) — BUILT + agent-team review-clean** (both adversarial claims HOLD, no Critical; 2 High applied + re-verified; H2 idempotency + Medium/Low logged).

### Session 6 (after Module 7 is in) — runbook ready: `sessions/session-6-prompt.md`
1. ✅ **Task 0 (Claude Code) — DONE:** approved copy locked in `lib/sms.ts` variant A (address pass-through, title-case, single-segment auto-fallback, single variant); CLI trace/scrub runners added. 146 tests + build green.
2. **Phase A — Deploy (safe):** Vercel env (incl. **real `ADMIN_PASSWORD`**, `SEND_TIMEZONE=America/New_York`, `BIZ_NAME`, `TALAN_FORWARD_PHONE`, `AB_VARIANTS=A`), deploy, point the Twilio Messaging webhook → `/api/webhook/twilio` + set `TWILIO_WEBHOOK_URL`, then **self-test** by texting the number (interested + STOP).
3. **Gate checklist** (Twilio ✓; + real admin pw, Talan wording blessing, credits, self-test pass, accept exposure), then **Phase B — live pilot run:** **`npm run trace -- --max=25`** first (then full) → **`npm run scrub`** → dry-run → smoke → confirm-gated paced send in 10am–7pm ET → capture metrics.
- ✅ **`npm run schema` already applied** — the unique indexes (`messages(twilio_sid)`, `opt_outs(phone)`) are live.

## Blocked / waiting on
- **Prod admin password:** local `.env.local` has a throwaway `ADMIN_PASSWORD=leadflow-dev`. Set a real `ADMIN_PASSWORD` in Vercel project env before the app is publicly reachable.
- ~~Tracerfy API key~~ — obtained + added to `.env.local` (2026-06-22); auth + full trace/scrub round trip verified live.
- ~~Tracerfy credits~~ — loaded (2026-06-22, balance ~998). **Budget check before the full 500:** ~500 trace + ~matched-count scrub credits; ~998 is likely enough but tight — the skiptrace route's pre-flight guard 402s rather than partially tracing if short.
- Twilio — **send path built + smoke-tested (Session 3).** From number **(850) 821-3720** (`+18508213720`) live; SID + auth token in `.env.local` (and owe-Vercel-env), never in repo. Smoke SMS delivered to Jordan's phone. ⚠️ **Still required before a REAL list send:** confirm the account is upgraded from trial and `+18508213720` has **A2P 10DLC** approved (smoke to one's own number can pass even on a setup that carriers would later filter at scale). **Inbound webhook handler `/api/webhook/twilio` is now BUILT (Session 4);** pointing the Twilio Messaging webhook at its public URL (+ setting `TWILIO_WEBHOOK_URL`) is a Session 6 / local-tunnel step.
- Final approved SMS copy — needed by Session 3. The **as-implemented, single-segment versions** that `lib/sms.ts` actually sends are now recorded in `sms-copy.md` ("As-implemented" section); they differ slightly from the original creative (em dash → hyphen, trimmed to fit one GSM-7 segment) and still need Talan's sign-off.
- Credentials shared in chat in plaintext — recommend rotating the Twilio auth token + Tracerfy key after the pilot.
- Talan's cell for the SMS ping (`TALAN_FORWARD_PHONE`) — needed to test Session 4's forward (your own phone works as a stand-in). **Delivery method decided: dashboard + SMS ping, no email/Resend.**
